# Trust audit of the unattended releases — 5 Sep 2026 (#778 clause 2)

Produced by a workflow: one agent per release re-proved it with `tools/spotcheck.sh` and re-read its diff against its claims; one agent per parked item read the entry and the code it names. **The refutation stage did not run** (every refuter hit the session limit at ~11:00, resets 12:10pm Perth), so everything below is an UNREFUTED reading. Three findings were verified by hand and fixed in v15.57 (#779, #780, #781); #570 was closed on his own recorded answer. Treat every other line as a lead that must be checked against the code before it is acted on — the same way those four were.

## Releases v15.30–v15.56: 27 audited

effort: {'real': 19, 'thin': 5, 'ceremony': 3} · claim holds: {'yes': 16, 'partly': 11} · spotcheck: {'PROVEN': 12, 'PROVEN-WEAK': 13, 'tests-only': 1, 'NOT-PROVEN': 1}

### v15.30 (queue 736) — spotcheck PROVEN, claim yes, effort real
Claims the rotate dial's 45° snap tolerance was captured once at panel build so a later magnet toggle was ignored, and is now read on every pointermove; the diff (js/inspector.js:4663,4667) replaces `const SNAP_TOL = magnetOn() ? 7 : 0` with `snapTol = () => (magnetOn() ? 7 : 0)` called inside the move handler, which is exactly that.

- [low] **Pre-existing, not introduced: with the magnet off the dial still shows 'snapped' and vibrates when raw lands exactly on a notch** (js/inspector.js:4667) — Not caused by this commit (dates from queue 620's `? 7 : 0`), noted because the release's own comment says 'magnet off → nothing holds'. `held = Math.abs(raw - notch) <= snapTol()` with tolerance 0 is true whenever raw is an exact multiple of 45. State: rotation was left at 45 by an earlier magnet-on drag, magnet then turned off, finger pressed on the ring; the first pointermove with zero delta gives raw = rd.v + 0 =

### v15.31 (queue 737) — spotcheck PROVEN-WEAK, claim yes, effort thin
Claims the Paste look → Border gate refused a source whose only border feature was a trim path or repeater although the paste copies all four fields; the diff changes the one gate condition in styleBlockedReason (js/inspector.js:2968) from `stroke || shadow` to `stroke || shadow || trimPath || repeater`, adds a second seam, and adds a four-case test on the gate function — the code does exactly what the message says.

- [low] **Release prose describes a refusal the shipped app cannot produce** (js/inspector.js:2968) — trimPath and repeater are only ever created on layer.type === 'shape' (inspector.js Border card, lines 5974-6060; storage.js:569/579 only normalises ones already present). Every shape carries stroke from creation (scene.js:623 makeLayer, app.js:2617/2630 for drawn shapes), and the Border card seeds layer.stroke (5959, 5993) and layer.shadow (6008) unconditionally before the Trim/Repeater tiles can be tapped; nothing 
- [low] **Duplicate seam added; test binds to it, which is what makes the release proof weak** (tests/tests.js:54287) — `FM._styleBlockedReason = styleBlockedReason` already existed at the parent commit (js/inspector.js:2993, now 2999, commented 'the suite drives this directly'). The commit adds a second export `FM.inspector._styleBlockedReason` (js/inspector.js:6307) and the new test reads only that one. With the commit's source reverted the test dies on 'seam missing' instead of on r1 (trim-path source refused), so spotcheck reports

### v15.32 (queue 738) — spotcheck PROVEN, claim yes, effort real
Claims the no-op hint now lands in the measured effect's own row body instead of on whichever .fx-row.fx-open comes first (possibly an open mask row) and into classes that did not exist; the diff sets row._fx in fxRow, finds the row by r._fx === lfx in scheduleNoopCheck, and appends to .fx-ed-body / .fx-swipe-wrap — which is exactly what it does for top-level effect rows.

- [low] **Same 'first open row could be a mask' lookup left in fx-browser's scroll-into-view** (js/fx-browser.js:237) — After adding an effect, fx-browser.js:213 collapses only entries in layer.effects (dest.forEach(e => e._expanded = false) — for a penmask MARKER that sets _expanded on the marker object, not the mask object), so a mask that is open stays open. Then line 237 does document.querySelector('.fx-row.fx-open') and scrollIntoView on it. State: a layer whose mask has been dragged into the stack (layer.effects = [{type:'penmas

### v15.33 (queue 739) — spotcheck PROVEN, claim yes, effort real
Claims that ungrouping a group keyed at rest then moving now toasts about the lost animation, and that the animated toast no longer hides the effects/look toast; the diff in js/app.js does exactly that (bakeGroupTransform checks `anim` before `identity`, and FM.ungroup emits a combined toast when both losses apply) and the test asserts both toast texts through a stubbed FM.toast.

- [low] **A single keyframe at rest now reads as 'animated' and toasts a motion loss that does not exist** (js/app.js:2979) — FM.isAnimated (js/scene.js:43) is `Array.isArray(p.kf)`, so a group whose x/y/rotation/scale carries ONE keyframe at its rest value (tap the diamond once, never move — a legal state; only removing the last keyframe reverts to static, scene.js:364 / timeline.js:504) is `anim` with an identity transform. Before this commit the identity check ran first and ungroup was silent (correct — the members do not move and nothin

### v15.34 (queue 740) — spotcheck PROVEN, claim yes, effort real
Claims the sound-effect previewer stops building its own never-closed live AudioContext and plays through FM.audioCtx(); the diff changes exactly the one assignment in js/sfx.js preview() (line 651) to `liveCtx = FM.audioCtx ? FM.audioCtx() : <old code>`, and nothing in sfx.js ever closes liveCtx, so the shared context is neither duplicated nor closed.


### v15.35 (queue 741) — spotcheck PROVEN-WEAK, claim yes, effort real
Claims the mask tool's precision-pad nudge now commits its own undo step on release like a canvas drag does; the diff adds an opts.end() release hook to the shared FM.nudgePad (js/draw-tool.js:770, called from pointerup/pointercancel) and the mask tool's pad passes end: () => FM.history.commit() (js/mask-tool.js:291), which is exactly what the drag's onUp does at js/mask-tool.js:230.

- [low] **nudgePad's mouse buttons===0 bail-out ends the swipe without calling opts.end, so that nudge is never committed** (js/draw-tool.js:765) — In FM.nudgePad's pointermove handler, `if (e.pointerType === 'mouse' && e.buttons === 0) { pd = null; return; }` ends the drag but does not call opts.end(), so the mask pad's history commit is skipped on that path. State that reaches it: PC mouse, setPointerCapture threw in pointerdown (swallowed by the try/catch) so pointerup fires elsewhere, the button is released off the pad, the cursor comes back over the pad → p

### v15.36 (queue 742) — spotcheck PROVEN, claim yes, effort real
Claims mask-tool.open() no longer resets the viewport because the overlay is zoom-aware since queue 561; the diff deletes exactly that reset line and the empty `if` beside it (js/mask-tool.js:325-326), and a code read confirms the overlay is placed per frame by FM.placeOverlayOnCanvas, drawn through FM.projectToOverlay, reads input through FM.eventToProject, and hit-tests at 16 / previewDispScale() (screen rect / project span), so the removal is correct.

- [medium] **Identical stale viewport reset left in the two zoom-aware siblings (point editor, crop tool)** (js/point-edit.js:404) — js/point-edit.js:404 `start()` and js/crop-tool.js:169 `start()` carry the byte-identical line and comment that #742 removed (`if (FM.viewport && !FM.viewport.isDefault()) FM.viewport.reset();   // overlay lays out in screen px — a zoomed viewport double-scales it`). Both modules have used FM.placeOverlayOnCanvas (point-edit.js:117, crop-tool.js:50) and FM.projectToOverlay (point-edit.js:130, crop-tool.js:37) since v
- [low] **File header still says the viewport is reset first** (js/mask-tool.js:9) — Line 9 of the same file reads `Overlay lays out in screen px over #preview (viewport reset first); commits via FM.history.` after the release deleted the reset. That is a comment whose claim the code contradicts, which is precisely the pattern the 2 Sep audit that produced #742 was hunting; the next audit pass will re-flag this file.

### v15.37 (queue 743) — spotcheck PROVEN-WEAK, claim yes, effort real
Claims noiseBuffer only implemented brown so 'pink' fell through to white for Pass by and Vinyl crackle, and that Paul Kellet's -3 dB/octave filter now generates pink; the diff adds exactly that branch (coefficients faithful to Kellet's refined filter, deterministic seed unchanged), exposes FM.sfx._noiseBuffer as a seam, and the test measures first-difference energy ratio with white and brown as controls.

- [low] **Recipes tuned against white were not re-levelled; Vinyl crackle's bed drops well under its pops** (js/sfx.js:484) — Computed (not browser-measured) by porting the exact mulberry32 generator: pink RMS is 0.18-0.21 against white's 0.58 (about -10 dB) at 44.1k and 48k. Pass by (line 354) and Vinyl crackle (line 484) kept their gains (0.95 envelope; 0.22 bed vs fixed 0.3 pops). Vinyl's bed goes through a 1800 Hz highpass: roughly 36% of pink's energy sits above 1800 Hz versus ~92% of white's, so the crackle bed is about 14 dB quieter 
- [low] **spotcheck's WEAK detector misses this test's guard message, so a seam-only failure printed PROVEN** (tools/spotcheck.sh:70) — weak() greps for 'seam missing|is not a function|not exposed|undefined|null'. The 743 test's guard throws 'FM.sfx._noiseBuffer / FM.audioCtx missing' (tests/tests.js:54469) - the word 'missing' without 'seam ' before it - so the fails-as line shows the revert failed on the absent _noiseBuffer seam, not on pink measuring white, yet the verdict line printed PROVEN instead of '(WEAK: fails on a missing seam ...)'. Any t

### v15.38 (queue 744) — spotcheck PROVEN-WEAK, claim yes, effort real
Claims the effect-browser badge shortener now recognises Motion Blur (Footage)'s reason "Nothing moves inside this layer" and badges it "layer never moves" instead of "does nothing here"; the diff adds `|nothing moves` to the one regex in deadShortLabel (js/fx-browser.js:841), which is the only shortener in the repo and does exactly that.

- [low] **Spotcheck harness truncates this test's title, so tools/spotcheck.sh reports NOT PROVEN for a release that does discriminate** (tools/_spottests.py:25) — Audit-harness defect, not the release's code (the tool postdates the release: 0c5f34c, #778). The title regex `^\s*test\(\s*(['"])(.*?)\1\s*,` is non-greedy and stops at the first quote followed by a comma INSIDE an escaped title. Input: the test declared as test('744: a \'nothing moves inside this layer\' reason is badged \'layer never moves\', not \'does nothing here\'', …). Output of `python3 tools/_spottests.py b
- [low] **The test reaches the shortener through a seam added in the same commit although an equivalent seam already existed, so the revert-proof is only 'seam missing'** (tests/tests.js:54483) — The test calls FM.fxBrowser._deadShortLabel, exported for the first time at js/fx-browser.js:1567 in this commit. FM._fxDeadShortLabel = deadShortLabel has existed at js/fx-browser.js:855 since queue 603 and is what the 603 test uses (tests/tests.js:56179). With the commit's source reverted the test throws 'FM.fxBrowser._deadShortLabel seam missing' before any assertion runs — spotcheck's own definition of a WEAK pro
- [low] **The test hardcodes the reason sentence instead of reading MOTION_FX, so the exact failure mode of #744 can recur unnoticed** (tests/tests.js:54485) — The test feeds a literal copy of MOTION_FX.motionflow's sentence to the shortener. If MOTION_FX.motionflow at js/fx-browser.js:774 is later reworded (e.g. 'The picture in this layer is still…'), the shortener falls back to 'does nothing here' on the real tile while this test keeps passing on its private copy — the same disconnect between reason text and regex that produced #744. The audit entry offered the structural

### v15.39 (queue 745) — spotcheck PROVEN, claim yes, effort real
Claims the "New" carousel is now derived from CATEGORY_OF's batch order (newest first, hidden/media-only/text-only/single-layer-type/filter-clashing ids skipped, 12 max) with squish leading; the diff replaces the hand-written FM.FX_FEATURED array with a lazy getter that does exactly that, and loading the commit in a browser gives [squish, matchgrade, compoundblur, lumamatte, pixelsort, lensdistort, temporaldenoise, compresscrunch, vhstape, dispersion, lightwrap, chromakeypro], which is what fx-browser.js buildFeatured renders (getter read once per open).

- [medium] **Queue 318's browser-guard assertion is now dead: pushes to FM.FX_FEATURED land on a throwaway array** (js/fx-registry.js:189) — FX_FEATURED became a getter that builds a fresh array on every read (no setter). tests/tests.js:42332 at the commit (42334 at HEAD) does `FM.FX_FEATURED.push(aFilter.id)` then opens the browser and asserts the planted filter id did not render, to prove the guard at js/fx-browser.js:885 (`.filter(reg => !(FM.filters.get(reg.id)))`). Measured at the commit in a browser: `FM.FX_FEATURED.push('x'); FM.FX_FEATURED.indexOf
- [low] **Knowingly shipped a visible double 'Needs a setting' badge on three New cards** (js/fx-browser.js:888) — The #745 entry itself says 'adding lumamatte/compoundblur/matchgrade also triggers #30' and the release featured all three anyway. At v15.39, on any layer: open the effects browser → New row → Match Grade, Compound Blur and Luma Matte each render TWO 'Needs a setting' badges (line 885's `needsInputHint||deadHereHint` plus line 888's second `needsInputHint`, and needsInputHint includes needsSourceLayer which is true f
- [low] **'Cannot go stale again' overclaims: derived order is key-insertion order, not add order, and the test hardcodes the newest ids** (js/fx-registry.js:190) — Object.keys(CATEGORY_OF).reverse() is source-position order. The file already shows an effect added later but inserted into an older batch's block with its family (flashdark, queue 349, sits inside batch 13); any effect added that way in future will never reach New, and the 'prepend on every UPDATE' half of the standing rule at lines 165-166 is not representable at all. The new test pins feat[0]==='squish' and four b

### v15.40 (queue 746) — spotcheck PROVEN-WEAK, claim partly, effort real
Claims cannotMove() no longer badges "layer never moves" on a parented layer or one with a keyed anchorX/anchorY/z; the diff does exactly that (one early-return on layer.parent, three keys appended to the animated-channel list, a seam, one test), and the parent and z halves are genuine fixes, but the anchor half is factually wrong: the compositor reads anchorX/anchorY as raw numbers and coerces a {kf} object to 0.5 (compositor.js:3042-3051, and inspector.js:3872 withholds the keyframe button for that reason), so a "keyed anchor" does not move the layer and the release now suppresses a badge th

- [low] **Keyed anchorX/anchorY counted as motion, but the compositor never animates the anchor** (js/fx-browser.js:781) — State: a shape or text layer with no effects, static transform, and transform.anchorX = {kf:[...]} (compositor.js:3044-3047 documents that the AI op path wrote exactly this and that a saved/imported project carries it). Every compositor read goes through anchorX(tr)/anchorY(tr) at compositor.js:3050-3051, which return 0.5 for any non-finite-number, so the layer is drawn at a constant anchor and does not move. Before 
- [low] **Text animators are not consulted, so an animated text layer is still badged 'nothing moves inside this layer'** (js/fx-browser.js:776) — Pre-existing and outside the audit item's named scope (parent/anchor/z), but the release title claims the class ('layer never moves is not said of a layer that moves'). State: a text layer with layer.textAnim.preset set to any preset other than 'none' (FM.textHasAnim, compositor.js:2320; the animator moves glyphs inside the rasterised plate at compositor.js:2205), no effects, no parent, static transform, no behaviors

### v15.41 (queue 747) — spotcheck PROVEN, claim yes, effort real
Claims a featured ("New" carousel) card that needs a setting now carries one "Needs a setting" badge instead of two; the diff deletes the leftover queue-529 needsInputHint line (old js/fx-browser.js:894) in buildFeatured, leaving queue 572's single `needsInputHint || deadHereHint` line, bumps v15.40→v15.41 and fx-browser.js ?v=77→78, and adds a real-DOM test that opens the browser and counts `.fxb-needs` on the Match Grade card.


### v15.42 (queue 748) — spotcheck PROVEN, claim yes, effort real
Claim: "Storage full" toasts once, not every other autosave, when the project-index write keeps failing on quota; the diff makes saveIndex/touchCurrent return writeJSON's boolean and gates the once-flag reset in save() on sceneOk && indexOk, which is exactly the stated fix.

- [low] **writeJSON has no read-back, so a silently dropped index write still reads as indexOk=true (pre-existing, but the new comment overclaims)** (js/storage.js:19) — writeScene() (line ~66-70) re-reads the rev after setItem because 'a write that silently does nothing (a full or restricted store) is otherwise indistinguishable from success', and treats that as a quota failure. writeJSON() (line 19) only returns false when setItem THROWS. So with a store that drops setItem('fm.projects', ...) without throwing, touchCurrent() returns true, indexOk is true, the once-flag resets, and 

### v15.43 (queue 749) — spotcheck PROVEN-WEAK, claim yes, effort real
Claims the export-resume pre-roll for Motion Blur (Footage) Echo style is now derived from the effect's persistence (ceil(log 0.05 / log persist), 74 at persist 0.96) instead of a fixed 25 while the other temporal effects keep 25 and a scene with none gets 0; the diff to js/export-resume.js prerollFrames does exactly that (persist = min(0.96, 0.35 + amount*0.3), same formula as compositor.js:9959, amount read as max over keyframes), and the exporter's warm loop at js/exporter.js:1086-1096 consumes the count unchanged.

- [medium] **Sibling not covered: Time Warp Scan's memory is its whole sweep, 25 frames cannot rebuild it, and the new comment claims the count is now 'the smallest number the scene's effects need'** (js/export-resume.js:143) — Same bug class as the one fixed (pre-roll shorter than the effect's memory), left at 25 for the timewarp branch. Input: a layer with Time Warp Scan at defaults (duration 2.5s, mode Sweep, loop off), 30fps export of moving footage, crash at frame 200 (the bar landed at frame 75; the frozen band was assembled strip-by-strip from frames 0-75). Resume: prerollFrames returns 25, the warm loop starts at frame 175; composit
- [low] **The header comment still says the echo 'decays over roughly 25 frames' - the file now contradicts itself, which is the exact pattern the hunt was for** (js/export-resume.js:124) — The audit finding's fix line was 'derive frames from persist ...; fix the comment'. The inner comment block (lines 135-139) was rewritten to say 74 at 0.96, but the file-level comment fifteen lines above it (line 123-124: 'or in the echo case of the whole preceding run, which decays over roughly 25 frames') was left, so a reader of the file gets 25 and 74 for the same quantity. No runtime effect.
- [low] **The `_prerollFrames` seam is redundant and is the only reason the spotcheck is WEAK** (tests/tests.js:54582) — `prerollFrames` has been on the public FM.exportResume object since v7.53 (js/export-resume.js:374 today, :364 at the parent commit) and the older test at tests/tests.js:39271 already calls XR.prerollFrames. The new test instead guards on the private alias `_prerollFrames` added at js/export-resume.js:369 and throws 'seam missing' first, so with the source reverted the test fails on an absent property, not on 25 != 7
- [low] **Keyframed Shutter with the Overshoot ease can exceed its largest keyframe, so the pre-roll can be computed from a smaller persist than the seam actually renders at** (js/export-resume.js:141) — maxOf takes the max of k.v over the keyframes, but FM.evalProp (js/scene.js:104) applies FM.EASE_PRESETS.overshoot = [.34, 1.56, .64, 1], whose bezier peaks at about 1.10 of the segment. Input: Echo style, Shutter keyframed 1.0 -> 2.0 with the Overshoot preset, a crash whose seam lands in the overshoot window (roughly the last 40% of the segment). At the seam the compositor evaluates amount ~2.1 -> persist min(0.96, 

### v15.44 (queue 751) — spotcheck PROVEN-WEAK, claim partly, effort ceremony
The commit message, POLISH-LOG line and REQUESTS tick all say held pointers are now counted so a still finger is no longer stale after 1.2s, with a test that rests 1.45s on a clip, rebuilds and moves and a "held pointers ignored" mutation caught; the diff contains none of that — it only adds hideTrimHud/hideSnap/_sheetSuppressFor=null to recoverStuckGesture, touchGesture() at the top of the two horizontal edge-scroll loops, three seams, and a seam-driven test of the tidy-up, whose own comment says the staleness change "is not attempted here".

- [high] **Headline fix does not exist: a still finger is still recovered as a dead gesture after 1.2s** (js/timeline.js:4460) — The stale test at rebuild() is unchanged: `stale = now - gestureStamp > STALE_MS` with the stamp refreshed only by the window pointermove listener (lines 300-303, buttons !== 0) and by gesture starts. No pointer counting exists anywhere in the diff or in the current tree (grep for heldPointer/pointersDown/pointerdown capture finds nothing; `git show b3b7bafba8^:js/timeline.js` vs HEAD shows the same stamp logic). Inp
- [medium] **Recovery still leaves a moved clip with detached keyframes and no undo step** (js/timeline.js:305) — The audit finding this item closes lists 'no origStart, no shiftLayerKeyframes, no history.commit' and proposes running the abortGestures path (383-410, which restores clipMove.layer.start = origStart and the group). The commit added only hideTrimHud/hideSnap/sheet-null. State: a clip drag that has moved (applyClipMoveAt writes clipMove.layer.start live at 3599 and group starts at 3605) is recovered by rebuild() -> r
- [medium] **Sibling autoscroll loop not stamped: reorder edge-hold still reads as dead and leaves a zombie rAF** (js/timeline.js:1580) — The commit says 'both autoscroll loops stamp the gesture' and stamps clipEdgeScroll and trimEdgeScroll only. The layer-reorder drag (buildDragHandle, 1342-1349) sets reorderActive — one of the five flags rebuild() defers on at 4454 — and its own autoScroll loop at 1580-1601 never calls touchGesture(). State: hold the ≡ handle still in the 44px vertical edge zone for >1.2s while the loop scrolls, then any rebuild requ
- [low] **Test and mutation described in the release do not exist; the shipped test cannot see the claimed behaviour** (tests/tests.js:54600) — The only test tagged item '751' calls FM._recoverStuckGesture() through a seam with the HUD/snap line/sheet flag pre-set and asserts they are hidden/null. It never presses a clip, never rests 1.45s, never rebuilds mid-gesture, never checks that 'the clip follows'; grep for 1.45/rests/rebuild-then-move finds no such test. A 'held pointers ignored' mutation cannot have been run because no held-pointer code exists. The 

### v15.45 (queue 752) — spotcheck PROVEN-WEAK, claim yes, effort real
Claim: the trim edge-scroll brake counter (trimDrag._scrollFrames) was set once at pointerdown so a tripped brake killed edge-scrolling for the rest of the drag, and it now resets per edge-hold like the clip path; the diff adds exactly that one-line reset at the only trim arm site (js/timeline.js:4233, inside the `inZone && dragged && !trimScrollRAF` gate, mirroring the clip path at 4199) plus a test-only seam `FM._trimDrag`, and the code does what is claimed.

- [low] **spotcheck's WEAK detector misses this release's seam-guard failure (singular/plural regex)** (tools/spotcheck.sh:70) — Input: tools/spotcheck.sh cbd1c22d12 (revert js/timeline.js to parent, keep tests). The reverted run fails at the test's first line with the message 'seams missing: _trimDrag / _trimZonePx' (tests/tests.js ~54622) — the seam guard, not the behaviour. weak() greps for 'seam missing' (singular) and the text says 'seams missing', so no WEAK suffix is printed and the script's verdict line reads plain PROVEN, and tools/.s

### v15.46 (queue 753) — spotcheck PROVEN, claim yes, effort real
Claims the trim HUD's filled landing notch is painted only when the dragged edge sits on a frame (a snap onto an off-frame clip edge no longer fills it) and that the strip "says so with a class" otherwise; the diff (js/timeline.js updateTrimHud, +2 lines of logic) does gate the fill on |edgeT*fps - round(edgeT*fps)| < 0.02 and toggles a `tth-offgrid` class, but no stylesheet rule exists for that class anywhere, so the only user-visible change is the absent glow.

- [low] **`tth-offgrid` class has no CSS rule anywhere — the 'strip says so' half of the claim is invisible** (js/timeline.js:3323) — State: snapping on, drag B's left grip onto A's end at 1.21s (30fps). updateTrimHud sets onFrame=false and does box.classList.toggle('tth-offgrid', true). grep of styles.css (at 1d2478089b and current HEAD), index.html and all js/*.js finds no selector or reader for `tth-offgrid` — only this one toggle line. Result on screen: 41 grey notches, the pink in-mark at centre, and no blue landing glow; nothing tells the use
- [low] **The contradicted comment the audit was filed against is still in the code** (js/timeline.js:3382) — Audit #753 (hunt pattern: 'a comment whose claim the code beside it contradicts') pointed at applyTrimAt's snap comment (then lines 3229-3231, now 3380-3382): 'since every edge that goes through here is now frame-aligned anyway, the two agree in practice.' snapEdge (line 441) collects raw l.start / l.start+l.duration of every other layer plus FM.time and marker times, none frame-rounded, and applyTrimAt applies the g

### v15.47 (queue 776) — spotcheck PROVEN, claim partly, effort thin
Claims the v15.08 dark-look dim was measured hiding the film half a second before the splash dissolved, that it is withdrawn so the film plays through in both looks, and that a new behavioural test now fails if the intro is faded; the diff does remove the dim (boot script line, CSS rule, splash-dark class) so the film stays at opacity 1 under the dark look, but the new test 776 cannot see the dim (spotcheck DEAD, and by code path it never can), the release is proved only by 688's regex on the boot markup, the timing narrative contradicts the code, #688 clause 3 (dark lands on dark) is silently

- [high] **REQUESTS.md ticks #754 DONE v15.47 for code this commit does not contain** (REQUESTS.md:28586) — The commit's REQUESTS.md diff flips '- [ ] **754 — Audio/mask rows drift from effect rows (js/inspector.js)**' to '[x] … ✅ DONE v15.47' and adds a line describing 'one eyeButton() renders the eye for effect, audio, mask and behaviour rows … Test on real rows; mutation … caught'. At 48cbd73635 `git show 48cbd73635:js/inspector.js | grep -c eyeButton` = 0 and the commit touches no js/ file; the eyeButton code first app
- [high] **Test 776 cannot fail on the bug it was written for (dead against the dim)** (tests/tests.js:54772) — Spotcheck: with index.html/styles.css reverted to the parent (dim restored) test 776 still PASSES. Code path explains why it never can: `up` is `!sp.classList.contains('splash-out')`; the boot script adds `splash-dim` on the first timeupdate with currentTime >= 1.0 and calls dismiss() (which adds `splash-out` synchronously) on the first timeupdate with currentTime >= 1.15 — the same tick or the next one, ≤ ~250 ms la
- [medium] **Dark look now ends on a full-screen white that dissolves into the dark home (#688 clause 3 silently un-done)** (index.html:159) — State: fm.settings.homeLight === false. After this diff the film (object-fit: cover, opacity 1 through dismiss) shows its solid-white ending from ~1.2 s of 2.05 s, and the splash box's own ground ramps #111→#fff via `#splash.splash-light { animation: splash-ground … }` (theme-glass.css:821, brace depth 0, page is data-theme=glass, class added unconditionally at index.html:189); dismiss() then fades that white over 0.
- [medium] **Measurement narrative contradicts the code's timing, and a comment records the wrong mechanism** (index.html:154) — The commit/entry/comment say opacity was driven '0.88 → 0.60 → 0.26 → 0 from currentTime 1.18 … WHILE THE SPLASH IS STILL UP — the animation is gone half a second before the splash dissolves'. With duration 2.05, at currentTime 1.18 `d - currentTime = 0.87 <= 0.9`, so dismiss() fires (splash-out added, 0.9 s dissolve begins) on that timeupdate tick or at most one ~250 ms tick after the dim began; the film reaches 0 r
- [low] **Diagnosis not tied to his device's state; the changed path is inactive by default** (index.html:101) — `var LIGHT = true` is the default (homeLight absent); the removed dim was gated on `!LIGHT`. State: a phone on the default light look ran no dim before this release and runs none after — behaviour is byte-for-byte identical, so 'the intro plays again' is a no-op there. Neither the commit nor the #776 entry records which look his phone is on or reproduces his 'flashing black-and-white and white' on a phone; the measur

### v15.48 (queue 776) — spotcheck tests-only, claim partly, effort ceremony
The release claims the 776 intro test now requires a full-opacity reading before believing a zero and "reports an unrendered frame as a setup fact rather than a failure"; the diff does add the everVisible gate, but the setup case is a plain thrown Error (the runner has no setup/skip handling, so the suite still goes red), and the gated `blanked` assertion cannot fire on the v15.08 dim it exists for — with that dim restored the test passes 1/1.

- [high] **The gated `blanked` assertion can never see the v15.08 dim the test exists for** (tests/tests.js:54782) — State: index.html and styles.css from 48cbd73^ (the v15.08 dark dim restored: `if (!LIGHT && d - t <= 1.05) vid.classList.add('splash-dim')`, `.splash-dark .splash-dim { opacity: 0; transition: .4s }`), tests/tests.js from 586be4d. Result: the 776 test PASSES (Regression 1/1, blanked=null, everVisible=true). Mechanism, from a per-poll trace in a scratch worktree: film opacity 1.0 at 0.93s, 0.997 at 1.04s (splash-dim 
- [medium] **'Setup fact rather than a failure' is only a wording change — the test still fails** (tests/tests.js:54797) — The new branch is `throw new Error('setup: ...')`. tests/tests.js's `test()` records any thrown error as ok=false; tests/run.html and tests/_cdp.py contain no handling of a 'setup:' prefix, skip, or pending-on-message (grep: zero hits). So in the exact scenario the commit describes — the frame not rendering, the film reading 0 throughout — the suite is red and tools/ship.sh refuses the release exactly as before v15.4
- [medium] **A film that never becomes visible is now reported as 'not evidence against the app'** (tests/tests.js:54777) — The 'rendered' determination is the signal under test: everVisible is set only when the film's own computed opacity exceeds 0.9. State: the frame renders normally but the app holds the film at 0 — e.g. styles.css loses `#splash-vid.playing { opacity: 1 }` (line 1930), or any future rule keeps the playing film transparent (the shape of 'it does not do anything anymore'). Result: everVisible stays false, and the test t

### v15.49 (queue 754) — spotcheck PROVEN, claim yes, effort real
Claims one shared eyeButton() draws the struck-through off-eye for effect, audio, mask and behaviour rows and that the open audio row keeps its drag grip; the diff does exactly that — a new eyeButton(off, what) at js/inspector.js:2622 replaces the four inline eye renderers (1518, 2207, 2759, 4945) and the audio row's grip guard drops `!expanded` (2171) — but the entry's "mutation (the audio row's plain eye back) caught" is not something the shipped test can do.

- [medium] **Entry claims a mutation proof the shipped test cannot perform** (tests/tests.js:54858) — REQUESTS.md #754 and the commit message say "mutation (the audio row's plain eye back) caught", and two sentences later the same entry says the audio row's eye is deliberately NOT asserted. Reading the shipped test: it asserts struck(fxEye) on the visual effect row (control), struck(mkEye) on the mask row, and openRow.querySelector('.fx-grip') on the open audio row — nothing reads the closed audio row's eye (the loop
- [low] **Behaviour row's eye is routed through the new renderer but never exercised by the test** (js/inspector.js:4945) — The fix changes the behaviour row (be-row) to `eyeButton(off, 'Behavior'); eye.classList.add('be-eye')`, and the commit message counts behaviour rows among the four fixed. The test builds a layer with one effect, one mask and two audio effects and never renders a behaviour row, so a mutation returning js/inspector.js:4945 to the plain glyph (or dropping the classList.add that carries the 40px `.be-head .be-eye` tap-t

### v15.50 (queue 755) — spotcheck PROVEN-WEAK, claim partly, effort real
Claims ALL 110 dead `v = evalProp(p.k,t); if (v == null) v = def` fallbacks in js/compositor.js (plus 13 text-effect tnum reads) now go through a key-checking fparam() so a missing param key renders at its default; the diff does convert ~109 of that exact spelling plus the 13 text reads and the fix is real for those, but 32 sites of the same dead fallback in variant spellings (15 effects) were left behind and seven of them still render as no-effect-at-all with params {} — measured.

- [medium] **Turbulent Displace with a missing amount key still renders as no effect — the live prep path was not converted, only an unreachable kernel fallback** (js/compositor.js:8685) — The commit converted the kernel body's `td_a` fallback (turbulentdisplace: function… at ~L8182), but that branch is only reached when `pre` is null. The dispatcher at L6587 (`const pre = mapFn.prep ? mapFn.prep(...) : null`) and the GL chain's uniformsFor (L6533-6538) ALWAYS call WARP_FX.turbulentdisplace.prep, which still reads `let a = FM.evalProp(p.amount, t); if (a == null) a = 30;` on two lines (L8685-8686) — ev
- [medium] **Fourteen more effects keep the same dead fallback in variant spellings the sweep's regex missed; seven measured rendering as nothing with params {}** (js/compositor.js:4226) — Remaining dead-after-evalProp fallbacks at the commit (and still on main): sharpen L4226 `amt = (a == null ? 1.5 : a); if (amt <= 0) return;`, L4296 palette-mapped kernel `am = (a == null ? 1 : clamp01(a))`, colorize L4622 `czAmt=(czAmt==null?1:czAmt)`, checker L4623 (size→0→clamped to 2, not 24), grid L4639 (size→0→4, not 32), lensblur L4678 `lb_r=(lb_r==null?10:lb_r); if(lb_r<1)return;`, halftonelines L4748 and smo
- [low] **Test comment asserts a fact the registry contradicts (Fractal Ridges schema default is 0.6, not 'not 0.6')** (tests/tests.js:54876) — The 755 test's comment says 'Fractal Ridges falls back to amount 0.6, its schema default is not' as the reason for not comparing bare against schema defaults. js/compositor.js L664 registers fractalridges amount with `def: 0.6`, the same value as the kernel fallback (L5675). The test's actual assertions (bare != none, bare == amount:0.6) are correct and discriminating; only the recorded justification is false, which 

### v15.51 (queue 756) — spotcheck PROVEN-WEAK, claim partly, effort ceremony
Claims that "the four params whose kernel draws an absent key differently from the schema default" now carry `legacy` so an old instance's controls show what it renders; the diff adds exactly four schema attributes (glow.radius 12, filmgrain.shape 0, filmgrain.highlights 0, voronoi.motion 0) which both inspector renderers do consume and which match the single-path kernels for film grain and voronoi, but glow's 12 is only what the PC ctx.filter path draws (the phone's GL path draws an absent radius at 0, its `: 12` literal being dead), and ten sibling params in five other effects have the ident

- [medium] **Glow legacy 12 is wrong on the phone path — an absent radius renders at 0 there, so the inspector still lies where it matters most** (js/compositor.js:6686) — drawLayer (js/compositor.js:13410-13412) routes a layer through cssColorOps() whenever ctxFilterOK() is false, i.e. on the device that cannot run ctx.filter (queue 661 — the phone). For a glow whose params lack `radius` (imported / AI-authored / hand-written JSON — the very state #756 exists for; storage._sanitizeEffects deliberately leaves absent keys absent), cssColorOps does `const gr = FM.evalProp(p.radius, t)`; 
- [medium] **Ten sibling params in five effects have the identical def-vs-fallback mismatch with no legacy; the entry claims 'the four'** (js/compositor.js:10648) — REQUESTS.md #756 and POLISH-LOG v15.51 state that 'the four params whose kernel draws an absent key differently from the schema default now carry legacy'. A scan of schema `def` against each kernel's absent-key fallback (verified by reading each pair) finds the same bug untouched in: shake — amount def 120 (861) vs fparam fallback 20 (10648), speed 14 vs 12, twist 10 vs 4, zoom 12 (864) vs 0 (10650), jitter 0.65 vs 0
- [low] **The test reads four schema literals back; it cannot see the inspector, the kernels, or either render path** (tests/tests.js:54901) — The new test asserts `paramsOf(fx).find(key).legacy === <hardcoded number>` for four params plus Edge Glow as a control. It does not render an instance lacking the key, does not read the kernel's fallback, and does not open the inspector — so it passes if fxScrubber/fxSegment stop honouring `legacy`, passes if a kernel's fallback changes, and passes today while the phone glow path draws 0 against a displayed 12. No t

### v15.52 (queue 757) — spotcheck PROVEN-WEAK, claim partly, effort thin
Claims a dirty plate (RGB under zero alpha) now runs its kernel unbounded instead of being skipped "and the comment says what happens"; the diff does add a DIRTY_PLATE sentinel and returns a whole-frame box from fxBounds (js/compositor.js:3176,3186,3225), but the entry's second prescribed half — correcting the comment's false in-place-chain premise — was not done, the edited comment block now contradicts the new code in a fresh place, and the path is unreachable through the app (drawPixelEffect always reads the plate via getImageData, which premultiplies).

- [medium] **Half the prescribed fix skipped: comment premise not corrected, and the edited block now contradicts the code** (js/compositor.js:3173) — Entry 757's Fix line reads: 'return a distinct sentinel ... ; also correct the comment's premise (each kernel goes through putImageData/getImageData, not one in-place ImageData)'. The commit did the first half only. At HEAD js/compositor.js:3164-3166 still says 'the effect chain mutates one ImageData in place, kernel after kernel, without going back through a canvas. MEASURED: a clean plate through Inner Blur comes o
- [low] **Whole-frame box is wrong for the two SEMANTIC bounded kernels (Letterbox, Border): it frames the whole comp, the erasure bounding exists to stop** (js/compositor.js:3225) — fxBounds now returns {x:0,y:0,w:W,h:H} for every dirty plate regardless of fx.type, and drawPixelEffect (3296) passes that as bb to all 13 BOUNDED_FX. For the 11 skip-kernels a full-frame box is byte-equivalent to null (boxblur 4595-4596 and dropshadow 5306-5309 clamp the padded box to the frame). For letterbox (6040) and border (6051) the box is geometry, not a skip — the effects test at 59209-59213 says so itself: 

### v15.53 (queue 758) — spotcheck NOT-PROVEN, claim partly, effort thin
The release claims `_curlLegacy` was a self-copy (true), that curl "was never prepped / never rewritten" so it needs no reference (false — dcfdded/v13.28 rewrote curl from a per-pixel body to `pre||WARP_FX.curl.prep(...)`, and that body is recoverable from `dcfdded^`), and the diff mechanically removes the copy, the WARP_REF entry and the equality row and adds a seam-presence test.

- [medium] **"curl was never prepped" is false; the release writes the false premise into code, test, log and commit** (js/compositor.js:7972) — `git show dcfdded -- js/compositor.js` (v13.28) replaces the per-pixel curl `curl: function(x,y,W,H,cx,cy,maxR,p,t,ps){ cx=wCx(p,t,W,cx); cy=wCy(p,t,H,cy); var cuAmt=FM.evalProp(p.amount,t); ... }` with today's `pre||WARP_FX.curl.prep(...)` form and adds `WARP_FX.curl.prep` (now js/compositor.js:8265, whose own comment says every line is "lifted VERBATIM from the kernel's own legacy body"). The v13.28 POLISH-LOG line
- [medium] **Test 758 hard-forbids the correct fix (a real curl reference)** (tests/tests.js:54938) — `if (R.curl) throw new Error('curl has a reference entry again — it was never prepped, ...')`. State: someone restores the dcfdded^ per-pixel body as `WARP_REF.curl` and re-adds the tol-0 row (the audit's option (a), which my check shows would pass exactly). Result: the suite goes red on test 758 with a message asserting a falsehood, and ship.sh refuses the release. The guard should have been `String(R.curl) !== Stri
- [low] **The source-identity guard is defeated by any non-semantic difference** (tests/tests.js:54939) — `String(R[k]) === String(W_[k])` is the only check that a reference is a separate implementation. Input: set `WARP_REF.tunnel` to a copy of `tunnel` with one extra space or a renamed local. Result: test 758 passes, and the tol-0 tunnel row at tests.js:49606 again compares a kernel with a copy of itself — the very bug #758 was raised for, now with a test that certifies it fixed.

### v15.54 (queue 759) — spotcheck PROVEN-WEAK, claim partly, effort thin
The release claims thirteen small comment/fallback corrections including "Border offered on open shapes, since the renderer draws it" and a ghost-flagged no-op probe; ten of the thirteen are as described, but the Border change does the reverse of the claim (it removes the working Outline toggle from spiral and open pen paths) and the ghost-flagged probe makes Time Warp Scan report "does nothing".

- [high] **Border claim is reversed: Outline toggle removed from spiral and open pen paths, and stroke.enabled becomes unreachable** (js/inspector.js:5943) — Commit message and REQUESTS entry say 'Border offered on open shapes, since the renderer draws it'. The code does the opposite. openKind was ['line','arc'] and is now isOpenStroke(layer), which also returns true for spiral and path-with-closed=false; canBorder (line 5949) is unchanged: `(layer.type === 'shape' && !openKind) || …`. So spiral and open paths, which DID get the Outline tile before this commit, now do not
- [medium] **Ghost-flagged no-op probe reports Time Warp Scan as doing nothing** (js/fx-thumbs.js:1290) — With FM._mfGhost = 1, timewarp (compositor.js:9730), motionflow (9909), framestutter (9875) and temporaldenoise (9588) all pass A through to B and return. effectDoesNothing then compares 'effect on' (= passthrough) with 'effect off' (= no effect) and finds them byte-identical, so it returns true. Measured in the browser against the current tree (fx-thumbs.js and compositor.js unchanged since f139706): a rect layer wi
- [low] **The anchor-hint assertion is vacuous — the pad is never in the DOM when the test runs** (tests/tests.js:54968) — `document.querySelector('.mt-trackpad-hint')` is guarded with `if (hint && …)`. The test does not open Move & Transform or select a layer, and at rest the selector returns null (measured). So the third of the 'three that can be observed' can never fail; the spotcheck revert failed on the seam before reaching it, so the claim that the assertion discriminates was not exercised.

### v15.55 (queue 770) — spotcheck PROVEN, claim partly, effort real
Claims both overlays are placed by FM.placeOverlayOnCanvas and mapped through FM.projectToOverlay so they land on a zoomed/cropped preview, neither resets the viewport on open, and the point editor's ink probe covers both; the diff does exactly that at the moment each overlay opens (verified by reading canvas-edit.js/app.js geometry and by the spotcheck), but the tracker only repaints on window resize or a tap, so removing its reset lets the view change under it, and the motion-path half of the test uses the helper under test as its oracle rather than the independent geometry the 561 probe use

- [medium] **Tracker overlay under-covers the picture after a zoom-out during pick mode (regression from dropping the reset)** (js/tracker.js:166) — State: viewport.scale >= 1.35 so the preview is a crop (app.js previewCrop). Input: FM.tracker.pick(layer), then press #vb-fit (app.js:5571 -> FM.viewport.reset()) or #vb-zoomout — #view-bar is not hidden by the tracker (only body:has(#el-browser) hides it, styles.css:8407) and sits above the wrap at z-index 20. Path: pick() places #trk-overlay over the CANVAS rect via FM.placeOverlayOnCanvas, i.e. a wrapper-space bo
- [low] **Motion-path half of the test uses the function under test as its oracle, and its header claims a negative check the body does not perform** (tests/tests.js:54976) — `want = FM.projectToOverlay(cv, 400, y0)` is the same mapping draw() now uses (motion-path.js:89), so the assertion is 'ink where the helper says' — if the helper were wrong, drawing and oracle agree and it still passes. The 561 point-editor probe (tests.js:53163) was hardened after review to compute truePx from #canvas-wrap's rect and the comp fraction ('THE ORACLE DOES NOT GO THROUGH THE HELPER') and to assert NO i
- [low] **Garbled unclosed comment and stale header in motion-path.js after the edit** (js/motion-path.js:228) — Line 228 reads '…a zoomed viewport double-scaled itt double-scales it' with the opening '(' never closed — a botched edit left in. The file header (lines 12-15) and the dispScale comment (30-31) still say 'The overlay covers #canvas-wrap' and 'the only mapping is the display scale', both now false: the overlay covers the preview canvas (which is a crop above 1.35x) and the mapping goes through the crop origin. No run

### v15.56 (queue 771) — spotcheck PROVEN-WEAK, claim partly, effort real
Claims an orphaned draft card (template or element deleted) stops promising a save-back and the delete prompt "names the right noun"; the diff does fix the card subtitle for both orphan kinds (js/home.js:1885-1893, entry-based for templates, name-based for elements) but the delete prompt's noun is a two-way ternary (js/home.js:1928) that now says "saved back to its element" for the third card kind, a plain draft with neither ofTemplate nor ofElement, which has no element to save back to — the old text ("not saved as an element") was correct there.

- [low] **Delete prompt now names a nonexistent 'element' for a plain draft** (js/home.js:1928) — Input: a draft with neither ofTemplate nor ofElement (the 'Draft — open it, build it, then ⋯ → Save as element' card, created by 'Build a new one…' or a pre-v12.26 orphan). Tap ⋯ → Delete draft…. Code path: `(p.ofTemplate ? 'template' : 'element')` has only two branches, so the confirm reads 'Anything in it that you have not saved back to its element will be lost.' There is no 'its element' — the card's own subtitle 


## Parked items ("built out — waiting on him"): 33 of 35 audited (#772, #775 not reached)

genuinely needs him: {'partly': 14, 'no': 19} · built-out claim verified: {'yes': 17, 'partly': 13, 'no': 3}

### #95 — needs him: partly · built out verified: yes
**Waits for:** A paste from his phone of Settings → "Your last playback" → Copy (the fm.lastAudioReport text written by js/audio-health.js), taken after playing something with sound until it cuts out and pressing stop. Entry tail (REQUESTS.md:2250): "⏸ 2 Sep — BUILT OUT UNTIL HE pastes 'Your last playback'". The l
**Evidence:** js/audio-health.js exists (220 lines, FM.audioHealth.note/noteRestart/report/save/reset, TROUBLE event wiring, pagehide + visibilitychange save) and is loaded at index.html:1018 (`js/audio-health.js?v=3`; 3 commits touched it: 04823ab v14.13, 367267b v14.15, 2080141 v14.33). Wired into the transport: js/app.js:1886 `FM.audioHealth.note(m, now, !m.el.muted && vol > 0)` inside the per-element sync l
**Could be built or decided now:**
- 1. Make the report reachable from the Home cog. js/settings.js:432 `if (inProject) {` gates ALL diagnostic rows (What's slow, Your last export, Last error, Your last playback, Your last project open, Your last scrub). Verified live at 380px: Settings opened from the Home cog (#hm-settings-btn) shows 12 labels and NONE of them; inside a project it shows 19. The entry, #96, #663 and the unblock page
- 2. Surface the report where a thumb lands. Measured inside a project at 380x800: the row is label 16 of 19, offsetTop 2121px in a 3089px scroll body with 738px visible — three screens down. Recommended: when fm.lastAudioReport exists with CUT OUT > 0, a toast on stop ('Playback report saved — Settings › Your last playback') and/or the report rows above the preference rows. Testable: FM.pause after
- 3. FM.audioHealth.reset() is never called by the app (grep js/: only tests call it). Stall/restart/played counters accumulate across every play since page load, while FM.playbackStats is re-created on every FM.play (js/app.js:1980), so the 'Your last playback' report mixes cumulative cut-outs with per-play sync counters and 'played' keeps growing. Decide: reset at FM.play (recommended — matches th
- 4. A test for the Settings row itself: none exists (tests/tests.js only mentions 'Your last playback' in a comment at :57150). Write: open Settings in a project with fm.lastAudioReport set → the row renders, Copy enabled; unset → 'Nothing yet…' and Copy disabled. Mutation: delete settings.js:552-575.
- 5. Split his message into the numbered clause checklist the project's own rule requires — the entry has NO '1. [ ]' lines at all. Clause A 'Timeline on my phone is still really laggy' (closed by his 1 Sep answer, currently prose only) and clause B 'the audios don't play smoothly, I just tested adding a voice memo' (open, waits on the paste). Tick A, leave B open with the ask.
- 6. Stand-in for his phone without him: Xcode is installed but `xcrun simctl list runtimes` is EMPTY — no iOS Simulator runtime, so Mobile Safari (real WebKit) cannot be driven here. That is the one stand-in every entry says is missing ('a throttled desktop cannot rule out iOS Safari'). It is a multi-GB download, so it is his call, one line: `xcodebuild -downloadPlatform iOS`. After that a session 
- 7. tools/spotcheck.sh cannot re-prove v14.13 (or any commit that ADDS a source file): its revert step runs `git checkout <parent> -- js/audio-health.js` and fails with 'pathspec did not match', exit 2. Fix the tool to delete files absent from the parent. Separately, all four control runs were NORUN 'suite did not finish within 300s' because four tests/_cdp.py instances (one full suite on :8777 plu
- 8. The '34 candidate causes checked, 33 refuted' (REQUESTS.md:2241) exists only as a count in prose; no list is anywhere in the repo, so it cannot stop the ground being re-hunted. Low priority: write the list to a file under tools/ or into the entry.
**Recommendation:** Keep the audio clause parked on the paste — the instrument is real, wired, tested and deployed, and the remaining question (does iOS Safari stall a media element) cannot be answered on this Mac without an iOS runtime he would have to download. But 'built out' is overstated: the ask as written is not followable from the Home cog (rows gated by inProject at settings.js:432), the row sits three scree

### #96 — needs him: partly · built out verified: yes
**Waits for:** Tail line (REQUESTS.md:2026): "⏸ 2 Sep — BUILT OUT UNTIL HE pastes 'Your last playback'" — play on his phone until the sound misbehaves, press stop, Settings → Your last playback → Copy, paste it. The entry ALSO still carries an un-struck ask at line 1878: "❓ASK: has a song failed to play since v13.
**Evidence:** Entry read in full (REQUESTS.md:1876-2027); it has NO numbered '1. [ ]' clauses (grep of the range returns only the header line), so no unticked clause exists to name. Instrument: js/audio-health.js exists (220 lines, committed in 04823ab v14.13, later 367267b/2080141); loaded at index.html:1018 `<script src="js/audio-health.js?v=3">`; wired from js/app.js:1781 (noteRestart in the resume branch), 
**Could be built or decided now:**
- Make the paste answer the open ❓ASK by itself: the audio report (js/audio-health.js:150-183 report()) prints no file name, type, size, element duration or decoded duration, yet every media record carries them (js/media.js:117-122 resolves {el, url, file, duration}; app.js:2411 FM.media.set). Add a per-audio-clip line: file.name, file.type, file.size, el.duration vs m.duration, readyState, networkS
- Record play() rejections. js/app.js:1782 and :2007 do `m.el.play().catch(() => {})` — a NotAllowedError/AbortError/NotSupportedError (the iOS 'pressed play, nothing happened' signature) is swallowed and appears nowhere; audio-health's TROUBLE list (js/audio-health.js:63) wires element events only. Add FM.audioHealth.notePlayFail(m, err.name) at both sites and a 'play() refused' line in the report.
- Make a playback that never started still write a report: js/audio-health.js:186 save() bails with `if (!S.playMs) return;`, so the entry's literal symptom — 'will not play at all' — leaves Settings saying 'Nothing yet'. Record each FM.requestPlay/FM.play attempt (time, whether START_WAIT_MS expired with no element advancing, app.js:2010-2035) so an empty playback produces a report.
- Write the phone stand-in test for the above: drive FM.play with a fake element whose play() rejects with NotAllowedError and assert the saved fm.lastAudioReport names it (pattern already exists at tests/tests.js:46508-46541 for the stall path).
- Strike or fold the ❓ASK at REQUESTS.md:1878 into the BUILT OUT line — once the report carries the file/format/length the question is answered by the paste; today the entry shows two different waits (a STATUS 'waiting on your answer' and a BUILT OUT 'pastes') for one item.
- Re-run tools/spotcheck.sh dabc2a1 and 04823ab when the machine is idle: both NORUN'd today (control timed out at 300s with 9 concurrent spotchecks, load avg ~10), so the 'mutation-checked' claims for v13.88 and v14.13 stand unre-proven.
**Recommendation:** Not delusion — every fix and the instrument the entry names are real, wired, tested, and the two #96 tests pass today. But 'BUILT OUT' overstates: the instrument was built for #663's symptom (sound cuts in and out — stalls and restarts) and cannot record #96's own symptom (never plays at all): a play() rejection is swallowed silently, a playback that never ticked writes no report, and the report o

### #129 — needs him: partly · built out verified: partly
**Waits for:** A paste from his phone, next time it happens: "Settings → A clip with no picture → Copy" (the tail line at REQUESTS.md:395, and the unblock page card 'rep-blank' at tools/unblock/unblock.html:266-277). The report is written to localStorage 'fm.lastBlankClip' when a video lands on the timeline and ne
**Evidence:** CODE EXISTS AND IS WIRED (verified by reading + one live run): js/media.js:308 FM.sniffHevc (head+tail, 256KB each); :345 FM.blankClipFacts (name/ext/type/canPlayType + el.error.code via MEDIA_ERR map at :344); :374 FM.codecSupport (5 rows canPlayType+MSE); :400 FM.saveBlankClipReport -> localStorage 'fm.lastBlankClip'; :421 FM.wireVideoRepaint: error listener :436-448 (one-shot via rec._blankSaid
**Could be built or decided now:**
- Fix FM.codecSupport() (js/media.js:374-390): ask with the full profile strings FM._hevcFromProbe already uses (hvc1.1.6.L93.B0, hvc1.2.4.L120.B0 …) and add an 'H.265 (any profile)' row from _hevcFromProbe; state in the table that a bare '' for video/quicktime is not proof of a container refusal. Measured: real Chrome reports hvc1 NO / quicktime NO on a page that just decoded an HEVC .mov to readyS
- Write the report on the refused-at-load path too: in FM.loadVideoFile's error listener (js/media.js:155) call FM.saveBlankClipReport with blankClipFacts (el.error.code is available there) before rejecting. Measured: headless Chrome refuses the HEVC .mov outright, handleFiles (app.js:4071) shows only a toast, and Settings → 'A clip with no picture' stays 'Nothing yet'.
- Check in a real HEVC .mov fixture or a generator (tools/mkhevc.swift, ~30 lines, swiftc + AVFoundation, 90 KB, hvc1 in the tail moov — generated on this Mac in ~1s; the entry's 'cannot make an H.265 recording here' is false) and use it for an end-to-end test of the instrument instead of stub EventTargets: load -> (Chrome refuses at load / decodes) -> the expected report or none.
- Add the missing tests for v13.91's two behavioural fixes and the Settings row: (a) an 'error' event dispatched AFTER loadedmetadata writes 'fm.lastBlankClip' once with 'decoder raised an error' and the timeout does not speak again (_blankSaid); (b) with FM.canDecodeHEVC stubbed TRUE (his phone), the timeout report contains 'Bytes carry the H.265 tag' for an hvc1 file; (c) Settings opens with the '
- Run the instrument in macOS Safari 26.5.2 (same WebKit as his phone; /Applications/Safari.app and /System/Cryptexes/App/usr/bin/safaridriver are present) with the generated .mov AND a real `screencapture -v -V 2 x.mov` (a genuine screen recording with the audio track and edit list an iOS one has). Nobody has ever run this in Safari; every measurement in the entry is Chrome. safaridriver needs a on
- Rule-16 decision to record in the entry: on HIS device the codec theory and the container theory are both improbable (Safari decodes HEVC and .mov — the entry says so itself — and his ordinary iPhone camera clips are HEVC .mov and work), so stop treating 'which of the two' as the open question; the open question is what is different about a 2-second SCREEN recording (video track starting after t=0
**Recommendation:** Do not leave this in 'built out — waiting on him'. The paste is genuinely the only source of evidence FROM HIS PHONE, but three things in the entry are false or untested and were reachable without him: the codec table it calls 'the whole answer' gives a wrong NO on a browser that just played the file (the #486 bug repeated one function down), two of v13.91's three fixes and the Settings row have n

### #unnumbered-lag — needs him: partly · built out verified: yes
**Waits for:** Tail line (REQUESTS.md:3976, added 2 Sep in commit 942f957 v15.02): "BUILT OUT UNTIL HE pastes a Measure report (Settings → Measure, with a few effects stacked on one layer)". The specific thing wanted from the paste is the GPU and DEVICE lines from HIS iPhone/Safari — whether `webgl warp` reads OK 
**Evidence:** WIRED: index.html:963 perf-probe.js?v=12, :972 gl-warp.js?v=4, :974 compositor.js?v=129 (app v15.57 at :228). js/gl-warp.js:239-364 defines FM.glWarp {run:243, runChain:303, stats:362, available:363 honouring FM._noGL}; getContext('webgl2')||('webgl') at :67-68; 0 NUL bytes. js/compositor.js:6509 drawWarpEffect; :6561-6581 chain path (FM._noGLChain control, FM.glWarp.runChain); :6602-6611 single-k
**Could be built or decided now:**
- Port the three kernels the entry itself left unported and named as 'the next real work … not blocked on Ezra' on 28 Aug, then silently dropped when the 2 Sep BUILT OUT line was written: `glass` (needs a `#version 300 es` shader for its 32-bit integer hash — gl-warp.js already requests webgl2 first at js/gl-warp.js:67), and `fractalwarp` + `turbulentdisplace` (their Float64Array lattices from `prep
- Run the WebKit stand-in for his phone that has never been used: open tests/_glwarp3.html (every ported kernel vs its CPU twin, with path counters) and the app's Settings → Measure in Safari.app on this Mac (present at /Applications/Safari.app). His phone is Safari/iOS (REQUESTS.md:9974 DEVICE line); every GPU number in this entry was taken in headless Chrome, so 'does the shader compile and agree 
- Stamp the 'what's slow' report with the app version: js/perf-probe.js:281 prints the header without it (the device report in js/app.js:1231 does print `app vX`). One line. Without it a paste from him cannot be tied to a release — his three earlier pastes (REQUESTS.md:9974, 10028, 10142) had to be dated by hand from surrounding prose, and the whole point of this ask is 'did v14.33 reach you'.
- Re-run `tools/spotcheck.sh 2080141` on its own. This audit's run returned NO-CONTROL for all three v14.33 tests ('suite did not finish within 300s') because four fm-spot worktrees were running concurrently (04823ab, dabc2a1 and two more), so the mutation proof for the GPU/DEVICE report block — the exact instrument the paste depends on — is currently unverified after the fact.
- Fix the entry's own contradictions (doc only): line 3467-3468 still opens with '*Status (v6.33)*' and 'STATUS: 🟠 NEEDS YOU — waiting on your answer' while the 28 Aug body says 'not blocked on Ezra; it is bigger than a tick' and the 2 Sep tail says BUILT OUT; '18 of 21' is stale — 19 of 22 real kernels carry `.glsl` since wrapshift.glsl shipped v14.16 (83806ae); tools/next.sh lists line 3467 both u
**Recommendation:** The paste itself is a legitimate hold: there is no post-v14.33 report from his phone anywhere in the repo, and nothing on this Mac can measure his iPhone's graphics chip. But 'BUILT OUT' is not true. The entry's own 28 Aug section named two architectural pieces as 'the next real work … not blocked on Ezra'; one (chain residency) shipped v14.12, the other (ES 3.00 / lattice-texture port of glass, f

### #425 — needs him: no · built out verified: yes
**Waits for:** One word on clause 3: "A" (the COPY button #btn-layermenu moves right with parent/trash/group/⋯, and the add-row switch moves with it) or "B" (copy stays on the left where #373 put it one day earlier — marked recommended). The tail line reads: "2 Sep — BUILT OUT UNTIL HE picks A or B on clause 3 … t
**Evidence:** CODE: js/app.js:5761-5776 — pcTransportLayout builds #t-sel, grabs ['btn-parent','btn-del-layer','btn-group','btn-maskgroup','btn-more-layer'] and does right.appendChild(sel) into .t-right (after undo/redo at index.html:497-498); copy (#btn-layermenu, index.html:472) and the switch (#btn-addside, index.html:481) stay in .t-side.t-left (index.html:471). All five ids exist in index.html (btn-parent:
**Could be built or decided now:**
- DECIDE clause 3 under rule 16 (LOOP.md:116): pick B, the recommended option. B is the code exactly as shipped, so it is zero edits to js/ or styles.css — only the entry changes: tick clause 3 as 'DECIDED B by rule 16 — say "move copy too" to flip', strike the '⏸ 2 Sep — BUILT OUT UNTIL HE picks A or B' line, and mark the entry [x] (v10.63 layout + v12.46 pill). ship.sh's oldest-first gate does not
- SHOW HIM THE PICTURE — never done. Rule 16 says show the picture with the alternatives named; the entry only ever offered letters in prose. tests/_transhot.html?w=1280&h=820&studio=1&sel=1 (added for this exact item, its own comment cites queue 425) renders the row with a layer selected; one screenshot at a desktop width, with 'A: copy joins the right-hand pill' written under it, is the whole cost
- FIX THE STALE CLAUSE-2 TEXT: the entry says the group's background is rgba(0,0,0,.45) and 'the test asserts alpha ≥ .35 and a dark colour'. Neither is true any more — v12.46 (#516, REQUESTS.md:18835) replaced the dark slab with a 1px outline over a 4% lift (styles.css:7325-7330: background rgba(255,255,255,.045), border 1px rgba(255,255,255,.13)) after he was shown four renders and picked it, and 
- REMOVE the #425 card from tools/unblock/unblock.html (lines 286-295) once the entry closes and republish with url= per #777; or replace it with a one-line 'copy is on the left — say "move copy too" to flip'.
- IF HE EVER SAYS A, the change is already known and small — no reason to hold the entry for it: add 'btn-layermenu' and 'btn-addside' to the grab list at js/app.js:5776 (into #t-sel, before btn-parent), restate tests/tests.js:498 ('transport-swap': copy-left-of-pill and switch-immediately-right-of-copy become 'switch immediately right of copy, both right of the pill') and flip tests/tests.js:9195 (
- COSMETIC: the clauses are numbered 1, 3, 2 in the entry (clause 3 was inserted above clause 2 on 22 Aug); reorder so a reader does not miss the open one.
**Recommendation:** Not a legitimate block. Both options are named, one is recommended, and the recommended one (B) is the code that has been live since v10.63 — so under rule 16 this entry should have been closed on 2 Sep with 'decided B; say "move copy too" to flip', plus one picture of the row. The only unticked clause (3) depends on nothing but a preference call the rules tell the session to make. Reclassify from

### #429 — needs him: partly · built out verified: partly
**Waits for:** Tail line (REQUESTS.md:16622): "Clause 2 (the + moving on swipe) REMAINS OPEN — BUILT OUT UNTIL HE checks it on his phone in the installed app." Earlier in the same clause: "this needs 30 seconds on his phone: swipe the timeline in Safari and see whether the + still shifts", and the settling questio
**Evidence:** CLAUSE 2 FIX PRESENT BUT INACTIVE IN THE COMMON STATE: styles.css:3379-3381 `:root{--stage-h:40vh}` / `:root{--stage-h:40svh}` / `#app{grid-template-rows:auto var(--stage-h) 1fr}` (blame c0bda67f, 2026-08-20). styles.css:3536 `body.m-editing { --stage-h: 40vh; }` (blame 7b6e9834, 2026-06-28), inside the same `@media (max-width:700px)` block (awk brace-count: opens 3367, closes 3642); it was alread
**Could be built or decided now:**
- FIX THE HOLE IN THE SHIPPED FIX: styles.css:3536 `body.m-editing { --stage-h: 40vh; }` sits inside the same phone media block (3367-3642) and overrides the v10.67 `40svh` at :3380 whenever exactly one layer is selected on the phone (js/app.js:852). Change it to `40svh` with the `40vh` fallback line above it, same pattern as :3379-3380. Add a suite test: at 380px, `getComputedStyle(document.body).g
- SHIP THE MEASURABLE OPTION THE ENTRY ITSELF NAMED ("whatever is changed next can be measured instead of hoped at"): on pointerdown/touchstart inside #timeline, freeze `--stage-h` to the stage's current px (inline on body or #app), release on pointerup/pointercancel. Then tests/_swipeonrow.html case D (lines 44-70, frame grows 820→880 mid-swipe) reads 0px instead of 24px in headless Chrome, so a SU
- ADD A SUITE TEST FOR CLAUSE 2 AT ALL: commit c0bda67f (v10.67) touched POLISH-LOG, REQUESTS, index.html, styles.css and tests/_growbisect.html only — no tests/tests.js change; tools/spotcheck.sh would verdict it NO TEST. Port _growbisect.html?pin=1 (lines 29-38) into tests/tests.js: select one clip (m-editing), grow the frame, `.tl-addrow-plus` top must not move (re-query by selector after the reb
- TURN HIS 'LOOK' INTO A PASTE: during a touch gesture on #timeline record whether innerHeight / visualViewport.height changed and how far `.tl-addrow-plus` moved, write it to localStorage the way js/home.js:168 writes fm.lastOpenReport, and surface it in Settings beside js/settings.js:584. His unblock answer then becomes a pasted line with numbers instead of an impression.
- FIX THE UNBLOCK CARD (#777): tools/unblock/unblock.html:525-536 titles #429 'The ruler past the cut-off' and asks 'does anything still move or draw past the end of the timeline?' — that is the superseded 20 Aug A/B/C/D ruler pick the entry itself says was the wrong guess (answered 21 Aug, built v15.03). The one open ask is clause 2: does the + still shift when you swipe, installed app vs Safari. R
- STRIKE THE STALE HEADER at REQUESTS.md:16435 ('NEEDS YOU — waiting on your answer'): nothing is waiting on an answer; clause 1 is built and clause 2 waits on a look, which items 1-4 make premature or unnecessary.
**Recommendation:** Reclassify #429 from "built out — waiting on him" to WORKABLE. The svh-vs-real-iOS-chrome confirmation is the one thing a headless harness truly cannot do, but asking him to test it now is premature: the shipped fix is overridden whenever a clip is selected on the phone (styles.css:3536), which is the state the page loaded in here and the state he is most likely swiping in, so his "still moves" wo

### #454 — needs him: no · built out verified: yes
**Waits for:** The tail line (added 2 Sep, commit f1483f1; re-stamped 4 Sep) says: "BUILT OUT UNTIL HE picks A, B or C on the last clause" for the layer-menu item "Save whole look as preset…" — A = remove it (his literal words), B = keep it, rename to "Copy look & motion to…" and move it out of the Presets card (r
**Evidence:** REQUESTS.md:17085-17156 (entry body, verified by classifier: 'built out — waiting on him', stale_asks False). His answer: REQUESTS.md:17087-17088, committed d6075ed 2026-09-01 ("#454 — HE CHANGED HIS MIND: keep BOTH savers"). The BUILT OUT UNTIL HE picks line: REQUESTS.md:17156, committed f1483f1 2026-09-02 (after the answer). Built-out claim checked against code: FM.canvasPresets — zero hits in j
**Could be built or decided now:**
- CLOSE THE ENTRY ON HIS OWN WORD — he already answered the A/B/C ask on 1 Sep (commit d6075ed, logged verbatim inside the entry at REQUESTS.md:17087-17088: "Actually u are right to keep both options to save effect presets and full layer preseets i change my mind"). That is neither A, B nor C: it is 'keep it as it is, and keep calling it a preset'. The entry's own text at 17089-17094 already says "t
- DO NOT ship option B under rule 16. B (rename + move out of the Presets card) was the recommendation BEFORE his 1 Sep reversal; his reversal explicitly calls the feature "full layer presets" and says keep both, so renaming it away from 'preset' would overwrite his stated answer. Rule 16 applies only when he has not answered — here he has.
- Remove the #454 card from the unblock page (tools/unblock/unblock.html:297-308) and republish the artifact with url=https://claude.ai/code/artifact/0ab35f83-9721-4e5e-b881-23c6e8b537a7 (#777 says keep that list current). The card's blurb "Presets are for effects only now" is false since 1 Sep and asks him a question he has already answered.
- Structural fix (CLAUDE.md: safeguards must be structural): tools/_classify.py line 88 (`answered = ('ANSWERED BY EZRA' in body) or ('HE ANSWERED' in body)`) and line 373 (`_ANSWERED = re.compile(r'ANSWERED BY EZRA|HE ANSWERED|EZRA ANSWERED|He answered|he answered')`) do not recognise "HE CHANGED HIS MIND", so this answered entry was neither lifted nor flagged by the STALE ASKS detector (verified: 
- Log (do not block on) one follow-up question in the entry: his reversal was about the two LAYER savers; the canvas/project preset (#183, his own feature, removed v11.25 with his saved "Cool loop") is still gone. Ask in one line whether he wants it back; keep going.
- Note in #406 (REQUESTS.md:15639, also parked BUILT OUT UNTIL HE names which menu) that his 1 Sep answer on #454 bears on it — he wants both savers kept — so #406's open question about which saver to delete may also be answered. That is #406's audit to confirm, not #454's, but the cross-reference should be written.
**Recommendation:** Not a legitimate block — it is an answered item that a later session re-parked without reading the answer sitting inside the same entry (the "re-checked today for strict oldest-first" stamp on 2 and 4 Sep is exactly the delusion he suspects: the tool said built-out, the session believed the tool, nobody read line 17088). Close #454 now on his 1 Sep word with a line containing "HE ANSWERED" so the 

### #406 — needs him: no · built out verified: yes
**Waits for:** Per the tail line ("2 Sep — BUILT OUT UNTIL HE names which menu"): one line from Ezra naming WHICH menu his 19 Aug message "Get rid of saving presets from this menu" meant (no screenshot arrived), because the entry says there are three preset savers and deleting the wrong one removes the only route 
**Evidence:** The built half checks out in code, with stale line numbers and one omission. (1) Three savers exist and are renamed as claimed: js/app.js:3909 `items.push({ label: 'Save whole look as preset…', action: () => FM.savePresetPrompt(layer) })` inside `FM.layerMenuItems` (app.js:3808; entry says :3307 — stale); js/inspector.js:1239 `{ label: 'Save this effect as preset…' … FM.effectPresets.capture(fx…)`
**Could be built or decided now:**
- Clause 1 under rule 16: ship option A — remove the 'Save whole look as preset…' row from FM.layerMenuItems (js/app.js:3909). It is the entry's own recommended pick, it is what his verbatim conditional says ('get rid of the one isn't just saving as effects' — that row is exactly the one that is not effects-only), and after his 1 Sep #454 answer (keep both effect presets and full-layer presets) it l
- Move the #182 test 'the layer menu names what its preset saves' (tests/tests.js:24236-24250, item preset-label) with the row: it currently asserts 'Save whole look as preset…' IS in FM.layerMenuItems and will go red under option A; re-point it at the Presets card button or the ⧉ menu so both of #182's requirements (not the bare 'Save as preset…', and says what it saves) still hold.
- Decide whether the ⧉ transport menu's duplicate 'Save look as preset' (js/app.js:5524, queue 437, test tests.js:2407) is 'this menu' too — the entry's table missed it entirely; at minimum add it to the table so the pick he is shown is the real set of four doors, not three.
- Clause 3 (acknowledgement): record in #406 that his 1 Sep message in #454 ('u are right to keep both options to save effect presets and full layer preseets') IS an acknowledgement that he read and understood the difference, tick clause 3 or narrow the ask to a one-word confirmation, and stop carrying the reminder at the top of every reply.
- Write 'HE ANSWERED' / 'ANSWERED BY EZRA' into #454 next to the 1 Sep reversal so tools/_classify.py lifts its block and next.sh's STALE ASKS banner can fire — today the classifier only credits those two phrases (tools/_classify.py:88) and 'HE CHANGED HIS MIND' is invisible to it, which is why both #454 and #406 are still filed as waiting on him.
- Republish tools/unblock/unblock.html (card data-id=406, lines 463-470) with the #406 card removed or reduced to 'A shipped — say B or C to change it', and the #454 card removed, via the Artifact tool with url=https://claude.ai/code/artifact/0ab35f83-9721-4e5e-b881-23c6e8b537a7 (#777).
- Fix the three stale line references in the entry's table (app.js:3307→3909, inspector.js:1060→1239, inspector.js:1677→2049) and the label 'Save effects only…' → 'Save effects only as preset' (changed by #583 v12.98).
**Recommendation:** Not a legitimate block. The entry itself says clause 1 is "NOT ACTUALLY BLOCKED — his own conditional decides it" and names A as recommended, and the one external dependency it cites (#454) was answered by him on 1 Sep — a fact the 2 Sep re-check missed. Under rule 16 a session should ship A (remove the duplicate whole-look row from the layer ⋯ menu, move the #182 test to the Presets card button),

### #215 — needs him: partly · built out verified: yes
**Waits for:** A paste from his phone. Tail line (REQUESTS.md:9959, 2 Sep): "BUILT OUT UNTIL HE pastes 'Your last export' (export something with sound on the phone; if silent, Settings → Your last export → Copy)". The report (localStorage fm.lastExportReport) is meant to name which of six audio-loss paths fired on
**Evidence:** Code present and wired: js/exporter.js:310-321 exportSay writes/stacks/dedups into #export-note; index.html:750 #export-note sits inside #export-overlay (index.html:734-753); styles.css:8919-8928 .export-note-warn amber styling. All six loss sites call exportSay: js/exporter.js:522-523 (dropped clips, sets 'all-unreadable' when !any), :533-535 ('all-suppressed'), :601-602 ('mix-silent'), :906-907 
**Could be built or decided now:**
- (1) Make the Copy confirmation visible. js/settings.js:490 confirms 'Copied — paste it to me' via FM.toast, but #toast is z-index 60 (styles.css:3332) and .set-scrim is z-index 220 (styles.css:5439) — settings.js:219-220 documents this exact trap ('a toast raised from this panel would be painted behind it and never seen'). So on his phone the Copy button gives no visible feedback, in the one path 
- (2) Offer the report on the card he is already looking at. #export-ready (index.html:752-762, renderer js/app.js:~4700-4760) shows 'NO SOUND — <why>' but has no route to the report; he must go find Settings. Add a 'Copy report' button on the ready card when out.audioDropped is set, or when !out.hasAudio && FM.projectHasAudio(); copy fm.lastExportReport with in-card confirmation (#export-ready is z
- (3) Close the one overwrite route v14.35 missed. js/app.js:4642-4646 onNote does `noteEl.textContent = text` (REPLACES); js/exporter.js:1042 calls it for crash-resume AFTER every audio-loss exportSay site (:523/:535/:602/:907/:937/:988), so a resumed export loses its stacked audio warnings — contradicting the entry's 'they stack instead of overwriting'. Fix: route onNote through FM._exportSay; tes
- (4) Re-prove the 'both fixes mutation-tested' claim. tools/spotcheck.sh ccdaffa (v14.35) today: all three tests NORUN 'suite did not finish within 300s' in the control run while another session's spotcheck (197e3a4) ran concurrently. Re-run when nothing else is running; if it NORUNs again, soften the entry's claim rather than repeat it.
- (5) Change the ask so a healthy paste is still useful. The report always carries 'AAC encode … AudioEncoder MISSING/present' and the device UA (js/exporter.js:1207,1229); the current wording says 'if silent'. Ask for the paste either way — it answers the AudioEncoder-on-iOS question and records his iOS version (nothing in the repo records it: no 'iPhone; CPU' string in REQUESTS.md/INBOX.md/POLISH-
- (6) A stand-in for his phone does not exist here: xcrun simctl list runtimes is EMPTY (no iOS Simulator runtime installed), so the WebKit AudioEncoder question cannot be measured on this Mac. Installing a runtime is a multi-GB download — a decision for him, not something to do silently.
- (7) Trivial: js/exporter.js:534-537 puts the 'all-suppressed' exportSay INSIDE `if (FM.toast)`; move it out so the card message never depends on the toast existing.
**Recommendation:** The decisive fact — which of the six loss paths fires on his iOS Safari, and whether WebKit there has an AudioEncoder — genuinely can only come from his device, and no stand-in exists on this Mac, so the paste is a legitimate block for the diagnosis itself. But the entry is not fully built out and the wait is being made harder than it needs to be: the Copy button he is asked to press confirms via 

### #508 — needs him: no · built out verified: yes
**Waits for:** "BUILT OUT UNTIL HE opens a project on his phone and pastes that report" — the Settings → "Your last project open" → Copy text (frame times of the push recorded on his device), because the entry says the transition "measures smooth here even with sixteen cards on screen" so "the next step is a readi
**Evidence:** INSTRUMENT IS REAL AND WIRED: js/home.js:126-172 openProbe (begin/settle/cancel/finish, writes localStorage 'fm.lastOpenReport', FM._openProbe seam at :174); begin() called in startPush at js/home.js:604 with cards/layers/pushMs; settle()/cancel() at js/home.js:358 inside endPush(hide); openProject js/home.js:2059-2066 starts phase 1 BEFORE `await FM.projects.open(id)` so the load and first build 
**Could be built or decided now:**
- FIX THE STALL THE INSTRUMENT JUST FOUND (no paste needed). Reproduced with the app's own #508 probe on this Mac: heavy project (6 shape layers, 8 effects — the fixture in tools/_phoneprobe.py SETUP), 17 cards, opened from a different project so the load runs. 1x: 74 frames, worst 83.4ms, 4 long frames at 0ms:50 83ms:83 367ms:33 717ms:33, verdict 'janky'. 6x CPU throttle (phone stand-in): worst 466
- A TEST THAT STANDS IN FOR HIS PHONE: extend test '508:' (tests/tests.js:53252) or add a sibling that builds the heavy fixture (6 layers + effects via FM.fxRegistry.makeInstance, as tools/_phoneprobe.py does), opens it from another project through the real card path, and asserts the report's worst gap / long-frame count under a threshold measured from the fix (tolerance-from-measurement). The curre
- REPORT BUG, fixable now: js/home.js:604 passes `layers: FM.scene.layers.length` at push START, before the load — so the pasted report says 'layers in the project 0' for a 6-layer project (it counts the OUTGOING project). Record it in settle() after the load, or after FM.projects.open resolves.
- REPORT GAP, fixable now: finish() (js/home.js ~154-172) never writes WHEN phase 1 ended, when the load resolved, or when phase 2 armed — only offsets from t[0]. Stamp those three markers so a long frame can be attributed to 'load' vs 'first build' vs 'join' without guessing; the entry's whole point is that attribution.
- CORRECT THE RECORD: the entry (28 Aug block, 1 Sep line) and POLISH-LOG v15.05 say '0 frames over 33ms with sixteen cards, blur or not' — that was measured on EMPTY projects (tests/_openanim.html creates bare projects; the 1x/6x re-run today confirms 0/56 and 0/53 there). Write that it reproduces here with a real project shape, and un-park the entry.
- SPLIT HIS VERBATIM REQUEST INTO CLAUSES (CLAUDE.md rule): the entry has NO numbered '1. [ ]' clause checklist at all. His message has three: (1) the tapped card glides smoothly to the left with a nice animation — built (fm-push-lead, styles.css:6220; 24 distinct transforms measured), (2) then the project comes in smoothly from the right — built (fm-push-in, styles.css:6268, two-phase push queue 12
**Recommendation:** Un-park #508. The instrument is genuinely built, wired, shipped (v15.05, cache-busters bumped, spotcheck PROVEN) and his paste would still be useful confirmation — but the block rests on 'it measures smooth here', and that was only ever measured on EMPTY projects. Pointed at a 6-layer/8-effect project the app's own probe reports the open as janky on this Mac at 1x and shows 267ms + 467ms stalls at

### #544 — needs him: no · built out verified: partly
**Waits for:** The entry's tail (REQUESTS.md:19978, written 2 Sep in f1483f1) says: "BUILT OUT UNTIL HE names WHICH things get the Claude Design pass" — i.e. Ezra is to say which specific things (from candidates #543 icons, #510/#432 template button, or the BEFORE-PUBLISHING.md identity pass) should get the design
**Evidence:** REQUESTS.md:19967-19979 (entry; STATUS 'NEEDS YOU', 2 Sep BUILT OUT tail; no numbered clauses at all). Original logging: git 0a74824 (24 Aug 18:41, v12.32). LOOP.md diff in 64fee0d (24 Aug 19:20, v12.33): '⏸️ PAUSED FOR DESIGN WORK AT HIS REQUEST (#544). He picked the icons (#543) and both shipped in v12.33.' — he named the things; the loop then resumed at 254cf64 (v12.34, template icon 'chosen fr
**Could be built or decided now:**
- Close #544 as DONE (24 Aug, v12.33/v12.34): he already named the specific things the same evening he asked — LOOP.md at 64fee0d records 'He picked the icons (#543) and both shipped in v12.33', and #546 (his words: 'I would like to see what you and claude design can cook up with the template icon') shipped v12.34 at 20:16. The request as made was satisfied; the entry was never updated and the 2 Sep
- Remove the #544 card from tools/unblock/unblock.html (lines 481-488) and republish the unblock artifact with url=https://claude.ai/code/artifact/0ab35f83-9721-4e5e-b881-23c6e8b537a7 — the card asks him to choose between #543 and #510/#432, both of which are ticked [x] and shipped; two of its three candidates are dead options.
- If a session insists on keeping it open, rule 16 applies: the only live candidate is the BEFORE-PUBLISHING.md identity pass, and that is separately HELD at his request (unnumbered entry REQUESTS.md:5601-5606) with a standing rule that starting it is his call — so the recommended decision is 'no further Claude Design pass pending; raise the identity pass when he mentions publishing', written into t
- Optionally fold the residual honest state into #545 (the standing 'use Claude Design' rule, HELD on his design-system-project decision) instead of keeping a second open entry that duplicates it — #545 already carries the 'what you might want to say' options.
**Recommendation:** Not a legitimate block. The thing it 'waits for' (which things get the design pass) was supplied by Ezra on 24 Aug — the loop state written that night says so in plain words — and both named things shipped (#543 v12.33, #546/#510/#432 v12.34). Two of the three candidates still listed are ticked done; the third (identity pass) is a separately held item with its own rule. Tick #544 done with the ver

### #482 — needs him: partly · built out verified: partly
**Waits for:** The tail line (2 Sep): "BUILT OUT UNTIL HE names the first category to judge by eye, or says 'no'" — one of Colouring 43 · Warping 28 · Blur 19 · Generative 19 · Stylize 19 · 3D 18 · Drawing/Edge 16 · Keying 11 · Opacity 7 · Text 6 · Repetition 5 — for the subjective "does each effect LOOK good / is
**Evidence:** SHIPPED ROUNDS ARE REAL AND WIRED. Round 1 (v12.05, POLISH-LOG.md:992): js/inspector.js:1569 builds the "Only used when <Band> is Custom" hint; test 'mode-gated sliders are live in their own mode and locked outside it' at tests/tests.js:51342. Round 2 (v12.18, POLISH-LOG.md:1005): js/compositor.js:5577-5586 electricedges kernel divides Sobel by 1442 then applies the 3.6 gain with the round-2 comme
**Could be built or decided now:**
- Round 4 of the ceiling scan: measure the 38 CANVAS_FX kernels (js/compositor.js:9532) and the WARP_FX table (js/compositor.js:7806) the way v14.81 measured the pixel table (tests/_482ceil.html pattern: walk each px/magnitude slider to 1.5x-8x, keep the last value that still earns it, verify ends render at 1080x1350), raise what earns it, add the canvas/warp pairs to the RAISED list in the fx-raise
- Rule 16 on the category question: pick Colouring (43, the largest, and the one his filters work builds on) as the recommended first category, render every Colouring effect at min / default / max on real footage as one contact sheet (no such sheet exists; tests/_afxsheet.html shows the pattern), rank the weak-looking ones with the picture, ship the sheet as an artifact and let him change the catego
- Rule 16 on the speed controls (Hz/px/s: glitch speed, scanline roll, cloud drift): decide and record it — recommended: leave speed ceilings alone (faster is taste, not reach, and cyclic/animation params were excluded from the scan for good reason) and strike the ask, naming the alternative so he can say 'open them' in one word.
- Split his verbatim quote into a numbered clause list ("go through every single effect", "think of ways to improve it or give it more function where reasonable", "improve their quality") and tick what the four shipped rounds actually cover — the entry has zero '1. [ ]' lines, which is a plain miss of the quote-and-tick rule.
- Once the spotcheck of 9e65e47 lands (tools/.spotcheck.log), record its verdict in the entry; if the property test is reported DEAD/NOT-PROVEN under revert, add a companion assertion pinning the specific raised numbers (e.g. edge.amount max >= 24) so a silent revert of the catalog is caught.
**Recommendation:** Reclassify from "built out — waiting on him" to ACTIONABLE. The shipped work is real (four rounds, all present and tested), but the parking is not honest: the entry's own tail names an unbuilt mechanical round (canvas + warp ceilings, "NOT A DECISION FOR YOU. Next round.") that never happened, the category choice is exactly the kind of decision rule 16 says to make and show, and the contact sheet 

### #570 — needs him: no · built out verified: yes
**Waits for:** The tail line (written 2 Sep, commit f1483f1) says: "BUILT OUT UNTIL HE picks: Smooth (recommended) · Stepped but finer · Leave it" — i.e. a design letter for how the add-row switch should move during a layer drag. The unblock page (tools/unblock/unblock.html:321-331) asks him the same A/B/C questio
**Evidence:** (1) HE ALREADY ANSWERED THE QUESTION IT IS PARKED ON. REQUESTS.md:25228-25229 records his verbatim reply "570 stepped", added 26 Aug in commit 6da7c14 (git log -S). The "BUILT OUT UNTIL HE picks: Smooth…" line was written a week later (2 Sep, f1483f1) and re-asks the same three options; the unblock page recommends Smooth, the option he rejected. tools/next.sh's STALE ASKS banner did not fire becau
**Could be built or decided now:**
- Close #570 as DONE under his own answer: he picked "stepped" on 26 Aug (REQUESTS.md:25229), and the code already flips the switch exactly when the dragged block crosses the add row (js/timeline.js:1602-1604; measured today at 380px: 0.50×5 then 0.33, flip at dy≈106px on a 43px pitch). Write the tick with the picture: the switch flips once per crossing because the add row's layer index has only two
- Strike the stale re-ask: remove/strike the 2 Sep "BUILT OUT UNTIL HE picks: Smooth…" line and the "❓ QUESTION FOR HIM" block at REQUESTS.md:25279-25289 (they re-ask what "570 stepped" answered), and remove card data-id="570" from tools/unblock/unblock.html:321-331, then republish the unblock artifact with url= (#777).
- Tighten the existing test rather than wait for his phone: extend tests/tests.js:4012 ('438: the add-row switch moves WHILE you drag') or add a '570' test using the same run() helper — assert the FIRST --sw change lands within one slot pitch of the add row's crossing (sample dy vs listTop + (addSlot+0.5)*slotH), not merely spread >= 0.05. Today's samples (flip between dy 85 and 106 with the crossin
- Fix the detector that let this sit: tools/_classify.py:372 `_ASK` matches only "❓ASK"; this entry's "❓ **QUESTION FOR HIM" plus its recorded "He answered" is exactly the STALE ASKS contradiction the rule exists for. Widen the regex to `❓\s*\**\s*(ASK|QUESTION)` and add #570's shape as a self-test case.
- Correct the entry's false 31 Aug claim in place: the test browser CAN drive a layer reorder (test 438 and tests/_switchlive.html both do, with FM.selectLayer(null) first); the "single row for six layers" was the phone SOLO view with a layer selected.
**Recommendation:** Not a legitimate wait. He answered "stepped" on 26 Aug; the 2 Sep "BUILT OUT UNTIL HE picks" line and the unblock card re-ask a decided question and recommend the option he rejected. The stepped behaviour is already built and verified today (test 438 green on a private port; probe shows one flip exactly at the add-row crossing), and "stepped but finer" cannot exist because the add row's index has 

### #484 — needs him: no · built out verified: partly
**Waits for:** The tail line (written 2 Sep, commit f1483f1) says: "BUILT OUT UNTIL HE picks A, B or C for Silk Ribbon (A = a shape under Add → Shape beside the banner and flag — recommended · B = an effect · C = skip it). Every other clause in this entry shipped." The open ❓ASK line asks the same A/B/C. Nothing p
**Evidence:** Shipped code claims verified statically (all present and wired; a dynamic tools/spotcheck.sh of accc3e9 could not finish in 10 min because another session's full suite and spotcheck were occupying the machine — so mutation-level proof is could-not-tell): 22 renamed labels all in js/compositor.js as `label:` with type keys unchanged, e.g. :1066 copybg→'Backdrop Clone', :723 counter→'Number Roll', :
**Could be built or decided now:**
- Silk Ribbon (the only stated block): rule 16 — take option A, the recommended one. LIB_SHAPES in js/addmenu.js:122-133 is a plain list of [key,label] pairs and 'banner'/'flag' are already there (line 126-127); add a 'ribbon' entry with a drawn path, render it at 24px and ship size, ship it, and show him the picture with B (effect) and C (skip) named so he can change it in one word. Alternatively d
- His 1 Sep requirement (rename EVERY effect and change the ordering) is recorded in the entry and in BEFORE-PUBLISHING.md:7-24 and then nothing was done on it. Its own 'SHAPE OF THE WORK' step (a) — a side-by-side of our names vs AM's — is a script, not a question: our 205 `{ type, label }` defs in js/compositor.js against the 93 AM names the entry captured from his screenshots. Measured today: 72 
- Category ORDER: CATEGORY_ORDER at js/fx-registry.js:147 is a display-only array (['color','blur','distort','proc','stylize','drawing','move','repeat','matte','opacity','text','threed']); no saved project references it and no test pins it (grep CATEGORY_ORDER/category order in tests: 0 hits). Reordering it to be deliberately unlike AM's can ship today with zero migration risk. Also 'Opacity / Visib
- Label-only renames of the remaining AM-identical names he now wants changed: the pattern is already proven safe by the v14.09 test (tests/tests.js:47600-47670 resolves every renamed type key and asserts labels moved) — draft proposals for the 72, ship the ones that are not pure craft terms, and give him the veto table, exactly as clause 1 did. Note the existing test's 'keepNames' block (chromakey/
- Leftover AM name in user-facing text: js/fx-registry.js:334 — the Backdrop Lens (magnifybg) description still reads 'Copy Background through a lens…' and DESCRIPTIONS feeds `desc` (fx-registry.js:574), which fx-browser.js:578-579 renders on screen. The v14.15 BRANDS sweep (tests.js:47658) only checks company names, so it cannot catch this. Fix the string and extend the sweep with the 22 retired AM
- Clause 3 ('Add anything else genuinely good for this app', his invitation) is unticked and has had no work; it does not depend on any answer from him. Either build one or close it explicitly with a reason — leaving it unticked while claiming 'every other clause shipped' is the contradiction.
- Re-ask, if re-asking at all, only with a picture: the entry's own item 1 (1 Sep) says 'Re-ask only with a picture, or not at all' and #545 says draw it; the 2 Sep BUILT OUT line and the unblock page card (tools/unblock/unblock.html:310-319) re-asked the identical A/B/C with no picture. No ribbon drawing exists anywhere in the repo (grep -i silk/ribbon over js/ tests/ tools/ fx-art/: only the unblo
**Recommendation:** Reclassify #484 out of 'built out — waiting on him': the block is a self-imposed A/B/C with a recommended option, which rule 16 says to decide, and his 1 Sep answer replaced the small question with a large unaddressed requirement that the entry itself calls not done. Concretely: (1) take option A — add a 'ribbon' shape to LIB_SHAPES in js/addmenu.js beside banner/flag, ship it with a rendered pict

### #592 — needs him: no · built out verified: no
**Waits for:** "BUILT OUT UNTIL HE sends one screenshot of the overshoot at v15.03 or later, with the timeline zoomed as he had it" (REQUESTS.md line ~24095, dated 2 Sep). Earlier in the same entry it also asks "is it still doing it?" — which he already answered on 27 Aug ("yes it's overshooting, why don't you che
**Evidence:** NOTHING WAS BUILT: `git log --oneline | grep 592` shows only three docs-only commits (de51a9f log, 2b8c556 REQUESTS.md+LOOP.md, d9d84e5 REQUESTS.md); no js/, styles.css or tests/tests.js change references 592 (grep tests/tests.js '592' hits only unrelated numbers at 50747/50762). So 'built out' = 'measured twice, changed nothing'. THE MEASUREMENT WAS WRONG: js/timeline.js:2686-2689 sets `--ar-x1 =
**Could be built or decided now:**
- FIX (one CSS line, no decision needed): styles.css:7531 `.tl-addrow::before { left: var(--ar-x0, 0px); … }` — change to `left: calc(var(--ar-x0, 0px) - 1px);` (mirrors the `top: -1px; bottom: -1px` on the line above it). The pseudo-element is absolutely positioned inside the row's 1px dashed border (padding box), so its right edge lands at row.left + 1 + --ar-x1, i.e. exactly 1px past the clip and
- TEST that stands in for his phone (tests/tests.js): at 380px, stage a deselected 4s shape clip in a 4s project, rebuild the timeline, and assert `row.getBoundingClientRect().left + parseFloat(getComputedStyle(row).borderLeftWidth) + parseFloat(getComputedStyle(row,'::before').left) + parseFloat(getComputedStyle(row,'::before').width)` equals the clip's `getBoundingClientRect().right` within 0.5px 
- The entry's own unexecuted NEXT step ('measure the CLIP'S RENDERED PIXELS, not its rect', 27 Aug) — it was never done; the 2 Sep line went back to asking him instead. The border-radius suspicion is moot now that the border-offset cause is measured, but it should be struck with the reason.
- Strike the stale ask: the entry's ❓ 'is it still doing it?' is answered in the entry itself ('yes it's overshooting… check for urself', 27 Aug); the 2 Sep line asks for a second screenshot of a symptom he had already photographed. Under rule 16 there was nothing to wait for.
- The 'project longer than its content cannot be staged' claim is TRUE (verified: setting project.duration=6 with a 4s clip came back 4 after timeline.rebuild() alone) — but it is irrelevant, since the overshoot shows in the basic 4s-clip/4s-project state the entry measured, and the unblock card for #592 (tools/unblock/unblock.html:554) can be removed once the fix ships.
**Recommendation:** Reclassify: NOT built out and NOT waiting on him — it is a 1px bug with a measured cause and a one-line fix. Ship `left: calc(var(--ar-x0, 0px) - 1px)` on `.tl-addrow::before` (styles.css:7531), bump styles.css's ?v=, add the pseudo-element-edge test above with tolerance 0.5px, mutation-check it, and show him a 380px before/after close-up with the alternative (subtract 1 in timeline.js) named. Str

### #606 — needs him: partly · built out verified: partly
**Waits for:** The tail line reads "BUILT OUT UNTIL HE sends one screenshot of that corner at v15.06 or later" — one phone screenshot of the timeline's right-hand drag-handle corner (the add-layer row's bare ≡ next to the layer rows' boxed ≡) on a build at or after v15.06, so the 3 Sep numbers can be compared agai
**Evidence:** styles.css:7802-7807 — `.tl-addrow-grip { position: sticky; right: 3px; … width: 34px; height: 100%; min-height: 30px; padding: 0 9px }` (the v13.47 change, present; `git log -L` shows only 1c8793f and 945e0ec ever touched it). styles.css:4993 — `.row-drag { position: sticky; right: 5px; … width: 30px; height: 30px; display:flex; align-items:center; justify-content:center; padding:0; background:#0
**Could be built or decided now:**
- Write the missing regression test for clause 1 (tools/spotcheck.sh 1c8793f reports 'NO TEST — this commit changed app source (styles.css) and touched no test in tests/tests.js'): with 2+ layers and the slim add row in the phone layout, assert |.tl-addrow-grip centre-x − .row-drag centre-x| < 0.5 and |.row-drag path centre − .row-drag box centre| < 0.5 on both axes, at scrollLeft 0 and scrollLeft>0
- Tick clause 1b now — it is a DO-NOT constraint, not work, and it is satisfied: styles.css:7803-7806 keeps width 34px, no border, and js/timeline.js:2906-2910 still builds the grip as three <span>s. Leaving it unticked is the only reason the entry looks unfinished on clause 1.
- Record the 440x956 measurement the entry itself named as the open hypothesis ('or his 440px phone lays the sticky grip out differently') and never ran, although his device size is on record at REQUESTS.md:15615 and :15770 says 'Measure this at his size, not just 380px'. Measured here at 440x956 on v15.57: layer box 405–435, its ≡ 412.5–427.5, glyph path 415–425 (centre 420 both axes = box centre 4
- Rule 16 on clause 2: decide it as 'measured centred at 380 and 440, four ways, no change made' and SEND HIM THE PICTURE of that corner at 440 with the numbers drawn on it (the entry asks him for a picture but never shows him one), naming the alternatives in one word each — LEFT / RIGHT / UP / DOWN nudge of the glyph — so a one-word reply reopens it. Do not nudge blind (#592), but do not hold the i
- Check which picture the '28 Aug SCREENSHOT RECEIVED (at v13.92)' line actually describes. It was written in commit 7c1ba5a (v13.94, whose subject is #661, not #606) and its description — bare ≡ circled, arrow down to a boxed one, arrows at the ≡ inside a box — is word-for-word the 27 Aug v13.45 (pre-fix) annotation. v13.47 bumped styles.css?v=632→633 so a v13.92 phone could not have been serving t
**Recommendation:** Keep the ask (one phone screenshot / one word) but stop treating the item as fully built out: (1) add the alignment test — the only shipped change in this entry has no test and spotcheck flags it; (2) tick 1b, which has been satisfied since v13.47; (3) replace the untested "his 440px phone" sentence with the 440 numbers measured here (aligned in every scroll/zoom state); (4) under rule 16 close cl

### #624 — needs him: no · built out verified: no
**Waits for:** One word from Ezra picking reading (a), (b) or (c) of his sentence "When you hold on a layer to select like on the left side of the screen, it should open up the edit menu for that layer because you're trying to multi select". Entry tail (REQUESTS.md:22784): "2 Sep — BUILT OUT UNTIL HE picks (a), (b
**Evidence:** REQUESTS.md:22760-22784 — the entry contains no build: no clause list, no test, no version; it says 'Do NOT build any of these until he says which' and then tags itself 'BUILT OUT UNTIL HE picks (a), (b) or (c)'. grep for 624 outside REQUESTS.md finds only LOOP.md waiting-on-him lists (562, 592, 652, 675, 744) and tools/unblock/unblock.html:333-343 — no commit, no POLISH-LOG line, no test. So 'bui
**Could be built or decided now:**
- DECIDE under rule 16: the code collapses the three-way ambiguity, so this is a taste call with one live option, not a coin flip. Readings (b) and (c) are ALREADY the shipped behaviour — js/timeline.js:1232-1233 (head pointerdown → 380 ms → beginPaintSelect) → js/timeline.js:1268-1270 (selectMode=true; toggleSelect(layer.id,true)) → js/app.js:2776 (toggleSelect sets scene.selectedId = the HELD laye
- Build (a): in js/inspector.js:6473 treat select mode as multi — `const multi = (ids.length >= 2) || !!FM.selectMode;` — so a hold with one layer selected renders no single-layer category grid (render nothing or the existing alignRow, not the 8 tiles); and in js/mobile.js:53-57 do not raise the sheet while `FM.selectMode && ids.length === 1` (the sheet is derived from the selection there, so gate i
- Write the test that stands in for his phone (none exists — `grep -c cat-grid tests/tests.js` is 0, and the closest test at tests/tests.js:18494 only fakes the state and checks the top-bar buttons, never the inspector): at 380x780, real pointerdown on a `.track-head`, wait >380 ms, pointerup; assert body has `sel-mode`, `#m-selcount` says '1 selected', and the inspector contains NO `.cat-grid` and 
- Split his one sentence into numbered clauses in the entry (there are none — the entry has zero '1. [ ]' lines, which violates the quote-him-verbatim rule's step 2): 1. hold on a layer head selects it (already true, tick with evidence); 2. what the edit menu does during that hold (the (a) decision); 3. 'for that layer' — the menu, when it opens, is for the held layer (already true via app.js:2776, 
- Remove the 'BUILT OUT UNTIL HE' tag from this entry and the #624 card from tools/unblock/unblock.html (republish with url=) — nothing was built, so the tag is the misuse LOOP.md:1732-1736 names: 'That is not blocked, it is unstarted. Decide, ship, and say plainly which option you took and how to overrule it.'
**Recommendation:** Reclassify: not built out, not blocked — unstarted. The 'three readings, two opposite' framing is wrong against the code: (b) and (c) already describe current behaviour, so the only decision is 'change to (a)' versus 'leave it', and the entry already rated (a) likeliest. Under rule 16 pick (a): in select mode with one layer held, do not raise the single-layer category grid or the phone sheet (insp

### #642 — needs him: no · built out verified: partly
**Waits for:** The tail line: "BUILT OUT UNTIL HE picks A, B, C, D or 'none'" — a letter for clause 3 (home-screen background) from the 3 Sep mock: A as shipped (recommended), B aurora ribbon, C one deep glow, D pools + grain + vignette. Clauses 1 and 2 (white box, white bars) are ticked as shipped in v14.26. The 
**Evidence:** Clauses 1–2 wired and shipped: index.html:186-201 adds `splash-light` for everyone and runs `if (pos) { pos.remove(); }` on #splash-poster (markup still at index.html:94); styles.css:1919-1923 `#splash.splash-light #splash-vid { width:100vw; height:100dvh; object-fit: cover }`; theme-glass.css:838-841 same rule with `max-width:none`; styles.css?v=673 and theme-glass.css?v=47 linked at index.html:4
**Could be built or decided now:**
- Decide clause 3 under rule 16 right now: keep A (the recommended option, and it is literally what already ships — styles.css:6007-6013 three radial pools, 12s/17s drift), tick clause 3 as 'decided: A kept; B/C/D drawn and on the unblock page, one word changes it', strike the BUILT OUT line, and close #642. Zero code to write; the unblock card at tools/unblock/unblock.html:345 stays as his override
- If a session wants the 'experiment' to produce a visible change rather than a no-op, ship D (grain + vignette over the existing pools) — it is an additive overlay (~20 lines of CSS/one canvas) that leaves the load-bearing pool rules untouched, screenshot at 380px in the LIGHT home, and name A/B/C as the alternatives.
- Re-render the mock in the light home look: js/settings.js:39 has homeLight: true by default and theme-glass.css:555 paints a different (transparent-over-light) ground, but the surviving 3 Sep home642.png shows four DARK phones — so 'A as shipped' in the picture is not the ground he sees by default. The 29 Aug text claimed both looks were rendered; no file from 29 Aug survives anywhere under /priva
- Fix the mock's recommendation text: it says A is recommended because 'it is the one you already said was close to actually looking good' — that quote is REQUESTS.md:22193, item #616 (timeline blue bars), not the home background. The justification is borrowed from an unrelated item.
- Reconcile the two rounds of letters: 29 Aug sent A pools / B ribbons / C horizon / D mesh (D recommended); 3 Sep sent A as shipped / B aurora / C glow / D grain (A recommended). Same letters, different meanings, recommendation flipped D→A with no reason given. If he answers 'D' from the first picture the unblock page will ship grain, not mesh. One session can state which set is live and why the re
- Clause 1's own unresolved note — 'Say which of the two it was once he confirms' (empty <img> placeholder vs the light page painting for a frame) — does not need his phone: build a headless-Chrome probe that boots index.html with the old empty viewport-sized <img> restored in a worktree, captures frame 0, and measures whether a light box paints. That turns the HYPOTHESIS the entry admits to into a 
- Housekeeping seen in passing: tests/tests.js:48044-48046 asserts the DARK film keeps object-fit contain, but index.html:186 says splash-dark was removed in queue 776 — the assertion is vestigial and no longer guards anything.
**Recommendation:** Not a legitimate block. Everything that needs code is shipped and tested; the one open clause is a taste pick with options drawn and one recommended, which is exactly the case rule 16 says to decide. Since the recommended option A is what already ships, the decision costs nothing: tick clause 3 as 'decided under rule 16 — A kept; B, C, D drawn and on the unblock page, say one letter to switch', st

### #619 — needs him: no · built out verified: yes
**Waits for:** The tail line (REQUESTS.md:22510) says "BUILT OUT UNTIL HE has seen the fill-in sheet" — i.e. a look, on his phone, at the Insert-your-Media sheet, plus a heads-up that after his #505 answer the PRIMARY tap on a template card now opens it for editing and the fill sheet moved to ⋯ → "New project from
**Evidence:** js/template-fill.js (14947 bytes, loaded at index.html:1007 as ?v=3): kindOf()/slots() count video/image + text + shape, sorted by start; showControlFor() removes all three controls from the DOM and mounts exactly one (Replace Media / 'Your words' text input / 'Your colour' color input); textIn/colorIn 'input' handlers write layer.text / layer.fill live, markDirty, paint(), refreshChip(); 'change'
**Could be built or decided now:**
- Tick #619 at the current version under rule 16 (a shipped feature he has not looked at is not a paste only he can supply). Run tests/_619fill.html at 380px, put the resulting picture/measurements in the entry, and write the one-word escape: 'tap = edit (your #505 answer); ⋯ → New project from template = fill-in sheet; say NO if you want the fill route gone'. Then delete the #619 card from tools/un
- Fix a stale test name: tests/tests.js:55405 is still titled '619: the template media-swap opens only with media, and never silently' while its body (lines 55423-55424) asserts the OPPOSITE — a text+shape template must report 2 slots and OPEN. A green line in the run log currently reads as the old media-only rule being in force. Rename to what it asserts.
- Add a wiring test for the only route left to the feature: no test in tests/tests.js drives templateCard's ⋯ menu (0 hits for 'New project from template', 0 for templateCard); the suite only calls FM.templateFill.open() directly. A test that builds a template, opens its card's ⋯ menu, picks 'New project from template', waits ~500ms and asserts FM.templateFill.isOpen() would catch the 420ms setTimeo
- Decide (rule 16, no answer needed) whether the ORDER should match his Alight Motion reference — the entry says 'the template's slots listed, each one tapped to drop in your own clip, and THEN it becomes a project. Not a fork you then have to hunt through.' Today js/home.js use() mints the project first (FM.templates.useAsNew, toast 'Creating project…') and the sheet floats over it 420ms later; pre
**Recommendation:** Not a legitimate block. The code is real, wired on both routes (tap → edit; ⋯ → New project from template → fill sheet), tested in the suite and probed at 380px; the only thing 'waited for' is him looking at it, and the unblock page has since re-parked it on exactly the scope question ('still want it?') the entry's own header said never to ask. Under rule 16: tick #619 at the current version with 

### #604 — needs him: no · built out verified: yes
**Waits for:** The 2 Sep tail line (REQUESTS.md:21544): "BUILT OUT UNTIL HE does the one check above" — export something short with sound on the PC, drag the exported .mp4 into a Chrome tab, press play; sound or no sound decides which half the bug is. Secondary mention: "The mixer's own drop report is in Settings 
**Evidence:** Probe pages exist and are committed: tests/_604sfx.html (6600 B) and tests/_604boxes.html (10151 B), commit 3d1a591 (27 Aug, tests/docs only — also added tools/probe.sh and 68 lines to tests/tests.js). Sink test: tests/tests.js:13501 test('export: the streaming MP4 sink assembles bytes exactly, across folds and out-of-order patches', {item:'604'}) drives FM._createMp4Sink (seam at js/exporter.js:1
**Could be built or decided now:**
- DOCS (stale ask, answered cross-entry): #662 (REQUESTS.md:26236, closed 28 Aug) is headed 'The export bug is MOBILE-ONLY. PC exports fine. (28 Aug — answering the question #604 and #215 have both been waiting on.)' and quotes him verbatim: 'The export issues I have were on mobile, I don't got my pc rn but I've exported pc before just fine'. That IS the answer to #604's PC-Chrome check, given five 
- CODE (zero-chunk hole in the VIDEO path, needs no answer): js/exporter.js:978-990 nulls `mix` only when encodeAudio THROWS. If AudioEncoder resolves with zero output chunks (a real mobile-only possibility), mix stays set, the muxer declares an AAC track (js/exporter.js:1001 `audio: mix ? {...}`), nothing is fed (js/exporter.js:1157 loops an empty array), the progress label reads 'audio + video' (j
- CODE (make the device measure itself): port tests/_604sfx.html's decode-and-measure (decodeAudioData on the finished blob → duration, channels, peak, rms) into the export report at js/exporter.js:1196-1236, guarded try/catch and not able to fail the export, so the one paste he owes says whether the file that left HIS phone contains audible sound rather than only whether a track was declared. Also 
- TEST (stands in for the PC check he was asked for): under CDP, load the exported blob into a <video> element via object URL, play it, and assert webkitAudioDecodedByteCount > 0 (and audioTracks.length === 1 where exposed). The entry's own step 1 named this method ('feed it back into a <video> and read mozHasAudio / webkitAudioDecodedByteCount') and then used the lenient decodeAudioData instead. Lo
- TRIVIAL: js/exporter.js:158 still says 'exposed for tests/_mp4sink.html, which checks the assembly against a dense reference buffer' — that file does not exist (ls tests/ shows only _604boxes.html and _604sfx.html); the real test is tests/tests.js:13501. Point the comment at the test.
- CLAUSES: the entry has NO numbered '1. [ ]' checklist at all, against the standing rule to split his message into his own clauses. His verbatim message carries four: (a) 'still laggy' — not mentioned anywhere in #604's 160 lines; (b) one sound effect 'cut in and out' during playback — deferred to #663 with 'Log it, do not merge it', and the v13.80 export-side limiter was then presented as fitting 
**Recommendation:** Take #604 off the built-out bucket: its stated wait (the PC-Chrome check) was answered by Ezra on 28 Aug in #662 and the 2 Sep 'BUILT OUT UNTIL HE …' line was written after that answer without reading it — the cross-entry form of the invisible-answer failure. Record HE ANSWERED (quote #662), strike the PC ask, and close #604 as superseded by #215/#677 (phone paste) and #663 (playback), with 'still

### #564 — needs him: no · built out verified: partly
**Waits for:** The tail line (REQUESTS.md:20851, dated 2 Sep) says: "BUILT OUT UNTIL HE picks A, B or C for the Outline & Shadows rows (the three options are drawn above; the Trim-path glyph gets redrawn at 30px once he picks, whichever it is)". I.e. it waits for one letter from him choosing between three row desi
**Evidence:** (1) The thing the entry says was built does not exist: `scratchpad/564-options.html` (REQUESTS.md:20836) is absent from the repo, from every session scratchpad under /private/tmp/claude-501, from ~/.claude, and from all git history (`git log --all -- '*564-options*'` returns nothing; `find` for '*564*' finds only unrelated files). The options cannot be shown to him because there is nothing to show
**Could be built or decided now:**
- Strike the 2 Sep 'BUILT OUT UNTIL HE picks A, B or C' line (REQUESTS.md:20851) and the 'Waiting on his pick' block (20836-20845): the pick was made under #610 and shipped in v14.20, and he said 'Right now it's good' on 1 Sep. Cross-link #564 <-> #610 in both entries (neither mentions the other).
- Remove the #564 card from tools/unblock/unblock.html:450-458 (it offers A/B/C buttons for a mockup file that does not exist and a question he has already answered) and republish the unblock artifact with url= per #777.
- Build the delegated fix under rule 16 — he wrote 'you figure out the best fix… I'll just tell you if what you did is bad after it is done'. Recommended: keep the tile grid, and group each enabled feature's detail rows under a small labelled sub-header (Outline / Trim path / Dashes / Shadow / Repeater) with a thin divider, so two features open together are distinguishable (today Outline+Shadow yiel
- Measure, do not guess, the 380px panel height with all five tiles on (the entry itself said this was 'worth measuring' and nobody did): screenshot the phone sub-panel with Outline, Trim path, Dashes, Shadow and Repeater enabled on a rect, before and after the sub-headers, and send him that picture with the alternative named so he can change it in one word.
- Resolve the ambiguity in his sentence by building, not asking: 'section' could also mean the inspector's sub-panels (one open at a time on phone). State that reading in the same message ('if you meant the cards, say cards') rather than parking the item.
- Write a test: with Outline and Shadow both enabled, the border sub-panel contains both feature groups at once, each under its own labelled header, and the header count equals the number of enabled tiles (this also pins that the panel is NOT an accordion, which is what his 1 Sep complaint reads as).
- Redraw the Trim-path glyph at 30px if the #432-style 'broken corner' still shows — check `BS_ART.trim` in inspector.js (~150) at the shipped size; this does not depend on any answer from him.
- Re-run `tools/spotcheck.sh 3687194 --width 380` on a quiet machine (this audit's run was NO-CONTROL because three spotchecks were competing and the control suite timed out at 300s), so the v14.20 'proof' is either confirmed or its test is fixed.
**Recommendation:** Reclassify #564 from 'built out — waiting on him' to workable and take it off the unblock page. It is not waiting on him on any reading: the A/B/C pick it names was made and shipped as #610/v14.20 (tiles), the mockup file it points at does not exist anywhere, and his 1 Sep answer — quoted inside the same entry — signs off the look and delegates the remaining fix with 'I'll just tell you if what yo

### #674 — needs him: no · built out verified: partly
**Waits for:** The tail line reads "BUILT OUT UNTIL HE answers (b) and (c)": (b) whether the transport buttons' TAP AREA grows to 44px with the icons unchanged, and (c) whether Export / Canvas / Help stay reachable while a layer is selected. Both are design/taste choices; neither is a paste, a screenshot or a look
**Evidence:** REQUESTS.md:26628-26651 — entry; no numbered clauses, five '·' bullets, two '⚠️ … I have not checked' flags, tail 'BUILT OUT UNTIL HE answers (b) and (c)'. INK HALF (true): js/timeline.js:1767-1776 — `const onPicture = (layer.type === 'video' || layer.type === 'image') && !layer.audioOnly; if (onPicture) ink = INK_LIGHT;` before `clabel.style.color = ink.color`; test tests/tests.js:53329 '674: a c
**Could be built or decided now:**
- (b) DECIDE under rule 16, do not ask. Recommended: leave the transport at 34x34 in its 40px row and close the ask, naming the alternative. Grounds already in the repo: #469 (REQUESTS.md:14914-14919) — he answered the identical 'grow the invisible tap area to 44 on the strength of the 44x44 guidance' question with 'No, they're fine' and the entry says not to re-raise it; 8 controls x 44px = 352px d
- (c) DECIDE under rule 16. Recommended: keep 'the selection owns the bar' exactly as it is — styles.css:3563-3583 already records the measured reason (Export slid left under a held thumb and the bin landed at 334..372 where Export had been; a hidden button cannot move) — and add the CUE the QA finding actually asked for, since #m-selcount already prints 'N selected' (app.js:871-877) and #m-back alr
- The 3 Sep note says 'the other catch-all is checked below' and never reports the result — report it. The guard test 'no catch-all puts a raw error message on screen' (tests/tests.js:57645) scans js/app.js ONLY; raw err.message still reaches him via toasts in js/captions.js:454 ('Speech detection failed: ' + err.message), js/sfx.js:653 and :666, js/voice-rec.js:690. Route those through FM.reportErr
- Strike or make true the claim 'the global error handler already points at Settings → Last error → Copy': there is NO window.onerror / unhandledrejection handler anywhere in index.html or js/*.js (grep is empty). Only FM.reportError's alert text mentions Settings, and it has exactly three callers (app.js:4071 import, :4674 export, :5275 sample clip). Build it: a window.addEventListener('error'/'unh
- Check the two '⚠️ NOT REPRODUCED and I have not checked' flags — the first reproduces by reading: js/storage.js:233 warmReverseCaches() fires FM.ensureReverseCache for every reversed/slow clip WITHOUT awaiting, and app.js:1365-1368 has each call put up the same sticky 'Preparing frames…' toast and each `finally { FM.hideToast(); }` — with two reversed clips the first to finish hides the toast whil
- Turn the entry's five '·' bullets into the numbered '1. [ ]' clause checklist the CLAUDE.md rule requires and tick what shipped (ink: yes, v15.07, spotcheck PROVEN; export/import/sample-clip catch-alls: yes; other files' catch-alls: no; wrap: decided-not-built; touch targets: (b); project actions: (c)). Today nothing in the entry can be ticked because nothing is listed as a clause.
- Record the (a) decision honestly: #98 answered the default SIZE ('160pt — what you have now', REQUESTS.md:1755), not whether text WRAPS or overflows a 1080 comp. Either state 'text does not wrap, by decision — AM does not either' as a rule-16 pick with the alternative (wrap at the comp edge) named, or build the wrap. Right now the two are conflated.
**Recommendation:** Reclassify: not a legitimate 'built out — waiting on him'. The one half that is claimed built and proven (the clip-name ink) is genuinely built and spotcheck-PROVEN, and the export catch-all is genuinely humanised — but the entry also states a falsehood (a global error handler that does not exist), leaves two '⚠️ not checked' flags that it could have checked (one reproduces by reading two function

### #676 — needs him: partly · built out verified: yes
**Waits for:** One line from Ezra, from his own phone, describing what "twice" looks like when the add sheet opens: (a) the sheet slides up twice in a row (one animation replaying), (b) it opens, closes and opens again (two gestures being seen), or (c) a second copy lands on top — and, since v15.08 (3 Sep), whethe
**Evidence:** CSS fix present: styles.css:8780 `#add-sheet.open { animation: fm-hinge-up 360ms … both; transform-origin: 50% 100%; transition: none; }` with the queue 676/706 comment at 8771-8779; reduced-motion restore at styles.css:8799-8800; base rule still `transition: transform .22s ease` at styles.css:4128 and `.closing` (queue 773) at 4132. Test present and wired: tests/tests.js:53391-53421, test('706: t
**Could be built or decided now:**
- Re-entrancy guard in openAdd() — js/mobile.js:366. Today `openAdd()` has no `if (addSheet.classList.contains('open')) return;`: a second call while the sheet is already open runs close(); redrawAdd() (rebuilds the whole menu body via FM.addMenu.render) and syncAddSheetTop() mid-hinge — which IS the 'opens twice' look for the two-gestures candidate. The guard does not block reopen-mid-close (closeA
- A 'Your last add-menu open' report in Settings, the same shape as 'Your last scrub' (js/settings.js:606-628, written from js/timeline.js:3264 into localStorage 'fm.lastScrubReport', Copy button, toast 'paste it to me'). In openAdd(): count openAdd calls within 1s, `.open` flips, animationstart/animationend/animationiteration on #add-sheet (with animationName), transitionrun/transitionend for trans
- Decide, per rule 16, and name the alternatives on the unblock card: RECOMMENDED = ship the guard + the report (zero iOS risk, both testable in Chrome, and a spotcheck-able test). Alternatives to name for him: (A) iOS-only fallback — under `@supports (-webkit-touch-callout: none)` replace fm-hinge-up's perspective()+rotateX with the plain slide, since perspective+rotateX on a position:fixed sheet i
- Rephrase the unblock card (tools/unblock/unblock.html:505-512) from 'describe which of the two it looks like' to 'open the add menu once, then Settings → Your last add-menu open → Copy → paste', once the report ships. And make #706 the single carrier of the ask: #676's tail should say 'see #706' instead of repeating the question — two entries re-reading the same open ask every tick is the cost the
- Strike the 'live candidate' the entry left standing: '#676 says the sheet's measured top is published AFTER opening' — the code shows syncAddSheetTop() runs BEFORE classList.add('open') in the same synchronous call (js/mobile.js:367-370), and its only other caller is the resize listener (mobile.js:410). That candidate was falsifiable from the source and has sat as an open question since 30 Aug.
**Recommendation:** Reclassify from 'built out — waiting on him' to workable. The observation itself is genuinely device-only (Chrome cannot show an iOS animation replay, and the 2 Sep real-touch probe found no double by any route), so the ask is legitimate as far as it goes — but under rule 16 it is not the end of the work. Ship two zero-iOS-risk changes that make his description unnecessary: (1) the re-entrancy gua

### #657 — needs him: partly · built out verified: yes
**Waits for:** A pasted 10-second "what's slow" sample taken WHILE SCRUBBING: "press play AND drag the playhead, then Settings → what's slow → Copy" (REQUESTS.md:26035, 3 Sep BUILT OUT line). The entry's own earlier NEXT line (:26007-26009) says the sample must come from the PHONE; the unblock page card asks for i
**Evidence:** VERDICT FIX (v14.15) IS THERE AND WIRED: js/perf-probe.js:141-150 — `else if (m.effective != null && m.effective < 0.75)` prints 'READ: the frames are steady, but they are BOUGHT — this is drawing at N% scale', 'softer while playing', and names Settings → Playback quality; called at :466-467 with `effective: qi ? qi.effective : null` from FM.playbackQualityInfo (js/app.js:433-437, effective = play
**Could be built or decided now:**
- Tell him to scrub BEFORE the sample, not after it. The pre-measure prompt js/settings.js:435 ('Press Measure, then use the app normally for ten seconds'), the hint :468, and the toast js/app.js:408 ('Measuring for ten seconds — keep using the app') never say to scrub; the only instruction ('press play AND drag the playhead during the ten seconds') is printed in the FINISHED report at js/perf-probe
- Fix the instruction so it CAN work. The split at js/perf-probe.js:248-252 files a frame as playing if FM.playing, else as scrubbing only if playbackQualityInfo().inMotion — and noteMotion at js/app.js:182-183 returns early while FM.playing, so dragging the playhead WHILE playing produces zero scrubbing frames. 'Press play AND drag the playhead' as written in perf-probe.js:395, unblock.html:233 and
- Decide under rule 16 whether #657 is still open at all. Its finding (the report contradicted itself) shipped v14.15 and is tested. Of the four entries it says it feeds: #125 is [x] (REQUESTS.md:853), #387 is [x] closed by his own 1 Sep answer that scrubbing/playback no longer feel wrong (:14521-14523, :14542-14544) — two days BEFORE the 3 Sep 'built out until he pastes a scrubbing sample' line was
- A test that stands in for his phone on the scrub half: tests/tests.js:50407 only STUBS FM.playbackQualityInfo().inMotion; nothing drives a real playhead drag and checks the scrubbing bucket fills. Add a test that dispatches a pointer drag on the timeline playhead during FM.perfProbe.run(700) and asserts /scrubbing [0-9]/ — it verifies the render-funnel motion detector (js/app.js:181-200) actually 
- Reconcile the three wordings of the ask (phone at :26007, PC on the unblock card, unspecified at :26035) into one, or the answer he gives will not match the question one of them asks.
**Recommendation:** The v14.15 build claim is true and tested. The wait is only half legitimate: the paste is his to supply, but the app never asks him to scrub before or during the sample (the instruction is printed after it ends), the instruction it does give cannot fill the scrubbing bucket (scrub frames are discarded while playing), and the complaint the sample would settle (#387) was closed by his own answer on 

### #706 — needs him: partly · built out verified: yes
**Waits for:** "BUILT OUT UNTIL HE says whether the add menu still opens twice on his phone at v15.08" — and, if it still does, his description of what "twice" looks like (sheet slides up / drops / slides up again, vs the layer added twice, vs the EDIT sheet opening after the add; "a 3-second screen recording woul
**Evidence:** The shipped change is real and wired: styles.css:8780 `#add-sheet.open { animation: fm-hinge-up 360ms …; transform-origin: 50% 100%; transition: none; }` with the explanatory comment at 8771-8779 naming queue 676/706; base rule styles.css:4123-4131 still carries `transform: translateY(100%); transition: transform .22s ease` for the way down; reduced-motion block styles.css:8799-8800 removes the ke
**Could be built or decided now:**
- A phone-side self-report so his 'description' becomes a paste: instrument #add-sheet on open — count FM.mobile.openAdd() calls per tap, animationstart/animationend/transitionrun/transitionend on the sheet, .open class flips, and sample the sheet's translateY over the first ~900ms — and show it in Settings as 'Your last add-menu open' with a Copy button, same shape as 'Your last export' (js/setting
- Test the actual hypothesis on WebKit, which nobody has done: Safari.app is installed (/Applications/Safari.app, /usr/bin/safaridriver present); serve the parent of a653a6f (a653a6f^) and a653a6f on a spotcheck-style worktree port, open at ~380px wide in Safari, add .open to #add-sheet and record animation/transition events + transform samples. If the parent replays and a653a6f does not, the 'iOS S
- Re-run tests/_rt706.py 8777 and tests/_rt706.py 8777 empty at HEAD (v15.57): the table in the entry was measured on 2 Sep BEFORE v15.21 (#773) rewrote openAdd/closeAdd with inline style.transition/style.transform and a rAF release (js/mobile.js:366-395). The 'one motion' measurement has not been repeated since that rework touched the exact property.
- Decision under rule 16 that can be made now: no iOS Simulator runtime is installed (Xcode.app present, `xcrun simctl list runtimes` is empty). Either download one runtime (multi-GB, one-off) so the app can be run on a simulated iPhone Safari here, or state plainly that WebKit-on-Mac is the stand-in — pick one and write it into the entry instead of leaving 'iOS-specific' as a guess.
- Update the unblock-list ask (tools/unblock/unblock.html, #777) so it asks for the paste from the new Settings report rather than a prose description of 'twice'.
**Recommendation:** The 'built out' claim is honest and proven (spotcheck CAUGHT the CSS change), but the block is only half legitimate. The final yes/no does need his phone — Chrome cannot reproduce it and no simulator runtime exists — yet the entry parks on his DESCRIPTION of the symptom, which rule 16 / #591 say not to wait for. Do two things now without him: (1) add a Settings 'Your last add-menu open' report (sa

### #654 — needs him: no · built out verified: partly
**Waits for:** "BUILT OUT UNTIL HE picks A, B or C for the audio panel's exit" (REQUESTS.md:25878). A = a Done button on the panel's own back row (recommended, "reuses the header he asked for in #297"), B = the top-bar arrow becoming an ✕, C = the back row naming where it goes. The entry says the three were "drawn
**Evidence:** SHIPPED HALF (real, wired): js/app.js:855-870 FM.syncSelectionChrome sets #m-back aria-label to 'Done selecting' (selOwns) / 'Close clip options' (phone && n===1) / 'Projects'; called from js/mobile.js syncEditingClass() on every inspector refresh (mobile.js:44-45, 300). index.html:298 is the #m-back button with the default aria-label "Projects / file". js/mobile.js:314-315 confirms the diagnosis:
**Could be built or decided now:**
- Under rule 16, ship option A now: a text-labelled Done button on the docked clip panel at phone width (body.m-editing), wired to FM.selectLayer(null) exactly like #m-back (js/mobile.js:315), hidden at desktop width; reuse the #297 shared-header pattern (js/fx-browser.js:1326-1381, `fxb-subdone`) or add one row above #inspector. Test at 380px: with one layer selected, a visible control whose text i
- Actually produce the picture the entry claims was sent: a 380px probe page or PNG showing A/B/C (built-in preview: load, resize 380, screenshot), commit it under tests/ or tools/unblock/, and reference it from the #654 card in tools/unblock/unblock.html:437-448, whose B and C currently read only 'Option B as drawn' / 'Option C as drawn'.
- Do the entry's own ⚠️ browser/hardware Back clause: push a history state when m-editing (and sel-mode) opens and close the panel on popstate instead of leaving the PWA; test: enter m-editing at 380px, dispatch popstate / history.back(), assert body.m-editing is gone and the app is still on the project.
- Record the two unanswered questions from the entry's ➡️ list in the entry: scrim tap (the docked panel has no scrim) and back-swipe (none — #insp-grab is display:none in m-editing per styles.css:3555), so the diagnosis is complete rather than half-written.
- Log, without blocking, the question 'did your friend mean the clip panel or the audio effects browser?' — the audio FX browser (js/audio-fx-browser.js:341,344) already has a ✕ and scrim-tap exit, so the clip panel is the likelier one and A covers it.
- Strike the 'BUILT OUT UNTIL HE picks A, B or C' line and re-file the entry as ACTIONABLE (or DONE once A ships), and republish the unblock artifact with url= so the #654 card either shows the picture or disappears.
**Recommendation:** Unpark. The block is a design letter with a drawn-and-recommended option, which is exactly the case rule 16 says to decide: ship A (Done on the panel at phone width, wired to FM.selectLayer(null)), name B and C in the log, and send the 380px picture — which, on the evidence, still has to be made, because no drawing exists in the commit, the repo, or any scratchpad, and the unblock card cannot show

### #677 — needs him: partly · built out verified: yes
**Waits for:** A paste from his phone: export something with sound on the phone, then Settings → "Your last export" → Copy, and send the text (the entry says this report, trustworthy since v14.35, will name which of the six audio-loss paths fired; #215 and #604 are parked on the same paste).
**Evidence:** BUILT-OUT CLAIMS, checked in code: js/settings.js:474-509 renders 'Your last export' from localStorage fm.lastExportReport with a Copy button (clipboard + select fallback); js/exporter.js:1195-1238 writes that report after muxer.finalize (audio TRACK WRITTEN/NO TRACK, dropped, mix peak, drops, suppressed, AAC encode present/MISSING, resumed, protected, device) — only on the onReady branch, and js/
**Could be built or decided now:**
- FIX THE SEVENTH LOSS PATH the six do not cover (measured here, no paste needed): in js/exporter.js encodeAudio() (line 611-639) normalise meta.decoderConfig.description before it reaches mp4-muxer — if it is not a bare 2-5 byte AudioSpecificConfig (WebKit/iOS 26 hands back Apple's 39-byte AAC magic cookie, an ES_Descriptor starting 0x03), extract the tag-0x05 payload or synthesise the ASC from sam
- ADD A TEST that stands in for his phone: wrap AudioEncoder so the first chunk's meta.decoderConfig.description is the 39-byte cookie (bytes are in the scratch probe), run FM.exporter.run with two FM.sfx.add layers, and assert the exported bytes' esds DecoderSpecificInfo (tag 0x05) is 2 bytes / the track decodes. Today that scenario passes every existing test and reports 'audio TRACK WRITTEN / drop
- ADD ONE LINE TO THE REPORT (exporter.js:1195-1238): the hex of the first audio chunk's decoderConfig.description and its byte length. Without it the paste he is being asked for CANNOT distinguish this path — it would read TRACK WRITTEN / dropped no, i.e. 'healthy'. With it, a 39-byte value is the verdict in one glance.
- ADD 'all-unreadable' etc. is fine, but there is no 'muxed-undecodable' verdict: after finalize, run a cheap self-check (decodeAudioData on the finished blob, or at least parse the esds DSI length) and set FM._audioTrackDropped = 'track-undecodable' so the ready card (app.js:4726-4744 AUDIO_WHY) and the report stop saying Sound ✓ for a file Apple cannot open.
- PUT THE REPORT ON THE READY CARD: showExportReady (app.js:4700+) has no Copy-report button; the paste requires a trip to Settings. A 'Copy report' button on the card he is already looking at — enabled when hasAudio is false or the description was non-standard — collapses the ask to one tap. No answer from him needed.
- RECORD WHAT IS ALREADY KNOWN in the entry: REQUESTS.md:1435 says he is on iPhone 16 Pro Max, iOS 26.5.2, and MDN compat data says AudioEncoder arrived in Safari 26 — so 'aac-unavailable' is unlikely on his phone, and the WebKit AudioEncoderCocoa source (generateDecoderDescriptionFromSample returns CMAudioFormatDescriptionGetMagicCookie verbatim) points at the cookie path above. The entry's 'six lo
- The 'reproduced his exact setup' claim had no saved artifact (it entered REQUESTS.md in commit 2bb221a, which shipped #673 and touched no probe; tests/_604sfx.html is video+ONE sfx; no test in tests/tests.js calls sfx.add). Save the two-SFX probe under tests/ (scratch copy exists) so the claim is re-runnable.
**Recommendation:** Reclassify: the instrument is real and wired (built out = yes), but 'waiting on his paste' is not the honest state, because the paste as currently written cannot distinguish the most likely cause. Evidence assembled here without him (his iOS 26.5.2 from REQUESTS.md:1435, MDN: AudioEncoder new in Safari 26, WebKit AudioEncoderCocoa returning the raw CoreMedia magic cookie, Apple's AAC cookie measur

### #712 — needs him: no · built out verified: no
**Waits for:** A look on his phone: "open the effects browser fresh after a reload, scroll into Distort, tap Back — does the app stall for more than a moment?" (❓ASK at REQUESTS.md:28150-28152; tail line 28157: "Still BUILT OUT UNTIL HE says whether his phone stalls on that Back"; unblock card tools/unblock/unbloc
**Evidence:** REQUESTS.md:28131-28157 — entry text; :28149 'Nothing is built until he answers'; :28157 'BUILT OUT UNTIL HE says whether his phone stalls'. No numbered '1. [ ]' clauses exist in the entry at all (it is an audit finding, not his words); the only actionable line is '➡️ Fix shape' (:28152-28155), unbuilt. js/fx-thumbs.js:1036-1048 generate(): 2 probes + FRAMES=10 (:16) renderFrame calls in one synch
**Could be built or decided now:**
- Port the existing per-rAF time budget to stock tiles. js/fx-thumbs.js:1036-1048 generate() renders 2 probe frames + a 10-frame strip synchronously inside one pump() tick (1448-1483); the resumable slicing already exists 300 lines later for LAYER strips (SLICE_MS=8 at :1125, layerStep :1332-1379 keeps a job at the head of the queue and pump() paints its first frame early at :1471-1477). The entry i
- Stop paying for tiles nobody can see. Back (closeView, js/fx-browser.js:1400) only does view.remove(); it neither re-mounts the overview NOR drains the queue, so the 41 cold Distort keys queued at :1405 keep generating one-per-rAF after the view is gone (mountKey :1531-1534 queues; pump :1466 caches the result even when every pending canvas is detached). Skipping/deferring keys whose pending canva
- Build the phone instrument instead of asking — the exact precedent is #768 (REQUESTS.md:28795: 'THE INSTRUMENT IS BUILT … Settings → Your last scrub → Copy', js/timeline.js:3242-3268 writes fm.lastScrubReport, js/settings.js:606-627 renders it with a Copy button). A 'Your last Back from an effects category' row recording click-to-first-yield ms, queue length at Back, ms until the queue drained, an
- Make the '2 Sep measured in the pane (ANGLE Metal: 3ms / 65ms / 200ms)' claim reproducible: no probe file for it exists anywhere on disk (searched all ten scratchpad dirs; only hang333b.py, the SwiftShader probe, exists). tests/_cdp.py:61-65 already supports FM_GL=angle and tools/probe.sh runs a single tests/_*.html probe — a tests/_712back.html driven by `FM_GL=angle tools/probe.sh _712back.html`
- Once slicing lands, lower test 333's 240s budget and correct its comment (tests/tests.js:43407-43411 says each return 're-renders that category's thumbnails SYNCHRONOUSLY', which the code does not do), and reword #712's STATUS from '🟠 NEEDS YOU' to built/ready; drop or rewrite the unblock card at tools/unblock/unblock.html:514 to 'paste the report' and republish with url=.
**Recommendation:** Reclassify: not legitimately waiting on him. The entry's own text says the time-budget fix is worth doing regardless of his answer, the identical mechanism already exists in the same file for layer strips, and the only phone-only fact can be captured by an instrument exactly as #768 did — so 'BUILT OUT' is false (nothing at all was built) and the ❓ASK is a question standing in for work. Do now, in

### #663 — needs him: partly · built out verified: yes
**Waits for:** A paste from his iPhone: play a project until the sound cuts out, press stop, then Settings → "Your last playback" → Copy, and paste that block (the same paste #95 and #96 wait on). Tail line at REQUESTS.md:26324: "BUILT OUT UNTIL HE pastes 'Your last playback'". No such paste has ever arrived: grep
**Evidence:** js/audio-health.js exists (220 lines, last changed v14.33, commit 2080141) and is loaded by index.html:1018 (`js/audio-health.js?v=3`). Wired into the real transport: js/app.js:1886 calls FM.audioHealth.note(m, now, sounding) inside the playback tick; js/app.js:1781 calls noteRestart on the resume branch (`if (m.el.paused)` at 1739); js/app.js:2079 calls save() on stop; audio-health.js:215-219 als
**Could be built or decided now:**
- Close a real blind spot in the instrument that needs no answer from him: js/audio-health.js:118 skips stall judgement whenever el.paused is true, noteRestart only counts within 400ms of the element last being audible (line 150), and 'pause' is deliberately not in the TROUBLE list (line 60) — so an element that iOS pauses and then refuses to restart (play() rejected; both app.js:1782 and app.js:200
- Run the instrument end-to-end in a real browser at the phone width, which nobody has done: the Browser pane (real Chrome, runs rAF, mobile preset ~380px) with tests/_fixtures/control.mp3 as a clip — play, stop, open Settings, read 'Your last playback'. Every existing #663 test drives note() directly or uses a fake element; the entry's own 30 Aug line admits live attempts recorded 0.0s. Screenshot 
- Add a real-media-element suite test: the headless harness DOES run rAF (tests.js:46553 passes under _cdp.py), and tests/_fixtures/control.mp3 is already in the repo but is referenced 0 times in tests.js. Play it through FM.play for ~1s and assert the report says 'CUT OUT 0' and 'played' > 0; if headless Chrome's autoplay policy rejects play(), that is itself the play-rejected signal from item 1 an
- Correct the entry's stated reason for not proving it live ('the browser I develop in does not run animation frames at all') — the harness runs rAF; only the direct-drive claim is accurate. Also file the spotcheck limitation for pre-2-Sep commits (no ?only= at the commit → 300s cap; revert fails on files the commit created) as a tools/spotcheck.sh fix so v14.13's 'mutation-checked' claim becomes re
- Nothing else: the actual reading from HIS iPhone genuinely needs him — the symptom is mobile-only, has never reproduced on this Mac, and no iOS simulator runtime is installed (the download is a cost decision to put to him, not something to do silently).
**Recommendation:** Keep #663 in the "built out — waiting on him" bucket for the paste itself (that half is a legitimate physical-only ask: the reading has to come from his iPhone, and it has never arrived), but the entry is NOT fully built out. Two halves need no answer and should be done before it is re-filed as waiting: (1) the instrument's blind spot — a sound that iOS pauses and cannot restart is recorded as 'CU

### #760 — needs him: no · built out verified: partly
**Waits for:** The tail says: "BUILT OUT UNTIL HE picks — a size and a colour for the tiles, and a letter (or a correction) for the people." I.e. three design picks: tile size 40/48/56 (48 recommended), tile colour A–D (D recommended), and a people figure A–E (NO recommendation marked — A is only tagged "as shippe
**Evidence:** REQUESTS.md:28658-28677 — entry; clauses 1-3 all unticked; no mention of any prior attempt (grep for #160/#435/v3.49/v3.81/v6.03/v8.66/v10.75 in the entry: 0 hits), so 3(a) is undone. tests/_people760.html (tracked, 92 lines, last commit e3e49df v15.06 which is queue 553 — it rode in on an unrelated release): row A calls FM.traceShapePath(g,{shape:'person'|'woman'}) from ../index.html (lines 80-86
**Could be built or decided now:**
- Clause 1 (bigger PC shape tiles) under rule 16: build it in the real solver, not the mock — the PC Shape grid is planGrid() output (js/addmenu.js ~786-820) from the `ico` cfg at js/addmenu.js:717 (minW 34, minH 34, icoMin 34, icoMax 58). Measured live at 1280x800: data-am-fit="278x78 6c2r", tiles 39.7x35px, glyph 34px, panel 307px wide. Raise icoMin/minH (e.g. 44) or add a prefer-bigger-art object
- Clause 2 (colour) under rule 16: ship option D (white glyph on the per-index tinted tile) — a CSS change on `.addmenu-card--ico` colour (styles.css ~5059) keeping tintFor()'s plate/border (js/addmenu.js ~942-1000) — with A/B/C named as alternatives; test: the ico card's computed color is white while its border colour still carries the golden-angle hue.
- Clause 3(a) needs NO answer and is not done: write the plain history of every previous attempt and why it was not good enough — v3.49 (rebuilt), v3.81 (restroom pictogram), v6.03 (redraw), v8.66 (#160, arms), v10.75 (#435, shoulder flush with arms) — using compositor.js:11899-11908's own (a)(b)(c) record and REQUESTS.md #160 (line 7058) and #435 (line 16831).
- Clause 3(b)/(c): the candidates B–E in tests/_people760.html are Object.assign(AS_SHIPPED, {tiny deltas}) through one drawFigure builder and are near-identical to A in people760.png; draw genuinely different figures (different construction, not 0.004-0.02H proportion nudges), mark ONE recommended, re-send — then under rule 16 ship the recommended one with the letters as alternatives.
- Add the people pick to his unblock page: tools/unblock/unblock.html has cards 760a (size) and 760b (colour) only — no 760c for the people — so the one channel he uses to answer cannot return the letter the entry is waiting on. Add it and republish with url=.
- A test that can stand in for his eye: an `.addmenu--panel` Shape-tab test at 1280x800 asserting the fit's --am-ico ≥ the chosen size, plus tests/_pictolegible.html (exists, from #435) run on the chosen people figure at 24/48/220px.
**Recommendation:** Reclassify from "built out — waiting on him" to READY and work it under rule 16: none of the three picks is something only he can supply, and the "built out" claim is only pictures — no code changed, the size mock misrepresents what ships (the real PC grid is height-limited to 35px tiles and the recommended 48px does not fit the measured box), the people "options" are five near-copies of the figur

### #765 — needs him: no · built out verified: partly
**Waits for:** The tail line reads: "⏸ BUILT OUT UNTIL HE picks a look for the three lettered buttons (1 / 2 / 3 in the chat, #545)" and the STATUS line says "the three buttons wait for your pick — 1 / 2 / 3 in the chat". I.e. a design-letter pick from the 3 Sep sheet scratchpad/split765.html (1 keycaps, 2 quiet t
**Evidence:** KEYBOARD HALF (clauses 3, 4, key half of 2) — real and proven: js/timeline.js:67 clipToolSide(); :140-153 clipKeyAction(k) — side 0: a→clipTrimStart, d→clipTrimEnd, s→clipSplit; else s→clipExtendToPlayhead, a with side<0 / d with side>0 → clipMoveToPlayhead, other letter returns false; :4616 exported as FM.timeline.clipKey; :4649-4651 the #tl-trim buttons call the same bodies. js/app.js:6606-6611 
**Could be built or decided now:**
- Build #772 option A 'Keycap Rail' (marked recommended on its own sheet at split772.html:415 and in unblock.html:404): a vertical column of three keycap buttons on the seam between #inspector-panel and the timeline on PC (>=701px), each carrying its letter A / S / D; wire them to the EXISTING one-body functions clipTrimStart / clipSplit / clipTrimEnd (playhead inside) and clipMoveToPlayhead / clipE
- Clause 5 ('dont leave the old buttons near the playhead coz they annoying asf'): delete #tl-trim / #tl-nudge from the desktop — index.html:568-576, styles.css:1987-2014 and 2032-2055, syncTrim/syncNudge in timeline.js — once the column exists. Nothing about this depends on which look he picks. The phone is untouched: styles.css:2014/2055 already hide both groups <=700px and the phone uses the insp
- Ship it under rule 16 (LOOP.md:116-120) and send him the picture with B 'Seam Tabs' and C 'Docked Key Rail' named so he can change it in one word.
- Strike the stale '⏸ BUILT OUT UNTIL HE picks a look … 1 / 2 / 3' line and the STATUS line in #765 (they refer to the superseded add-row sheet), fix the false sentence 'The phone keeps its playhead buttons either way' (styles.css:2055 — the phone never had them), and remove the #765 card from tools/unblock/unblock.html:411-424 (or fold it into #772's card), then republish the unblock artifact with 
- Test: add a wide-width test (like tests/tests.js:53617, which already exercises the nine inside/left/right cases via the keys) that clicks each of the three seam buttons and asserts the same nine outcomes, plus asserts the buttons carry the letters A/S/D and that #tl-trim / #tl-nudge are gone on desktop. tools/mutate.sh against 'the column not built' and 'letters swapped'.
- Leave the logged ❓ about A/D being inert on the wrong side as is — it is already decided and shipped as inert (timeline.js:150-151, test control cases at tests.js:53658/53664); it is not blocking and needs no answer.
**Recommendation:** Reclassify #765 from 'built out — waiting on him' to ACTIONABLE. The only thing it waits for is a look-pick with options drawn and one recommended on both sheets, which rule 16 (LOOP.md:116) says to decide, not park — and the specific pick it asks for (1/2/3 from the add-row sheet) was superseded by #772 the same day, so the ask is not just parkable but dead. Build #772 option A (Keycap Rail) on t

### #763 — needs him: no · built out verified: partly
**Waits for:** Tail line (REQUESTS.md:28711): "BUILT OUT UNTIL HE picks a gap and a letter, or says 'none' — nothing visual ships before that." I.e. clause 1 waits for him to choose 6px / 12px (recommended) / 18px for the skip-to-pill gap on PC; clause 2 waits for him to choose play-button design A (as shipped, re
**Evidence:** ENTRY: REQUESTS.md:28698-28711 — header line 28699 'STATUS: 🟢 READY — nothing is stopping this'; clauses 1 and 2 both '[ ]' (28703, 28706); tail 28711 '🎨 3 Sep — BOTH CLAUSES DRAWN AND SENT … BUILT OUT UNTIL HE picks a gap and a letter'. tools/next.sh lists it under '⏸ BUILT OUT — waiting on him' (34 items). SHEET EXISTS AND WAS SENT: /private/tmp/claude-501/-Users-ezrasmith-Claude-FreeMotion/af45
**Could be built or decided now:**
- Clause 1 under rule 16: ship the recommended gap on PC — the drawn '12px' option is `+2px` margins on #btn-tostart / #btn-toend (play763.html `.gap12`), replacing the `-4px` from queue 420 (styles.css:2577-2578). Scope it to the Studio/PC layout width (`@media (min-width: 700px)`, the threshold app.js:4789 names) so the 440px phone spacing and the 'skip-arrow-gaps' test (tests/tests.js:45133-45165
- Write the 1280px counterpart of 'skip-arrow-gaps' that fails without the CSS: measured today at 1280 the skip SVG edge sits 2.5px from the pill (box gap -4.0px) against ~12.5px at 440 — assert the desktop glyph-to-pill distance is >= the phone's, or >= 8px. tools/_rowgaps.html?w=1280 already measures the row and can seed the numbers; ship.sh's prove gate needs a test that fails without the fix.
- Clause 2 under rule 16: the recommended letter is A = as shipped, so the decision is 'A stands'. Tick clause 2 as 'drawn, sent 3 Sep (play763.png), A stands by default — say B/C/D to change it' instead of holding the entry open on it. No code needed.
- Show him the picture again with the alternatives named in one line (6/18 for the gap, B/C/D for the pill) so he can change either in one word — that is rule 16's whole mechanism.
- Fix the unblock-list card (tools/unblock/unblock.html:359-370, #777): its title says 'The play button on a draft card' (wrong — it is the transport-row timecode pill, not a draft card), it offers only the four letters with NO gap choice even though the entry says he must pick a gap, it carries no 'recommended' marker (the #760 cards do) and no 'None of them' option although the entry says 'he may 
- Fix the entry's own contradiction: line 28699 still says 'STATUS: 🟢 READY — nothing is stopping this' while the tail says BUILT OUT (the classifier files it by the tail, so it is hidden from work). Also correct the attribution: the -4px margins are queue 420 (styles.css:2563-2578), not queue 526 as the sheet and entry claim.
- Verify at 380px that nothing moves (the margin rule is already guarded at 400px, so a >=700px scope cannot touch the phone) — screenshot per the standing mobile rule.
**Recommendation:** Not a legitimate block. Both halves have drawn options with a recommendation, and rule 16 (LOOP.md:116-120) explicitly overrides #545's 'show him options first': pick the recommended option, ship it, show the picture with the alternatives named. Nothing here can only he supply. Do now: (1) ship clause 1 as the drawn 12px option — +2px skip margins scoped to the Studio/PC width (>=700px), leaving t

### #768 — needs him: partly · built out verified: yes
**Waits for:** Entry tail (REQUESTS.md:28795): "BUILT OUT UNTIL HE scrubs once on his phone with a layer selected and pastes that report" — the text under Settings → Your last scrub → Copy, written to localStorage key fm.lastScrubReport by the on-device scrub frame probe. No paste has arrived: grep for "FreeMotion
**Evidence:** js/timeline.js:3242-3268 — scrubProbe IIFE (begin/settle/cancel/finish; finish writes an 8-line report incl. device, selection, panel, layers/effects, frames, median/worst gap, long frames >33ms with times, verdict) to localStorage fm.lastScrubReport; exposed as FM._scrubProbe at :3268. js/timeline.js:3269-3273 — beginScrub() calls scrubProbe.begin with selected type (+ "with effects"), FM.inspect
**Could be built or decided now:**
- Run the real scrub gesture (the 768 test's pointer sequence) under CPU throttling 4x and 6x using the existing tools/_phoneprobe.py pattern (Emulation.setCPUThrottlingRate, tools/_phoneprobe.py:355), in four states: nothing selected / shape selected / Position-Scale open / a KEYFRAMED (animated) layer selected. The keyframed case was named as 'next probe' in the entry on 2 Sep and never run. A sta
- Profile what a selection adds per scrub frame (CDP Profiler under 6x throttle) across the three suspects the entry itself named on 2 Sep and never profiled — inspector live-value refresh, selection overlay/handles redraw on the canvas, keyframe dots on the clip — then coalesce the inspector refresh to one per rAF and skip it when the panel is not visible. Recommended option: throttle + coalesce; a
- Fix the evidence-loss hole in the instrument: fm.lastScrubReport is overwritten by EVERY horizontal scrub (js/timeline.js:3264, settle at :4370), so one smooth scrub after the jumpy one wipes the report before he reaches Settings. Keep a second key for the worst-verdict scrub of the session ('Your last jumpy scrub') and show both in settings.js. Test: two scrubs via FM._scrubProbe seam, assert the
- Add an on-device nudge: when finish() computes a non-'smooth here' verdict, toast once 'That scrub was jumpy — report saved in Settings → Your last scrub → Copy', so the ask does not depend on him remembering the Settings row exists. Test: feed a synthetic frame list with a >33ms gap through an exposed finish()/record seam and assert the toast.
- Close the test gap: tests.js:53574 asserts only the report's SHAPE (regexes for headings) — nothing tests the verdict thresholds (worst<=34 → smooth; long<=2 → 'a couple of hitches'; else jumpy) or that a long frame is listed with its offset. Expose finish() on FM._scrubProbe taking a timestamp array and assert the three verdicts and the long-frame listing.
- Republish the unblock page (tools/unblock/unblock.html:249 already lists #768) only if the wording changes after the throttled probe; no republish needed for the code items above.
**Recommendation:** The built-out claim is TRUE and PROVEN (code wired end to end, real-gesture test, spotcheck at 380px fails without the fix). But the block is only half legitimate: the conclusion 'not reproducible on this Mac' was drawn from three unthrottled probes while tools/_phoneprobe.py — a CPU-throttled phone-speed harness built for an earlier lag entry — sat unused in the same repo, and the keyframed-layer
