{
 "clausesClaimedMissing": 20,
 "confirmedMissing": [
  {
   "tag": "141-export-custom-fps",
   "whatToBuild": "Add a Custom rung to the export frame-rate list, mirroring the canvas dialog exactly (no new pattern to invent):\n1. index.html \u2014 append <option value=\"custom\">Custom...</option> as the LAST option of #exp-fps, and add a hidden row after it modelled on #exp-custom-field / #cv-custom-fps: <label class=\"field hidden\" id=\"exp-custom-fps\"><span>Custom fps</span><input type=\"number\" id=\"exp-fps-num\" min=\"1\" max=\"120\" step=\"1\" inputmode=\"numeric\" aria-label=\"Custom export frame rate, 1 to 120\"></label>. While there, drop the stray selected from <option value=\"30\"> so \"Same as project\" is genuinely the default (see the adjacent finding).\n2. js/app.js:3535-3542 (showExportDialogNow) \u2014 wire a syncCustomFps() exactly like the resolution one at 3562-3573: toggle the row on sel.value === 'custom', seed the input from FM.scene.project.fps the first time, and guard the listener with a _customWired flag so repeated opens do not stack handlers.\n3. js/app.js:3806-3808 \u2014 add the third branch: fpsRaw === 'custom' -> clamp(Math.round(parseFloat(#exp-fps-num.value)), 1, 120) with the project's fps as the fallback, same shape as clampDim above it. The bitrate line at 3814 already multiplies by fps, so it follows automatically.\n4. js/app.js:3650 \u2014 add 'exp-custom-fps' to the audio-only hide list alongside 'exp-custom-field', and to the matching restore branch at 3655 so it stays hidden when you switch back to MP4 (that branch already special-cases the rows that must not be un-hidden).\n5. Do NOT persist it: expPrefsSave/expPrefsApply (js/app.js:3483-3499) deliberately exclude fps because #121 makes the cog the source of truth \u2014 a custom export rate is a one-off override.\n6. Tests \u2014 extend tests/tests.js near 19502 (\"export: a project on a custom frame rate can actually be exported at it\") with a case that picks Custom, types 24, and asserts the value reaching the exporter is 24; and re-check the part-3 alignment test (the five controls share one left edge) still passes with the extra revealed row.\n\nNo decision from Ezra is needed \u2014 the shape, the clamp (1-120), the seeding and the not-remembering are all fixed by existing precedent in the canvas and new-project dialogs and by #121. Bump the version label, the ?v= busters for index.html/js/app.js, and add a POLISH-LOG entry, then ship via tools/ship.sh.",
   "needsEzra": false,
   "reason": "CONFIRMED. There is no way to type an arbitrary frame rate on the export dialog. #exp-fps (index.html:745-762) is a static seven-option select \u2014 project/15/25/30/50/60/120 \u2014 and js/app.js:3535-3542 only rewrites the \"project\" option's LABEL and auto-selects it when the project's rate is off-ladder; it never appends a rung. The \"custom\" rung exists for resolution only (js/app.js:3555-3572 -> add('custom','Custom size...') -> #exp-custom-field, index.html:738-744), and js/app.js:3806-3808 has just two fps branches ('project' -> P.fps, else parseFloat). `grep -no 'id=\"[^\"]*fps[^\"]*\"' index.html` returns every fps control in the app: hm-new-fps/hm-new-custom-fps/hm-new-fps-num (new project), cv-fps/cv-custom-fps/cv-fps-num (canvas settings), exp-fps (export) \u2014 export is the only one of the three with no Custom sibling. Nothing dynamic adds one: no hits for customFps/custom-fps in js/*.js beyond home.js:2034 (new-project row) and app.js:5228 (canvas row).\n\nI tried hard to refute it and could not: POLISH-LOG v7.09 (line 481) describes only custom SIZE, v7.08 (line 480) only \"Same as project\"; no later entry builds it (#172 is export resolution \"Same as project\", #216 only hides the fps control for audio-only, #121 makes the cog the source of truth for fps but does not forbid a one-off export override \u2014 export still offers other rungs and a custom SIZE). #118 and #119+120 are OLDER than #141 and are where Claude wrote \"Custom is not offered on the export dialog, so say so rather than let it go quietly\" \u2014 #141 is Ezra reacting to that gap, not declining it. The entry records no change of mind, and the clause is not filler: part 1's own text is \"Needs custom fps AND a custom aspect/resolution\" (REQUESTS.md:379) while the shipped note under it only delivers \"Custom size...\". The only route to an arbitrary export rate today is to change the PROJECT's fps in the cog (1-120) and let export inherit it, which is a project-level edit you then have to undo \u2014 and since #118 dropped 24 and 12 from every list, a 24fps film-delivery export is unreachable from the export dialog at all.\n\nAdjacent finding while checking part 2: #exp-fps has TWO selected attributes (project at index.html:755, 30 at index.html:758). Last-wins, and showExportDialogNow only forces 'project' when the rate is OFF the ladder, and expPrefsApply deliberately skips fps \u2014 so on a fresh load a 25/50/60/15/120 fps project opens the export dialog on the 30 fps rung and would silently export at 30. The #121 test (tests/tests.js:19628) only exercises the off-ladder case (48), so nothing catches it. Worth its own item."
  },
  {
   "tag": "142-default-shape-colour-misses-freehand-and-vector",
   "whatToBuild": "Seed the draw tool from the setting instead of the literal, so the preview and the result both honour it.\n\nIn js/draw-tool.js FM.startDraw (before the `opts.layerId` adopt block at :693): if the stored setting is a real colour \u2014 read `FM.settings.get('shapeColor')` and accept it only on /^#[0-9a-f]{6}$/i, do NOT call FM.defaultShapeFill(), which rolls a fresh random hue when the setting is 'random' \u2014 set FM.drawTool.color to it and write the same value into the bar's `.db-color input` so the swatch, the live overlay stroke (js/draw-tool.js:77/:84) and the committed layer all agree. When the setting is 'random', leave white: 'random' is documented as \"what the app has always done\", and white is what the draw tool has always done, so that branch should not change behaviour.\n\nTwo ordering rules to keep: the adopt branch at :709 must still win (re-editing an existing drawing keeps that drawing's colour), and a colour the user picks in the bar during the session must survive the tool being reopened \u2014 track a \"user touched the swatch\" flag set in the `.db-color input` listener at :574 and skip the seed when it is set. That is the plain meaning of \"default\": it supplies the starting colour, a manual pick still sticks.\n\nOptionally also change the dead fallbacks at js/app.js:2093/:2096 from '#3a7bd5'/'#ffffff' to FM.defaultShapeFill() so a future caller that passes no colour lands on the preference rather than a literal. No current caller hits them, so this is belt-and-braces, not the fix.\n\nTest: the existing test at tests/tests.js:18902 never leaves makeLayer. Add a case that sets shapeColor to a known hex, runs FM.startDraw('vector') then the real finish path (tests.js:20005 already drives FM.drawTool.points + finish()), and asserts the created layer's fill \u2014 plus the freehand equivalent via FM.drawTool._commit. Assert too that with shapeColor 'random' the stroke is still white, so the preserved behaviour is guarded. Then correct the overclaiming sentences in REQUESTS.md entry 142 and POLISH-LOG.md:465 rather than leaving them contradicting the code.",
   "needsEzra": false,
   "reason": "Confirmed. The setting reaches only shapes born through FM.makeLayer without an explicit fill \u2014 i.e. the add menu (js/app.js:2028, FM.addShapeLayer) and fx thumbnails. It never reaches freehand or vector drawing, which entry 142 lists in scope (\"and the freehand/vector paths, which also create fillable layers\") and whose shipped note claims outright: \"It reaches every route that spawns a shape, including freehand and vector drawing.\"\n\nExact break: js/app.js:2084 FM.addPathLayer calls makeLayer (so scene.js:539 `base.fill = props.fill || FM.defaultShapeFill()` does assign the preference), then throws it away two lines later \u2014 :2093 `layer.fill = opt.fill || '#3a7bd5'` for closed paths, :2096 `layer.fill = opt.color || '#ffffff'` for open ones. And both callers always supply a colour, so those fallbacks are not even reached: js/draw-tool.js:491 passes `fill: t.color` (vector) and :209/:347 pass `color: t.color` (freehand), where `t` is FM.drawTool, initialised at js/draw-tool.js:13 with the literal `color: '#ffffff'` and never seeded from the setting. FM.startDraw (js/draw-tool.js:663) resets the whole tool on every open and touches `color` only in the adopt-an-existing-layer branch (:709). The bar's swatch is likewise hardcoded, `<input type=\"color\" value=\"#ffffff\">` at :559.\n\nSo with Default shape colour set to red: add-menu shapes come out red, but open the vector tool and the swatch reads white, the live preview strokes white, and the finished closed shape is white. Same for freehand.\n\nSearched for it under other names and elsewhere: `grep -rn defaultShapeFill js/ tests/ index.html` returns only scene.js:197 (definition) and scene.js:539 (sole reader); `grep -rn shapeColor` returns only settings.js and scene.js \u2014 draw-tool.js appears in neither. No later REQUESTS.md entry or POLISH-LOG.md release revisits it; POLISH-LOG.md:465 (v6.92) repeats the same false coverage claim. Entry 142 records no change of mind, decline or hold. The clause is a scope instruction (\"Applied to every shape\"), not filler. The guarding test (tests/tests.js:18902) calls FM.makeLayer directly and never goes through addPathLayer, which is exactly why this passed unnoticed.\n\nThe \"it arrived with a fill, so the exemption covers it\" defence fails: the exemption in the entry is for colours somebody designed (saved project, template, element, AI layer, duplicate). The draw bar's white is a hardcoded literal the user never picked, and it beats the preference on a fresh session."
  },
  {
   "tag": "118",
   "whatToBuild": "In js/app.js, delete the hardcoded FPS_PRESETS (line 5229) and decide the preset-vs-Custom branch by scanning the live control instead, exactly as js/home.js:2059-2064 does: iterate fpsSel.options, and if the project's fps matches an option value use that row, otherwise fall to 'custom' with fpsNum seeded to the project's fps and the custom row unhidden. That makes a 24 fps project open on \"Custom... 24\" (which is what entry 118 itself promises) instead of a blank select that Apply turns into 30, and makes 15/120 open on their own rows. The list can then never drift from index.html again. Add the missing test coverage in tests/tests.js (nothing there mentions cv-fps today): open the Canvas dialog with the project at 24, 15, 120, 25 and 30, assert the select is never left at selectedIndex -1, and assert pressing Apply round-trips the rate unchanged \u2014 24 must stay 24.",
   "needsEzra": false,
   "reason": "Confirmed. Entry 118 (REQUESTS.md:877-887, marked [x] v6.74) updated the frame-rate dropdowns (index.html:820-828 = 15/25/30/50/60/120/custom, comment at index.html:616 quoting him) and the AI fps snapper (js/ai-ops.js:145 uses [15,25,30,50,60,120] with a queue-118 comment), but left the Canvas dialog's mirror list stale: js/app.js:5229 still reads `const FPS_PRESETS = ['24','25','30','50','60']`. git log -S\"const FPS_PRESETS\" shows it was added at v3.28 (4a6d722, 15 Jul) and never touched by the v6.74 commit. `grep -n FPS_PRESETS js/*.js` returns only lines 5229 and 5238 \u2014 no helper, no other mirror, and no later entry in REQUESTS.md or POLISH-LOG.md fixes it. The entry records no change of mind and no Held note; it explicitly promises \"Custom still reaches 24 either way\", so 24 fps projects are a supported state.\n\nI verified the consequence rather than assuming it \u2014 ran the exact branch from app.js against a real <select> carrying the shipped options: project fps 24 takes the preset branch, sets a value that has no option, so selectedIndex becomes -1 and the select renders BLANK; Apply (js/app.js:5319-5320) then reads '' -> parseInt('')||30 and silently rewrites the project from 24 fps to 30. Measured: {24 -> blank, selectedIndex -1, applied 30}. 15 and 120 open on \"Custom...\" instead of their own preset row but do round-trip correctly (cosmetic only). 25/30 are fine. 24 is reachable via any pre-v6.74 project and via the Custom box (cv-fps-num is min 1 max 120); nothing migrates it. `grep -rn \"cv-fps\" tests/` returns NOTHING, so no test would ever catch it. js/home.js:2059-2064 already does this job correctly by scanning fsel.options."
  },
  {
   "tag": "121",
   "whatToBuild": "In js/app.js showExportDialogNow():\n1. Always reset the frame rate to the project on every open - drop the `onLadder` guard at js/app.js:3535-3540 and just set `fpsSel.value = 'project'`. The \"project\" option is labelled with the real rate (\"Same as project - 25 fps\"), so it reads correctly and resolves to P.fps at export time (js/app.js:3806-3809). This still passes #141's existing test, which accepts 'project' as long as the label names the project's rate.\n2. Delete the resolution restore - remove `const prev = sel.value;` (js/app.js:3545) and the `if (prev && ...) sel.value = prev;` line (js/app.js:3561). The list is rebuilt with \"Same as project\" first, so it defaults there naturally. Note this also resets a \"Custom size...\" pick, which is correct under the entry's own \"an export-time change is a one-off\" rule; syncCustom() already re-hides the width/height field when the value is not 'custom'.\n3. Fix the markup lie: remove `selected` from `<option value=\"30\">` at index.html:757 so the fps select's static default is genuinely \"Same as project\". (Belt and braces once step 1 lands, but the file currently contradicts its own comment.)\n4. Extend the #121 test at tests/tests.js:19628, which is the reason this went unnoticed. Add (a) the ON-ladder fps case - project 25, pick 60, reopen after the cog moves to 50, assert 'project'; (b) a resolution case - pick 720p, reopen, assert the value is '1'; (c) a fresh-markup assertion that exactly one option in #exp-fps carries defaultSelected. Mutation-check each with tools/mutate.sh, since the existing test stayed green through all three defects.\n\nThen bump the version label, the js/app.js and index.html ?v= busters, add a POLISH-LOG entry, update entry #121 in REQUESTS.md to record that v7.11 only did the localStorage half, and ship with tools/ship.sh.",
   "needsEzra": false,
   "reason": "CONFIRMED, and reproduced in a real browser rather than read off the source. Entry #121's own rule is that fps and resolution \"belong to the cog and are inherited fresh every time (an export change is a one-off)\", and js/app.js:3494-3499 states the same in a comment (\"Deliberately NOT restoring fps or resolution... They come from the project every time\"). Both are false in the shipped build.\n\nMeasured (probe run headless via tests/_probe.py against the live tree):\n1. FPS, within a session: project 25fps, open Export, pick 60, close, cog sets project to 50, reopen -> the dialog still reads \"60\". The reset at js/app.js:3540 is guarded by `if (!onLadder)`, so any project rate that IS one of the rungs (15/25/30/50/60/120 - i.e. almost every real project) leaves the stale export value untouched. The off-ladder case (48fps) correctly resets, and that is the only case tests/tests.js:19628 exercises.\n2. Resolution: project 1080x1920, pick 720p, close, reopen (project unchanged) -> still 720p. js/app.js:3545 `const prev = sel.value;` and :3561 `if (prev && ...) sel.value = prev;` deliberately re-apply it. Worse, the stored value is a SCALE: after the cog resizes the canvas to 2160x3840 the same 0.6667 re-applies as \"1440p\", so the export silently follows an old ratio into a project it was never chosen for.\n3. A third defect the auditor did not name, and the worst because it needs no prior pick: index.html carries `selected` on BOTH `<option value=\"project\">` (line 754) and `<option value=\"30\">` (line 757). Last-one-wins, so on a fresh page load exp-fps boots on the hardcoded 30 rung. Measured: a 25fps project opens Export reading \"30 fps\" while the project option beside it reads \"Same as project - 25 fps\". A 120fps project would export at 30. The markup's own comment claims \"Same as project is FIRST and is the default\" - it is not.\n\nRefutation attempts, all failed: grepped every reference to exp-fps/exp-res across js/, index.html, tests/, styles.css, tools/ - nothing else touches them, no shared helper, no alternate name. hideExportDialog (js/app.js:3627) does not reset them and js/settings.js never notifies the dialog. Searched REQUESTS.md and POLISH-LOG.md for a reversal or a competing \"remember my export fps/res\" request - #141, #172, #173 and #174 all reinforce the inherit rule and none reinstates memory for fps/res; the entry records no change of mind from Ezra. git log -S settles it: the `prev` restore predates the fix (cb76652, v2.74) and the v7.11 commit (4050f12) only stripped `fps`/`short` out of the localStorage payload, never touching the in-DOM persistence.\n\nThe other half of the entry - \"if you change a setting in the export menu it shouldn't change the cog menu\" - IS correctly built; the probe confirmed the project is untouched after setting export fps and resolution. So this is half-shipped, not unshipped."
  },
  {
   "tag": "285",
   "whatToBuild": "Make the panel fit all the way down to the 150px drag floor, then extend the queue-285 test to sweep 150-240 (it currently stops at 240, which is why this survived). Three ways, and they trade off differently: (1) reflow the grid to 4 columns below ~200px so 8-9 tiles need 2 rows instead of 3 -- keeps the 40px tap target, changes the look; note the styles.css:7110 warning that .cat-grid must stay flex-wrap because .cat-span2/3 set grid-column and would come back to life under a real grid, so do it by narrowing the flex basis, not by switching display; (2) drop the label below ~200px and go icon-only, which frees ~11px a row; (3) lower the 40px floor to ~33px, which fits but puts the tiles under the 36px touch target the suite elsewhere enforces. A fourth option avoids the layout question entirely: raise the clampH floor in js/app.js:3990 from 150 to 200 so the band cannot enter the range where the buttons do not fit.",
   "needsEzra": true,
   "reason": "Confirmed by measurement, but narrower than the auditor stated, and it was disclosed rather than dropped. The shrinking itself is fully built (styles.css:7125 sizes --cat-h, --cat-pad, --cat-ico and the gaps off --tl-h). I probed the real app at 1280x900 in Studio with a layer selected and swept the band: zero scroll and zero spill at 300/264/240/220; at 200 the tile hits its 40px floor and the panel needs 6px of scroll with nothing spilling; from 190 down, 2 of the 8 cards hang below the panel bottom (16px of scroll at 190, 26 at 180, 36 at 170, 46 at 160, 56 at 150). FM.clampTimelineH(50) returns 150, so that band is reachable with an ordinary drag on #tl-resizer. So across the bottom ~40px of a 150-648 drag range, the buttons do not all stay on screen -- the exact symptom he reported. Refutation attempts all failed: the only sizing path is that one clamp (no helper, no alternate file); .cat-grid (styles.css:747/828/3696, js/inspector.js:2901) is a fixed 3-column flex-wrap with no reflow at short bands; nothing in REQUESTS.md or POLISH-LOG.md after v8.77 revisits it, and tools/next.sh shows no open item for it; the entry records Claude offering to go further (\"Say the word if you would rather it kept going\") but no answer from Ezra, and it was ticked DONE instead of leaving a Held note. The existing test (tests/tests.js:24414) only checks 240 and 420, which is why the gap is invisible to the suite. Mitigating: the limit is written into the entry (REQUESTS.md:1866-1869) and into POLISH-LOG v8.77, which he reads, and the panel does scroll so the buttons stay reachable -- just not all visible at once."
  },
  {
   "tag": "257",
   "whatToBuild": "Give 257's white gradient ring its own slot so it survives on desktop and 286 keeps working \u2014 the resolution the repo already used for the 339/286 collision.\n\n1. Move the 257 ring from `.addmenu-card--rainbow::after` to `.addmenu-card--rainbow::before` (styles.css:7274-7286), keeping the same gradient, padding:1px and xor mask, with a lower z-index than 286's ring. `::before` is free on this card \u2014 only `.addmenu-card--multi::before` (styles.css:1055) uses it, and I measured `content: none` on the Sound effects card in the panel. `::after` then stays entirely 286's, so the card gets a white gradient edge at rest AND still lights toward the cursor like its neighbours, on both layouts.\n2. Take the `inset: -1px` lesson from v9.88 with it (REQUESTS.md:11668): the card carries a transparent 1px border, so `inset: 0` draws against the padding box and traces one pixel inside the visible outline. 286's own ring has the same issue but that is not this fix.\n3. Fix the false comment at styles.css:7096 \u2014 it asserts there is no collision on these two selectors, which is what let this sit.\n4. Widen the test at tests/tests.js:22232 to run at desktop width as well as 375, and make it assert the ring is the WHITE linear gradient (e.g. stops are near-255 and it is not the cyan radial) rather than merely /gradient/ \u2014 as written it passes on 286's ring, so it would not have caught this and would not catch it again. Mutation-check by deleting the specificity fix; it must go red at desktop.\n\nNo decision from Ezra needed \u2014 his words already say what the edge should be, and the alternative on the table (leaving the one card in the panel with no outline while both neighbours have one) is not something he asked for. Verify with a screenshot at 1280x800 on the Audio tab, cursor away from the panel and then beside the card, plus the usual 380px pass to prove the phone is unchanged.",
   "needsEzra": false,
   "reason": "CONFIRMED, and I reproduced it myself rather than taking the auditor's word \u2014 though the auditor's framing (\"never built\") is wrong in an important way: it WAS built in v8.36 and it is still correct on the phone. A later release broke it on PC.\n\nWHAT I MEASURED (live, http://localhost:8777, glass theme, real editor open, not the home overlay):\n- Desktop 1280x800, add menu in #inspector-panel on the Audio tab, the three cards read:\n  \u00b7 \"Import audio\"   border rgba(150,160,176,0.28)  \u2014 visible edge\n  \u00b7 \"Record voice\u2026\"  border rgba(235,70,70,0.28)    \u2014 visible edge\n  \u00b7 \"Sound effects\"  border rgba(0,0,0,0)           \u2014 NO edge at all\n  and its ::after is opacity 0 with backgroundImage `radial-gradient(220px at -9999px -9999px, rgba(150,232,255,0.85)\u2026)`, background-attachment fixed \u2014 i.e. queue 286's cursor ring, not 257's white one.\n- Phone 380x800, same card in .addmenu--sheet: ::after opacity 1, `linear-gradient(115deg, rgba(255,255,255,0.92) 0%, \u2026)` \u2014 257's ring, intact.\n\nWHY IT COLLIDES: styles.css:7275 `.addmenu-card--rainbow::after` is (0,1,1); styles.css:7082 `#inspector-panel .addmenu-card::after` is (1,1,1) and wins regardless of source order. js/addmenu.js:912 puts the class on a `.addmenu-card`; index.html:389/401 nest `#inspector` inside `<aside id=\"inspector-panel\">` and js/inspector.js:5609 renders the add menu there, so the two always meet on desktop. styles.css:7096 states the opposite in a comment (\"there is no collision. Checked rather than assumed\") \u2014 that comment is wrong; it only checked .fxb-tile.\n\nWHY IT'S WORSE THAN \"a different ring\": theme-glass.css:207 deliberately sets `border-color: transparent` on the rainbow card BECAUSE the 257 masked ring was supposed to supply the edge. With that ring overridden, the Sound effects card is the only card in the desktop panel with no outline whatsoever \u2014 its two neighbours have visible tinted borders.\n\nTHINGS I TRIED THAT DID NOT REFUTE IT:\n- No other rule anywhere restores it: the only `--rainbow::after` in styles.css/theme-glass.css is line 7275; nothing scopes it to #inspector-panel; ::before is untouched on this card (only `.addmenu-card--multi::before` exists, styles.css:1055).\n- No later entry or release covers it. #267 (v8.57) is the ICON half only. #291 (v8.82) is the sound-effects SHEET's travelling glow, not the card. #290, #304, #311 are the menu contents. POLISH-LOG has nothing after v8.36 about this border.\n- He never withdrew or held it, and it is not filler \u2014 \"the border colour around the rainbow should be white too, not solid white, give it some gradient\" is the second of the entry's two clauses.\n- Not out of scope on PC: #286 is explicitly \"PC: a glow that follows the cursor on the add menu\", so he uses that panel on desktop.\n- The test at tests/tests.js:22232 cannot catch it twice over: it runs only inside `atPhoneWidth(\u2026, 375)`, and even at desktop width it would pass, because it asserts only that ::after's background matches /gradient/ and its mask matches /gradient/ \u2014 the cyan `radial-gradient` cursor ring satisfies both.\n\nCHRONOLOGY, which is the honest version: 257 shipped v8.36, 286 shipped v8.78. This is a silent regression introduced by 286, of exactly the class the repo already recorded once \u2014 REQUESTS.md:11655, where queue 339's ring overwrote 286 on `.cat-card::after` and the note concludes \"a pseudo-element is one slot \u2014 'add a glow' twice on one element is a collision, not a layering\". Same trap, opposite direction, and this time nothing noticed because 257's test is pinned to the phone."
  },
  {
   "tag": "154",
   "whatToBuild": "Three pieces, in this order.\n\nA. FIX THE FILE FIRST (2 minutes, unblocks everything). Give the orphan body its own header at REQUESTS.md:4961 \u2014 `- [ ] **153 \u2014 The trim drag should show the numbers and the frame notches, like Alight Motion.**` \u2014 and leave it UNTICKED so tools/next.sh can reach it. Do not touch #154's own (genuinely done) content above line 4960.\n\nB. THE STRUCTURAL GUARD (his standing rule: remove the possibility, don't write a note). Add to tools/next.sh, alongside its three existing detectors: flag any indented line matching `^\\s+\\(\\d+ \\w+[^)]*\\)\\s*His words:` that sits MORE THAN TWO LINES into an existing `- [ ]/[x]` block \u2014 that is a second request glued into another entry's body. I tested this against the live file: it fires on exactly #154 and nothing else across all 415 entries, zero false positives. (A plain numbering-gap check is NOT the right guard \u2014 24 other gaps in the 31\u2013470 range are legitimate, so it would cry wolf.)\n\nC. THE FEATURE, both halves, in js/timeline.js + styles.css.\n  1. The readout. A small panel that appears while trimDrag is live, positioned above the clip's lane, two rows of three: `Start \u00b7 End \u00b7 Duration` on top, `In \u00b7 Out \u00b7 Change` below. Start/End are the clip's place on the TIMELINE (L.start, L.start + L.duration); In/Out are the trim points within the source (L.trimStart, L.trimStart + duration*speed); Change is SIGNED (`+00:02:59`). Format with the existing frame-accurate MM:SS:FF timecode helper at js/timeline.js:663 \u2014 do not write a second formatter. Update it from applyTrimAt(), tear it down at the pointerup branch (js/timeline.js:3240) and in the cancel/restore path at line 191.\n  2. The notch strip, under the numbers. One tick per frame across the clip's whole span, with the exact notch the edge will land on FILLED IN, plus a pink mark at the in-point and a green mark at the out-point. The landing frame is already computed \u2014 applyTrimAt's `dt` at line 2387 is quantised to whole frames \u2014 so read it from there rather than recomputing. Reuse the ruler's `.notch` styling as the base so the two cannot drift into looking like different things.\n  3. Guard it with a test that drives a synthetic trim drag and asserts (a) six labelled values present, (b) Change carries its sign, (c) exactly one notch is filled and it is the frame the trim actually lands on. Mutation-check via tools/mutate.sh \u2014 deleting the fill should turn it red.\n\nVERIFY AT 380px before calling it done: this readout is six values plus a per-frame strip in a phone-width lane, and \"the option rows run off screen\" is already a known open bug in this app. A long clip at high zoom is thousands of frames wide \u2014 cap the strip's node count the way the ruler windowing at js/timeline.js:700 does, or it will cost a rebuild on every pointermove.\n\nONE TASTE CALL WORTH SHOWING HIM RATHER THAN BLOCKING ON: In/Out are meaningless for a shape/text/drawing layer that has no source. Default to hiding those two cells (keeping Start/End/Duration/Change) for sourceless layers, ship it, and let him say if he wants em-dashes instead.",
   "needsEzra": false,
   "reason": "Confirmed, and the history makes it worse than the auditor claimed. (1) NOT BUILT: applyTrimAt() at js/timeline.js:2384 is the entire trim path and its only visual feedback is showSnap()/hideSnap() at line 2393 \u2014 a 1px snap guide (#tl-snapline, styles.css:2594). Its only two call sites (js/timeline.js:2699, 3091) draw nothing; pointerup at 3240 just nulls trimDrag. The trimDrag state object (js/timeline.js:1481) holds no DOM node. No Start/End/In/Out/Change labels exist in js/*.js, index.html or styles.css. `.notch` exists only as ruler frame ticks (js/timeline.js:710, styles.css:2289) and `.fx-scrub-notch` in the inspector sliders \u2014 neither is per-clip, neither fills a landing notch, neither draws in/out marks. No test in tests/tests.js touches it. Nearest relative is the SLIP ghost's slack readout (v3.61, \"\u25c2 0.7s \u2026 1.3s \u25b8\") \u2014 different gesture, none of the six values. (2) NOT DECLINED OR HELD: the block ends on \"Worth checking first whether our trim already quantises to frames\" with no hold and no answer from him. (3) NOT DONE LATER: the verbatim text appears exactly once in the repo (REQUESTS.md:4961); nothing in POLISH-LOG.md ships a trim readout or landing-notch strip. (4) NOT FILLER: a specced request with an AM screenshot. (5) THE ORPHAN IS WORSE THAN DESCRIBED: commit b03ae56 is titled \"REQUESTS: log #153 (AM trim readout + frame notches) and #154 (black bar...)\" but its diff wrote ONLY #154's header and appended #153's body underneath headerless. \"153\" has never appeared in REQUESTS.md (grep -c \u2192 0). It was born unreachable on 14 Aug, not orphaned later, so tools/next.sh (which matches ^- [ ] **) could never surface it. (6) The entry's own precondition IS met: js/timeline.js:2387 rounds dt to whole frames, so the strip would not be drawing a promise the trim breaks."
  },
  {
   "tag": "165.3",
   "whatToBuild": "Give #draw-overlay its own two-finger POINTER gesture in js/draw-tool.js, so the wheel handler stops being the only pan.\n\n- Keep a pointer cache in draw-tool (same shape as canvas-edit's vpPtrs, js/canvas-edit.js:175-232). In onDown, when a second pointerType 'touch' pointer arrives: discard the in-flight stroke without committing it (drawing = false; FM.drawTool.points = []; redraw()) \u2014 today the second finger commits a layer, which is a bug in its own right \u2014 then snapshot dist/mid/scale/x/y and take the gesture.\n- On pointermove with two fingers: midpoint delta drives FM.viewport.x/y, finger-separation ratio drives FM.viewport.scale, anchored on the midpoint so the canvas stays under the fingers. canvas-edit's startPinch already has the anchored math (vpOriginScreen + the u/dist/scale snapshot) \u2014 mirror it rather than inventing a second version, and skip its layer-resize branch (nothing is selected in drawing mode).\n- Reuse the v8.03 KEEP=90 clamp (js/draw-tool.js:775-786) verbatim after every apply(), and call syncOverlay(); redraw() after a SCALE change (a pure translate carries the overlay for free \u2014 the overlay is a child of the transformed wrapper).\n- Release both fingers on pointerup/pointercancel; a third finger must not join (canvas-edit already learned that one).\n- Leave the wheel listener alone \u2014 it is the desktop half and it works.\n\nTest it with real touch-type PointerEvents dispatched at document.elementFromPoint, not a WheelEvent, or the new test repeats the hole in tests/tests.js:9095. Two mutations worth proving red: a pinch that leaves viewport.scale untouched, and a second finger that still commits a stroke.\n\nWorth doing in the same pass (cheap, and it closes the zoom half properly): the \u26f6 toggle is unreachable while drawing, so either leave #view-bar open when startDraw runs, or drop the pinch in and accept that zoom no longer depends on the rail at all. The pinch makes the rail optional, which is the better answer.",
   "needsEzra": false,
   "reason": "CONFIRMED for the \"grab the screen\" (pan) half; the auditor's mechanism is wrong in one detail but the conclusion holds, and I reproduced it live rather than by reading.\n\nWHAT I MEASURED (app served from the repo at 380x820, freehand drawing mode entered by FM.startDraw):\n- The element at the canvas centre while drawing is #draw-overlay (computed pointer-events: auto, rect 8,65,365,649 \u2014 exactly covering #preview at 8,65,365,649).\n- I dispatched a real two-finger pointer gesture (pointerType 'touch', pointerId 1 and 2, spread + drag) onto whatever document.elementFromPoint returned. Both fingers landed on #draw-overlay. FM.viewport went from {x:0,y:0,scale:1} to {x:0,y:0,scale:1} \u2014 nothing panned, nothing zoomed \u2014 and FM.scene.layers went 0 -> 1: the gesture DREW A STROKE and committed it as a layer.\n- So a pinch on the phone doesn't just fail to zoom, it leaves ink.\n\nWHY (correcting the auditor): it is NOT the capture-phase stopPropagation at js/draw-tool.js:147. stopPropagation cannot suppress another listener on the same element, and canvas-edit's pinch is bound to #preview (js/canvas-edit.js:702 canvas.addEventListener('pointerdown', startMove)). The real cause is that #draw-overlay is a SIBLING of #preview inside #canvas-wrap (styles.css:4607, z-index 40), so while drawing the touch targets the overlay and the event never traverses #preview at all \u2014 startMove/vpPtrs/startPinch (js/canvas-edit.js:175-232) never see a finger. canvas-edit has no body.drawing guard; it simply never gets the event. #draw-overlay also carries touch-action: none, so the browser's own pan/zoom is off there too.\n\nThe rest of the auditor's evidence stands: js/draw-tool.js:761 window.addEventListener('wheel', ...) is the whole pan implementation; grep for touch|pinch|touches|gesture in js/draw-tool.js finds no gesture handler; onDown/onMove (js/draw-tool.js:144-190) handle a single pointer for draw/erase only. The suite's own test (tests/tests.js:9095 'draw-pan-clamp') drives it with window.dispatchEvent(new WheelEvent(...)), which proves the listener fires, not that a finger can fire it. Touch scrolling does not emit wheel on iOS Safari or Chrome Android, and touch-action:none on the overlay removes even the native scroll that might have.\n\nWHERE THE AUDITOR OVERSTATES IT \u2014 the zoom half is reachable, just badly: #view-bar is a child of #stage, and body.drawing only hides #inspector-panel and #timeline-panel (styles.css:4614-4615). Measured: with the view bar opened BEFORE entering drawing mode it stays on screen during drawing (display flex, rect 328,58,46,756, offsetParent live) and #vb-zoomin still works \u2014 I clicked it and FM.viewport.scale went 1 -> 1.25. What is unreachable is the TOGGLE: #btn-amfit sits in #timeline-panel > #transport, and while drawing it measures offsetParent false, rect 0x0. So zoom exists but only if he happened to leave the rail open first (it defaults to class=\"hidden\"); from inside drawing mode he cannot open it. draw-tool.js's v8.03 comment (\"the view bar ... is hidden in that mode\") is wrong about the bar and right about the door.\n\nNOT EXCUSED BY ANYTHING IN THE RECORD: the entry never records him declining or holding it \u2014 it is clause 3 of a four-clause checklist he typed, and the entry ticks it. Nothing later covers it: no REQUESTS.md entry after 165 and no POLISH-LOG release touches drawing pan/zoom (the only later mentions are #97's summary at REQUESTS.md:1507 restating v8.02-8.03). The entry itself flags the exact gap in its last paragraph \u2014 \"it has only been tested with a synthetic event, never with real fingers on a real phone\" \u2014 so the honest reading is that it was ticked on an untested assumption that has now been tested and is false."
  },
  {
   "tag": "draw-on keyframes never registered as layer keyframes",
   "whatToBuild": "Three small, mechanical changes plus tests.\n\n1. Register the two props, GATED to the drawing case. Add to FM.animatedProps (js/scene.js, beside the existing trimPath line) and to keyframeSlots (js/timeline.js, beside its trimPath line), both behind the same condition the inspector uses to build the rows (inspector.js:5304): layer.type === 'shape' && layer.shape === 'path' && !layer.closed. Use distinct addresses such as 'drawOn.from' / 'drawOn.to' rather than bare 'trimStart'.\n\nTRAP \u2014 this is why the gate is load-bearing, not tidiness: layer.trimStart on a VIDEO layer is the source trim in SECONDS (default at js/scene.js:489; read raw as a number at js/exporter.js:190, js/audio-play.js:33, js/timeline.js:469, js/audio-react.js:111 and many more). keyframeSlots deliberately lists slots whether animated or not, so paste can resolve an address on a static target \u2014 an ungated slot would let a pasted keyframe turn a video's source trim into a {kf:[...]} object and NaN the entire clip's timing. animatedProps is safer (isAnimated filters a plain number out) but gate both for symmetry.\n\n2. Fix the sanitiser data loss. js/storage.js:868 -> `if (l.trimStart != null && !(FM.isAnimated && FM.isAnimated(l.trimStart))) l.trimStart = num(l.trimStart, 0, 3600, 0);` \u2014 the identical shape the speed and volume lines on the next two lines already use. Optionally add the same treatment for trimEnd (currently unsanitised entirely).\n\n3. Tests. The existing parity test at tests/tests.js:24465 (\"every keyframe the timeline draws can be addressed by copy and delete\") will cover the registration automatically if its fixture is extended with an open path layer carrying keyframed trimStart/trimEnd. Add two more: (a) an undo/redo round-trip through FM.storage._sanitizeTiming preserves a keyframed trimStart (this is the regression that exists today); (b) moving a drawing clip shifts its draw-on keyframes with it (drives FM.shiftLayerKeyframes). Also worth a test that a VIDEO layer gains no trimStart slot, so the gate cannot be dropped later.\n\nConsider but do NOT necessarily do: renaming the fields to drawFrom/drawTo to end the collision with video trimStart. Cleaner, but needs a migration for saved projects \u2014 recommend the gated registration instead, which carries no migration risk.",
   "needsEzra": false,
   "reason": "CONFIRMED, with one nuance in the entry's favour. The clause's headline promise IS delivered: Draw from / Draw to exist (js/inspector.js:5305-5306, via kfNumRow -> FM.toggleProp/FM.setProp), the renderer honours their keyframes (FM.trimSubs, js/compositor.js:9878-9884, called at 10055), and tests/tests.js:34765-34817 proves a keyframed Draw to grows over time. What is missing is that layer.trimStart/trimEnd are not registered anywhere as LAYER keyframes.\n\nRefutation attempts, all failed: (a) different name/file \u2014 `grep -rn trimEnd js/` returns exactly two sites in the entire app, inspector.js:5306 and compositor.js:9884; (b) shared helper \u2014 read both collectors in full: FM.animatedProps (js/scene.js:394-419) and keyframeSlots (js/timeline.js:263-281) each list trimPath's start/end/offset but neither lists trimStart/trimEnd; (c) trimPath is the mechanism \u2014 no, that is a separate feature (shape stroke draw-on, built at inspector.js:5166-5173, sanitised at storage.js:535-538) and the drawing path never touches it; (d) a later entry/release did it \u2014 searched REQUESTS.md and POLISH-LOG.md for trimStart|trimEnd|\"Draw from\"|\"Draw to\"|draw-on; v9.70 is the only release and nothing revisits it, no Held or declined note; (e) filler \u2014 no, the entry makes it clause 2 and inspector.js:5293 says \"keyframing is the entire feature\".\n\nConsequences, each traced to a call site: no diamonds on the clip (js/timeline.js:1673 and copyKfAt at 319 walk animatedProps); not retimed when the clip is moved (shiftLayerKeyframes, js/scene.js:272) or its speed changed (scaleLayerKeyframes, js/scene.js:302), so the draw-on is left behind at old absolute times; invisible to delete/copy/paste keyframes (keyframeSlots); not sorted (js/scene.js:454) and not divided on split (js/app.js:3052).\n\nData loss verified rather than reasoned: js/storage.js:868 `if (l.trimStart != null) l.trimStart = num(l.trimStart, 0, 3600, 0);` has no isAnimated guard, while the very next two lines (speed, volume) explicitly do. num() at js/storage.js:856 returns its default when +v is non-finite, and I confirmed in a real JS engine (osascript -l JavaScript) that +{kf:[...]} is NaN -> falls back to 0. js/history.js:27 runs FM.storage._sanitizeLayers on every restore, so a keyframed Draw from is silently flattened to a static 0 on every undo/redo. trimEnd is NOT in sanitizeTiming, so Draw to survives \u2014 that asymmetry is why this went unnoticed, since the advertised use is keyframing Draw to."
  },
  {
   "tag": "338-multi-clip-extend-icons",
   "whatToBuild": "Replace the two multi-clip icons in alignRow() with the fill-vs-outline pair already shipped twice. At js/inspector.js:2625, Move-all becomes the solid block + double chevron ('M3.5 8.5h8.5v7H3.5z' with fill=\"currentColor\" stroke=\"none\", plus 'M14 10l2 2-2 2M17 10l2 2-2 2' and the playhead bar 'M21 4.5v15'), mirrored for the left case exactly as js/inspector.js:2541 already does. At js/inspector.js:2632, Extend-all becomes the outlined block open at the near edge + dashed span + single arrow ('M12 8.5H3.5v7H12', 'M12.5 12h6' with stroke-dasharray=\"2 2\", 'M17 10l2 2-2 2', 'M21 4.5v15'). Implementation wrinkle: ab() (js/inspector.js:2605) cannot express this because svgIcon() (js/inspector.js:2123) hardcodes fill=\"none\" on a single path \u2014 give ab() the same { html: ... } escape hatch qbtn already has at js/inspector.js:2493. Extend-all is currently direction-agnostic (one icon) while Move-all has left/right variants; reuse the `right` flag already computed at js/inspector.js:2624 so both mirror together. Then extend the queue-338 test (tests/tests.js:35869) with a second case: at phone width select 2+ clips, park the playhead off all of them, query '#inspector .align-clipacts .qr-btn', and assert the same two things the single-clip case does \u2014 not the identical innerHTML, and fill-vs-outline differs (the test deliberately checks fill rather than pinning `d` strings, so keep that shape). Mutation-check by making the two the same drawing again.",
   "needsEzra": false,
   "reason": "Confirmed. The v9.86 fix for 338 redrew only the SINGLE-clip phone pair (js/inspector.js:2540 and :2548 \u2014 solid block + double chevron vs outlined block + dashed span). The MULTI-clip pair in the same phone inspector still carries the exact pre-fix drawings: js/inspector.js:2625 'M4 8h9v8H4z\u2026' (closed box) and js/inspector.js:2632 'M12 8H4v8h8\u2026' (same box open at one edge). These are not merely similar to the old art \u2014 they are it: the comment at js/timeline.js:3420, written as part of the queue-235 desktop fix, names 'h9v8H4z' and 'M12 8H4v8h8' as the bad pair and calls the gap \"about four pixels of difference at 15px\", which is Ezra's \"couple of pixels of ink\" complaint verbatim. Both are rendered through svgIcon() (js/inspector.js:2123), which hardcodes fill=\"none\" stroke=\"currentColor\", so neither can be filled; and ab() (js/inspector.js:2605) attaches no .qr-cap, so they render as two unlabelled icons side by side with only title tooltips (useless on a phone). The row is phone-only \u2014 styles.css:2483 hides .align-clipacts under @media (min-width: 701px) at styles.css:2482, and tests/tests.js:9967-9968 asserts its absence only on desktop \u2014 so it is on the platform he asked about. Refutation attempts, all failed: (1) no alternate name/file/helper \u2014 grep for all four path strings across js/, tests/, index.html returns only these four sites; (2) no later entry or release did it \u2014 no hits for \"Move all\", \"Extend all\" or \"align-clipacts\" in REQUESTS.md or POLISH-LOG.md, and the only adjacent item (REQUESTS.md:10951) explicitly places grouping buttons OUTSIDE the \"Edit N clips\" row and says nothing about its icons; (3) the entry records no change of mind, decline or hold; (4) it is his whole sentence, not filler; (5) git log -L 2620,2640:js/inspector.js shows those lines last changed 7 Aug (v4.35), ten days BEFORE the 17 Aug v9.86 fix. The existing test (tests/tests.js:35869) queries '#inspector .qr-nudge', a class ab() never applies, so the multi row was never covered. Aggravating detail: desktop's #tl-nudge already handles multi-select (js/timeline.js:3434-3438, \"Move all n clips\") using the FIXED art, so the phone multi-select sheet is now the only place in the app still on the old drawings \u2014 the entry's own claim that the art was \"kept byte-identical \u2026 so the two homes for one action cannot drift\" missed that there are three homes. Honest caveat: his screenshot circled the single-clip pair (\"the two full-width buttons directly above the Colouring / Border / Blending grid\"), and that pair WAS fixed, so this is the same complaint in a second phone location rather than a literally unticked clause of his sentence."
  },
  {
   "tag": "204",
   "whatToBuild": "Make an up-swipe that starts on the Recents block actually open Faves, which means the atEnd gate has to go and something else has to keep the browser scrollable. There is no version of this that is purely mechanical, because the two honest options trade different things away:\n\n(A) Put the gesture on the Faves control itself \u2014 the gold button plus the grab bar above it, at the bottom of the Recents block. Drop atEnd and bind pointerdown to .fxb-favmore (and the .fxb-pullhint strip) instead of the whole .fxb-recents-body, keeping every other mechanic untouched (PULL_MAX 88, PULL_COMMIT 34, REVERSE_BY 12 from the peak, the pow(dy,0.78) damping, the click-eating trap, the reduced-motion opt-out). Dragging up anywhere on that control opens Faves from any scroll position; the rest of Recents still scrolls the list normally. This is the design the code's own Section B-half comment already argues for (\"WHY IT IS A HANDLE AND NOT A BARE SWIPE-UP ON THE SECTION\"), and it is the lowest-risk change \u2014 but it is a swipe on the button, not literally \"swiping up on the recents menu\".\n\n(B) Let the whole Recents block claim up-drags. Drop atEnd entirely and keep the listener on .fxb-recents-body. This is literally what he asked for, and the cost is that the 154px Recents block stops being a place you can scroll the list from \u2014 an up-drag there opens Faves instead of scrolling, and on a 389px viewport that is about a third of the visible browser.\n\nEither way, replace the suite's scaffolding: tests/tests.js:19698 must stop forcing scrollTop to the end, and gain an assertion that the gesture fires from the scroll position where Recents is actually under the finger (scrollTop 0), or the same hole reopens. Also fix grab.title and the hint wording to match whichever gesture is real.",
   "needsEzra": true,
   "reason": "Confirmed, and measured in the real app rather than the suite. The up-pull exists (attachFavPull, js/fx-browser.js:823, bound at :938 to .fxb-recents-body) but is gated on atEnd(sc) \u2014 js/fx-browser.js:821 `scrollTop + clientHeight >= scrollHeight - 1` \u2014 checked at pointerdown (:860) and re-checked in pointermove (:875). Recents is the 3rd of 4 blocks in .fxb-scroll (mode toggle 40, New 170, Recents 154, Categories 488), so the 488px of Categories below it is larger than the visible scroller, and reaching the end always scrolls Recents out of the clipped area.\n\nSwept every scroll position 1px at a time, twice, on localhost:8793 with a layer selected and FM.fxBrowser.open():\n- 375x812 (fxb-sheet): scroller clientHeight 389, scrollHeight 1018, max 629. Gate passes only at scrollTop >= 628. The Recents body is visible only up to scrollTop 407. Positions where both are true: 0.\n- 1280x900 (fxb-sheet fxb-in-inspector, the PC layout from queue 397): clientHeight 217, scrollHeight 759, max 542. Gate passes only at >= 541; Recents visible only up to 407. Positions where both are true: 0.\nA real up-pull on the visible Recents body at scrollTop 0 (pointerdown y=770, moves to 610, elementFromPoint confirms the finger is on .fxb-recents-body) returned {armed:false, transform:\"none\", favViewOpened:false}. FM.fxSheet keeps the visual browser in sheet mode in both layouts, so there is no shipped layout where the scroller has no overflow and the gate is trivially true.\n\nTried to refute it four ways and all four failed. (1) No second implementation: openFavourites has exactly two callers in the repo \u2014 the grab button's click at :764 and the pull commit at :917 \u2014 and attachFavPull is the only gesture. (2) He never withdrew it; REQUESTS.md:8532 quotes him asking a SECOND time and the entry claims DONE v8.06. (3) No later entry or release fixed it \u2014 #462/v11.40 (commit 978fa90) rebuilt the Faves control as a gold button and deliberately kept the grab bar \"because the pull-down gesture is a real feature\", without touching the gate; nothing in POLISH-LOG after v8.06 revisits it; no open item covers it. (4) It is not filler \u2014 it is the entire request.\n\nThe suite hides it: tests/tests.js:19698 forces scEnd.scrollTop = scEnd.scrollHeight before every pull, so it only ever exercises a scroll position the user cannot be at while touching Recents. Meanwhile grab.title (:763) still advertises \"Pull up (or tap) for your faves\", so the app promises a gesture that cannot fire. The reasoning recorded in the entry is right that negating the gate would have made the browser unscrollable; what was never checked is that the mirrored gate and the finger's position are mutually exclusive."
  },
  {
   "tag": "184",
   "whatToBuild": "Do not build two more buttons \u2014 Claude already searched for a distinct operation and found none, and inventing two that duplicate their neighbours is the thing the entry deliberately refused.\n\nReopen the entry instead, which is mechanical and needs no decision:\n1. In /Users/ezrasmith/Claude/FreeMotion/REQUESTS.md:9142, flip `- [x]` to `- [ ]` and change the header to 'PARTS 1, 2 & 3 DONE v8.09 \u2014 one question open'. I verified with python3 tools/_classify.py that the entry's existing text already classifies as 'blocked on Ezra' (the 'one line from (him|you)' rule matches), so the tick is the only thing hiding it; no new classifier rule is needed.\n2. Correct the entry's own record while there: his quoted words never say four, so change 'You said AM has four' to 'the AM screenshot attached to this message showed four buttons above the 1.00x slider'. As written it puts words in his mouth, which is the opposite of the quote-him-verbatim rule.\n3. Add one row to the WHAT I NEED FROM YOU table at the top of REQUESTS.md: '184 | AM's speed menu has four speed-to-playhead buttons; ours has two (start-here, end-here). What do the other two do? | Send the AM screenshot again, or 'two is fine''.\n\nWorth considering as the structural fix, since this is the general case rather than one entry: ship.sh (or _classify.py) could refuse to let an entry be ticked [x] while its body still contains an unanswered \u26a0\ufe0f question \u2014 the same shape as the existing version-stamp and cache-buster gates. A ticked entry with a live question is invisible by design, and nothing in the repo currently notices.",
   "needsEzra": true,
   "reason": "Confirmed, with one correction to the auditor. The code is exactly as described: js/inspector.js:4662-4676 makes two .spd-solve buttons via a handler spdSolve(toEnd) that takes a single boolean, so only two behaviours are expressible; grep for spd-solve across js/, styles.css, index.html and tests/ finds no third or fourth, no alternative name, no shared helper, and no speed-to-playhead control on the timeline. tests/tests.js:29845 hard-codes 'expected two'. Nothing in REQUESTS.md, POLISH-LOG.md, INBOX.md or git history records him answering, declining or holding it, and no later entry or release did it (the only other speed entries, 451 and 455, are the raw layer.speed sweep and the slider step size).\n\nThe auditor's one error: he never said 'four'. His three verbatim quotes in the entry (parts 1, 2, 3) all shipped. 'AM has four of them' was Claude's own reading of the attached screenshot when the entry was first logged (git show f1d2bdf), and the later 'You said AM has four' is Claude attributing its own note back to him. So no clause HE typed is unbuilt.\n\nWhat is genuinely missing is the question, not provably the feature. git show 403644d shows the entry flipped [ ] -> [x] in the same commit that wrote '\u26a0\ufe0f WHY TWO BUTTONS AND NOT FOUR \u2014 one line from you closes this', with the header still reading 'PARTS 1 & 2 DONE'. Because tools/next.sh and tools/status.sh both match ^- \\[ \\] only, and the top WHAT I NEED FROM YOU table lists 469/460/432/456/250/395/392/387/391/342/215 but not 184, that question is unreachable by every queue tool \u2014 the exact 'nothing rots at the bottom' failure the tooling exists to prevent, caused this time by a tick instead of a bad grep."
  },
  {
   "tag": "363",
   "whatToBuild": "Straight mechanical sweep, no decision needed \u2014 the name was already chosen, shipped, and reasoned about at length in v10.09. Four one-line string edits:\n\n1. js/timeline.js:2955 \u2192 `label: 'Add controller (rig control)'` (matches the sibling menu's wording at js/app.js:4308).\n2. js/fx-browser.js:156 \u2192 `'Camera & controller layers have no pixels \u2014 effects can't apply to them'`.\n3. js/fx-browser.js:417 \u2192 `'Masks need a layer with pixels \u2014 camera/controller/group can't be masked'`.\n4. js/shortcuts.js:30 \u2192 `'Add camera, adjustment layer, controller, or sample'`.\n\nKeep every `layer.type === 'null'` comparison and the `'null'` type value untouched \u2014 that invariant is the whole reason saved projects still open, and it is already asserted at tests/tests.js:1674.\n\nThen close the hole that let it through. Extend the existing test at tests/tests.js:1631 (or add a sibling) to assert the old word is gone from the places the current one cannot see, still container-scoped so it cannot self-match the suite's own printed titles:\n- fire the `contextmenu` event on the timeline tracks element, read the rendered menu container's labels, assert none matches /\\bnull\\b/i and one matches /^Add controller/i;\n- assert the two fx-browser rejection strings directly (call the reject paths with a null-type layer and read the toast element, or assert against the source strings) \u2014 a plain regex over the toast text for /\\bnull\\b/i;\n- open the shortcuts sheet and assert its TIPS container has no /\\bnull\\b/i.\n\nShip under the normal gates: bump the version label in index.html plus the `?v=` cache-busters for timeline.js, fx-browser.js and shortcuts.js (ship.sh refuses otherwise), add a POLISH-LOG.md entry, stamp the version into the REQUESTS.md summary block, and add a line to entry 363 recording that the v10.09 sweep was incomplete and what finished it \u2014 do not silently re-tick it clean, since the entry's own \"the old word is gone from the UI\" assertion is the thing that turned out to be untrue.\n\nOne judgement call worth flagging to Ezra in passing (not a blocker, and not worth waiting on): existing saved projects still contain layers literally named \"Null\" in their data. Those are user data, not UI strings, and renaming them would be a data migration \u2014 out of scope for 363, which explicitly scoped itself to strings. Leave them; a user can rename their own layer.",
   "needsEzra": false,
   "reason": "Confirmed, and the auditor found only half of it \u2014 there are FOUR user-facing \"null\" strings still live, not two.\n\nI tried every refutation route and all four failed:\n- Different name / different file / shared helper? No. These are literal hardcoded strings with the word \"null\" in them, not references to a label constant. There is no shared type-label map to route them through \u2014 I grepped for one (`grep -rn \"null:\" js/*.js`) and none exists; each site spells the word out inline.\n- Did he change his mind, decline, or hold it? No. Entry 363 (/Users/ezrasmith/Claude/FreeMotion/REQUESTS.md:10435-10459) is marked `[x]` DONE and its own scope note is the opposite of a hold: \"Places to sweep: the Add menu, the layer row label, the inspector title, any hint text\u2026 A test should assert the old word is gone from the UI and the new one is present.\"\n- Did a later entry or release do it? No. `grep -n \"Controller\" REQUESTS.md` returns only lines 10436 and 10449, both inside entry 363 itself. `grep -n \"Controller\" POLISH-LOG.md` returns exactly one line, 796 (v10.09). `git log -S\"Controller (rig control)\"` returns exactly one commit, cb7f379 \"v10.09: the Null layer is a Controller now (#363)\". The app is now on v11.51 \u2014 nothing swept it since.\n- Conversational filler? No. The rename IS the request.\n\nThe four surviving sites (all verified by reading the surrounding code, not just grep):\n1. /Users/ezrasmith/Claude/FreeMotion/js/timeline.js:2955 \u2014 `{ label: 'Add null (rig control)', \u2026 }` in the right-click-empty-timeline quick Add menu.\n2. /Users/ezrasmith/Claude/FreeMotion/js/fx-browser.js:156 \u2014 `msg = 'Camera & null layers have no pixels \u2014 effects can't apply to them'`, passed to `FM.toast(msg, 2000)` at line 158.\n3. /Users/ezrasmith/Claude/FreeMotion/js/fx-browser.js:417 \u2014 `FM.toast('Masks need a layer with pixels \u2014 camera/null/group can't be masked', 1900)` in `addMaskFromBrowser`. THE AUDITOR MISSED THIS ONE.\n4. /Users/ezrasmith/Claude/FreeMotion/js/shortcuts.js:30 \u2014 `['Right-click timeline', 'Add camera, adjustment layer, null, or sample']` in the TIPS table of the keyboard-shortcuts help sheet. THE AUDITOR MISSED THIS ONE \u2014 and it is the sheet that documents the very menu in site 1, so the two are wrong in the same breath.\n\nThe renamed half is real, so this is genuinely partial rather than a wholesale miss: js/app.js:4308 `'Controller (rig control)'`, js/addmenu.js:232 `{ label: 'Controller', \u2026 }`, js/addmenu.js:805 the tint-table key `'Controller': '255, 118, 140'`, js/app.js:1980 `name: 'Controller'`.\n\nDeliberately NOT counted as missing: js/ai-manifest.js:60 describes layer types to the LLM (\"null (invisible rig control)\") and must keep matching the wire value `layer.type === 'null'` \u2014 that is a protocol string, not UI.\n\nWhy nothing caught it: the only test, /Users/ezrasmith/Claude/FreeMotion/tests/tests.js:1631, scopes itself to the Elements-tab `.addmenu-card` labels (line 1650) and the new layer's `L.name` (line 1674). Both scopings are correct and were chosen for a good reason \u2014 the entry warns that `document.body.textContent` self-matches because the suite prints its own test titles \u2014 but the result is a test that can never see a context menu, a toast, or the shortcuts sheet. It asserts \"no Null tile in the Add menu\", never the entry's actual promise that \"the old word is gone from the UI\"."
  },
  {
   "tag": "378-all-explanations-no-tracker",
   "whatToBuild": "1. Open a new numbered item at the BOTTOM of REQUESTS.md (new asks wait their turn) titled something like \"The rest of the explanation blocks \u2014 the `insp-hint` paragraphs `reg.desc` did not cover (from 378)\". Quote his 378 words verbatim, and list the live sites with file:line, split into two groups: (a) genuine explanation prose he would recognise from his screenshot \u2014 js/inspector.js:4497, 4507, 4514, 4518, 4524, 4530, 4082, 3095, 3206, 3614, 3698, 4674, 4708, 2486, 4807; (b) empty-state/status lines that queue 331 already ruled are keepers (\"No adjustable parameters\", \"This filter is empty\", \"No previews\", the mono/keyframe warnings). Cross-link 301, 331, 346, 350, 378.\n2. Amend 378's footer so the \u26a0\ufe0f points at that new open number instead of at closed queue 346, so the hand-off lands somewhere reachable.\n3. Tick 346's two `[ ]` sub-clauses at REQUESTS.md:7865-7868 \u2014 I verified both hints are gone from the code (\"Z sets depth\" and \"wiggle, oscillate, bounce\" return no matches in js/).\n4. Structural guard, since this is the fourth time an explanation request has half-landed: extend the queue 378 test (tests/tests.js:35751) or add a sibling that walks the inspector cards and fails when a NEW long-prose `insp-hint` appears \u2014 a length/sentence threshold with an explicit allowlist of the status/empty-state lines. Today nothing but a reader would notice a new paragraph being added, which is how the count crept back to a dozen.\n5. Also worth deleting the now-dead `.fx-desc` rule at styles.css:1294 while in there (needs a `?v=` bump per ship.sh).",
   "needsEzra": true,
   "reason": "Confirmed, with the code half correctly credited. The effect-panel `reg.desc` block is genuinely gone (js/inspector.js:1334 comment; `grep fx-desc js/*.js` returns nothing \u2014 only a dead CSS rule at styles.css:1294 survives), so clauses 1-3 of the entry are honest. What is missing is the \"make a note to get rid of ALL explanations\" half. Roughly a dozen explanation paragraphs of exactly the kind he screenshotted still render as `.insp-hint`: the six camera-panel ones (js/inspector.js:4497 camera motion blur \u2014 three sentences; :4507 field of view; :4514 and :4518 depth of field; :4524 and :4530 fog \u2014 all live, `cameraopts` is reachable via js/inspector.js:4579 for camera layers, gated at :2883), plus anchor point (:4082), fill (:3095, :3206), crop (:3614), edit points (:3698), speed (:4674, :4708), TTS export note (:2486) and captions (:4807). (The remaining `insp-hint` uses are empty-state/status lines \u2014 \"No adjustable parameters\", \"This clip has no picture to crop\" \u2014 which queue 331 explicitly carved out as keepers, and js/inspector.js:4574 is inside `motionBlurBlock`, never called; the auditor was right to exclude it.) I could not refute the \"no tracker\" part: (1) the entry's own footer hands off with \"\u26a0\ufe0f Still open elsewhere\u2026 (queue 346 names one)\", but 346 is `[x]` v9.93 \u2014 and I verified its two named hints ARE gone from the code, so 346 is genuinely shipped and its \"worth sweeping for the rest\" line is stranded inside a closed entry, unreachable to tools/next.sh; (2) every `explanation` hit in REQUESTS.md is in a `[x]` entry (301, 331, 346, 350, 378), nothing after 378, and POLISH-LOG.md has nothing post-v9.84; (3) `./tools/next.sh` lists 17 reachable items, none about explanations or hints; (4) the entry records no change of mind and no **Held** note; (5) the guard test at tests/tests.js:35751 asserts only `#inspector .fx-desc` is absent and never touches `insp-hint`. This is precisely the failure CLAUDE.md names \u2014 a request that is unreachable rather than deprioritised. Severity is moderate, not severe: the gap was written down honestly, just in a place no tool can find. Separately noted (belongs to 346, not 378): 346's two sub-clauses are still `[ ]` at REQUESTS.md:7865-7868 even though the code is verified done \u2014 a tick-hygiene miss only."
  },
  {
   "tag": "386-media-outline-never-reaches-the-frame",
   "whatToBuild": "One-line-shaped fix plus a test that measures the picture.\n\n1. Open the gate at js/compositor.js:10755 for media outlines. The cleanest version stops enumerating reasons and just asks the seam: compute `const eff = effectiveFx(layer, t)` when `!layer._cueFx`, and clone only when `eff !== layer.effects` (effectiveFx already returns the SAME array when it has nothing to add, which is what that identity check is for). Keep the `_cueFx` guard exactly as it is \u2014 drawLayer is re-entered for masks, motion blur, blend modes and effect isolation, and Object.assign propagates the flag onto every clone, so the stroke cannot be concatenated twice. Case D of the probe proves the outline is correct once the merged list gets through, so no rendering work is needed.\n\n2. Replace or supplement the seam-only assertion at tests/tests.js:4753 with one that rasterises: render a small opaque image layer through FM.renderScene with the toggle off and on and require the outline's colour to APPEAR. The existing test passes with the feature completely inert, which is exactly how this shipped. tests/_q386audit.html is left in the tree as a working measurement \u2014 it currently reports 0 green with the toggle on and should report ~5376 after the fix.\n\n3. Mutation-check the gate itself, not just canBorder and the translation. Both of those were already mutation-checked at v10.64 and both were caught, and the feature still did nothing \u2014 because no test looked at a frame.\n\nWorth folding in while there (small, same subject): the stroke kernel clamps width to 16 project px at js/compositor.js:3992, while the card's Size row offers 0-100 at js/inspector.js:5158. Once the outline renders, every value from 17 to 100 will look identical. Either clamp the row to 16 or raise the kernel's cap.",
   "needsEzra": false,
   "reason": "Confirmed, and measured. Entry 386 clause 1 (\"Outlines should still be a toggle option on videos and clips, not just shadow\") is marked DONE at v10.64, and half of it did ship: canBorder now includes video/image (js/inspector.js:5147) so the Outline & Shadows card offers the toggle, Position, colour and Size. But the toggle renders nothing.\n\nThe translation into the alpha `stroke` effect is written at js/compositor.js:1427 inside `effectiveFx`. `grep -rn \"effectiveFx\" js/ index.html tests/ tools/` finds exactly ONE render-path caller, js/compositor.js:10756, and the line above it is `if (layer && ((layer.captions && layer.captions.length) || _pvOn) && !layer._cueFx)`. An ordinary video or image layer has no captions and is not being live-previewed, so the merged list is computed nowhere and discarded. drawLayer then builds its post-fx list from the raw array at :10840 (`layer.effects.filter(...)`), which never contains the outline.\n\nMeasured headlessly (probe at /Users/ezrasmith/Claude/FreeMotion/tests/_q386audit.html, run with `python3 tests/_probe.py tests/_q386audit.html`), a 100x100 red image layer through FM.renderScene on a 1080x1920 project, counting green pixels:\n  - toggle OFF: 0 green\n  - toggle ON, width 12, colour #00ff00: 0 green (pixel-identical to OFF)\n  - the same stroke pushed straight onto layer.effects: 5376 green\n  - toggle ON *plus* a captions array, which opens the gate at :10755: 5376 green\n\nThat last case is the decisive one and goes beyond the auditor's evidence: the toggle produces exactly the right outline the moment the gate lets the merged list through. So nothing is wrong with the translation, the parameters or the effect kernel \u2014 only the gate. Contrast js/compositor.js:11744, the GROUP border the entry says this copies, which writes the stroke into tmp.effects and therefore renders.\n\nRefutation routes checked and closed: (1) not under another name or in another file \u2014 the only other layer.stroke reads in the compositor are text :11023, animated text :1805, shape :11054, a bbox pad :5690, a mask-tool overlay :10299 and a layer thumbnail :12235, none of them the media draw path; (2) the entry records no change of mind, no decline and no hold \u2014 it explicitly ANSWERS the one open design question (\"it follows the ALPHA\") and then claims the work built; (3) no later entry or release revisits it \u2014 `git log --since=2026-08-19 -- js/compositor.js` shows nothing touching this, and POLISH-LOG.md:851 is the only mention; (4) it is not conversational filler \u2014 it is the whole of clause 1, and the entry's own warning (\"it will do nothing on media and look 'still broken'\") predicted precisely this outcome.\n\nWhy the suite is green with it inert: tests/tests.js:4753 asserts FM._effectiveFx (:4762) and the presence of the checkbox in the card, never a rendered frame \u2014 the repo's own documented \"a function only the tests call is a decoration, not a seam\" trap. Both mutations the entry says it ran (removing video from canBorder, disabling the translation) are caught by that seam test while the picture never changes."
  },
  {
   "tag": "419-rail-diamond-remove-still-shared",
   "whatToBuild": "In js/inspector.js's moveTransformPanel rail diamond, compute the mode's effective channel set ONCE and use it everywhere, instead of using the unfiltered `props` for the decision, the removal and the lit state.\n\nConcretely: hoist the existing add-filter into a helper, e.g. `const scopeOf = () => props.filter(k => MT_PRIMARY[mode].indexOf(k) >= 0 || FM.isAnimated(layer.transform[k]) || (MT_SWEEP_STATIC[mode] && layer.transform[k] != null && layer.transform[k] !== MT_DEF[k]));` then inside the click handler use `const judge = only || scopeOf();`, `const usable = only || scopeOf();` (both branches), and at build time set `stateProps = scoped || scopeOf()` so `anyAnim`/`onHere` and the title follow the same scope.\n\nEffect: the diamond can never delete a keyframe it would not itself have created, and a tilt key at the playhead no longer flips the button into remove mode when rotation has no key there. Move and Scale are unchanged in practice \u2014 a z or scaleX with a key at the playhead is animated, so it is already in the filtered set, which the existing CONTROL 2 in the test asserts.\n\nThen extend tests/tests.js:25754 ('rotation and the two tilts keyframe independently (queue 419)') with the missing removal case: rotationX keyed at t=0 and t=2 with rotation static, playhead at 0, no row selected, press the rail diamond \u2014 assert rotation becomes animated and FM.hasKeyframeAt(L.transform.rotationX, 0) is still true and evalProp(rotationX, 0) is still 20. Also assert the symmetric case still works (press twice at the same time returns to the starting state). Mutation-check it with tools/mutate.sh by restoring `props` in place of the scoped set, which should reproduce the deletion.\n\nThis is mechanical and inside a design Ezra has already been given (per-row diamond for one channel, rail diamond for the whole mode), so it needs no decision from him. Update entry 419 in REQUESTS.md honestly rather than silently \u2014 the clause was ticked and the release note claimed the removal half, so the correction belongs in the entry he reads.",
   "needsEzra": false,
   "reason": "Confirmed, though the auditor's framing needs sharpening. What IS there: (a) each of the three rotate rows has had its own independent diamond since v4.78 \u2014 `mt-vbox-kf`, js/inspector.js:3339-3360, wired with kfKey 'rotation'/'rotationX'/'rotationY' at js/inspector.js:3934-3936 \u2014 and it toggles only that channel for BOTH add and remove; (b) the v11.33 add-side fix (MT_SWEEP_STATIC, js/inspector.js:3267) and the row-scoped path (`only`, js/inspector.js:3761-3762). So clause 2 is true of the per-row controls.\n\nWhat is NOT there: any scoping of the RAIL diamond when no row is selected \u2014 and `kfSel` really is null by default (`let kfSel = null`, js/inspector.js:5385, plus `kfNavSync()` -> `kfClearSel()` on every panel/mode change, js/inspector.js:5430). Two lines are still computed across all of MT_PROPS.rotate: `const judge = only || props;` and `const usable = only ? only : (add ? props.filter(...) : props);` (js/inspector.js:3763-3767), and the build-time lit state `onHere`/`anyAnim` (js/inspector.js:3752-3755).\n\nThe remove SET on its own is defensible: hasKeyframeAt implies isAnimated (js/scene.js:328-330) and the add filter already includes isAnimated, so remove is a subset of what the button would itself have added \u2014 the diamond is self-consistent. The genuine defect is `judge`. A tilt keyframe at the playhead flips the whole button into remove mode even when rotation has no key there. Concrete loss: rotationX = {kf:[{t:0,v:20},{t:2,v:60}]}, rotation static, playhead 0, nothing selected -> add=false -> one press deletes the tilt's t=0 key instead of keying rotation, so rotationX evaluates to 60 everywhere and the 20->60 tilt animation is destroyed; a second press keys rotation and re-keys rotationX flat at 60. That is exactly the entry's own words for the half it ticked ('removing hit all three unconditionally, so clearing a spin key took the tilt's'), and the v11.33 POLISH-LOG entry tells Ezra it is fixed ('clearing a spin keyframe took the tilt's with it. Now: keyframing rotation keys rotation'). The button also titles itself 'Remove keyframe at playhead' on the strength of a channel that is not rotation \u2014 which the code comment at js/inspector.js:3746-3747 says the fix was meant to prevent, true only when a row is selected.\n\nSearched for it elsewhere and it is not there: only one transform rail exists (`mt-kf` at js/inspector.js:3754; 3512/3660/4311/4599 are point-edit, subtitles, volume, speed); no entry after 419 in REQUESTS.md and no POLISH-LOG entry after v11.33 (current app is v11.51) touches the rail diamond; the 'Remove: every channel, so stray keyframes can always be cleaned up' comment dates to v2.08 by `git log -S`, so 419 never changed that branch. The 419 test (tests/tests.js:25754-25805) clicks rail() three times, all add-path, so nothing would catch it."
  },
  {
   "tag": "461",
   "whatToBuild": "Reopen 461 with a Held note recording that the REPLACE-vs-ADD question was never actually put to him, then put it to him in one line: \"On the effect category tiles \u2014 did you want the icons to REPLACE the coloured backgrounds (every tile the same neutral panel, the icon does all the work), or keep the colours as they are now? You asked back in v6.14 to make those colours brighter, so I did not want to delete them on a guess.\" If he says replace: delete the 13 .fxb-banner[data-cat=...] rules at styles.css:1670-1684 so every tile falls back to the neutral linear-gradient(120deg, var(--panel-3), var(--panel-2)) already on .fxb-banner at styles.css:1647; drop the now-dead b.dataset.cat assignment at js/fx-browser.js:1116 (check nothing else keys off it first); optionally tint each icon per category via currentColor so the identity survives; do the same for the audio twin at styles.css:4801-4804 / js/audio-fx-browser.js:189, which has gradients and no icons at all; and extend the test at tests/tests.js:25550 to assert all tiles compute the SAME background, so the gradients cannot creep back.",
   "needsEzra": true,
   "reason": "Verified: the clause was not built, and the entry's own author knew it and ticked DONE anyway. styles.css:1670-1684 still defines all 13 per-category gradients on .fxb-banner[data-cat=...], and js/fx-browser.js:1116 still sets b.dataset.cat = cat.key to drive them; line 1117 appends the new icon on TOP of the gradient rather than in place of it. POLISH-LOG.md:926 (v11.39) says so in plain words \u2014 \"The tile colours are unchanged\". The test at tests/tests.js:25550-25572 only asserts each tile has an icon and no two icons match; it makes no claim about colour. Searched for every escape route and found none: no other renderer or helper does it (js/audio-fx-browser.js:189 is the audio twin and still has gradients with no icons at all); no later entry touches it (462-470 are the FAVES button, filter rows, multi-select, the stairs split, TTS voice, damaged projects, keyframe hit targets); no reply from Ezra exists \u2014 REQUESTS.md:14051 asks \"If he meant REPLACE the colours rather than add to them ... he should say\", and grepping REQUESTS.md and INBOX.md for any answer finds nothing, INBOX.md is empty. The clause is not filler: \"Instead of how it is rn with random colours\" was a second message he deliberately sent right after the first, and the entry's own pre-work reading at REQUESTS.md:14030 called it the operative instruction (\"the gradient-only tiles are the thing he is replacing, not decorating\"). Per this repo's own rule, an unresolved clause makes the entry Open with a Held note, not DONE. One honest caveat that does NOT rescue the tick but does shape the fix: POLISH-LOG.md:389 (v6.14) records Ezra asking for these same banner gradients to be \"brighter and more pretty\", and that release doubled their alpha for him \u2014 so he has previously asked for MORE colour there, and \"random colours\" may be him naming the problem (colour that means nothing) rather than ordering a deletion."
  }
 ],
 "buildableNow": [
  "141-export-custom-fps",
  "142-default-shape-colour-misses-freehand-and-vector",
  "118",
  "121",
  "257",
  "154",
  "165.3",
  "draw-on keyframes never registered as layer keyframes",
  "338-multi-clip-extend-icons",
  "363",
  "386-media-outline-never-reaches-the-frame",
  "419-rail-diamond-remove-still-shared"
 ],
 "needsEzra": [
  "285",
  "204",
  "184",
  "378-all-explanations-no-tracker",
  "461"
 ],
 "refuted": [
  {
   "tag": "154-text-editing-black-bar",
   "why": "REFUTED on four independent grounds.\n\n1. HE WAS ASKED, AND HE ANSWERED. The auditor's central claim \u2014 \"he was never asked the question\" \u2014 is false. REQUESTS.md:6388 (entry #325, logged verbatim 17 Aug) records his answer to a batch of open questions: \"2 - no - no still laggy on phone - yes still laggy scrolling -  I think black bar is gone - pc buttons seem fine\". That is three days and roughly fifty versions after v7.02, on a device long since reloaded. #154's note was explicitly conditional \u2014 \"it needs a reload before the report means anything\u2026 If it DOES persist after reloading\u2026 ask him which strip he means\". He reloaded and said it was gone, so the condition that would have triggered the question never fired. REQUESTS.md:6401 maps that answer, deliberately not closing it: \"187 \u2014 the black bar: 'I think black bar is gone'. Same treatment as 306 \u2014 WATCHING, not closed.\"\n\n2. A LATER ENTRY DID IT. The black bar did not stop at #154 \u2014 it was carried forward as its own numbered item #187 (REQUESTS.md:7238-7330) and hunted across three more releases: v7.87 (the slam's flat ring, #239), v8.54 (the overscan), and finally FOUND AND FIXED in v9.95 \u2014 `#home-screen::after` at `inset: 0` walking off the edge under `hm-drift`, measured as a 7px strip 44% darker at 380x820, fixed at `inset: -12%` with the gradient position compensated, guarded by a test that steps the animation across 21 samples and mutation-checked (POLISH-LOG.md:782). #187 is ticked. So the family this report belongs to was not closed without an answer; it was closed with a measured cause, a fix and a regression test.\n\n3. THE AUDITOR'S CSS ARGUMENT IS WRONG. The claim is that v7.02's fix \"never exercises the body.text-editing path\". But v7.02's fix is document-level, not mode-scoped: styles.css:118 is `html { background: var(--bg); }` with the comment \"The document CANVAS, which covers the whole web view\u2026 Anything the page does not cover shows THIS colour\". Adding `.text-editing` to `<body>` cannot change what `<html>` paints, so the fix covers text-editing by construction \u2014 which is why the test asserts the html-vs-body invariant rather than enumerating modes. Separately, the text-editing takeover on a real device is NOT unhandled: js/screen.js (FM.screen, 135 lines) exists solely for it \u2014 its header documents the exact case, \"whenever the document cannot scroll (any full-screen takeover with body{overflow:hidden}) iOS still has to reveal the focused field, and the only budget it has left is offsetTop\", measured on iPhone 390x844 with the keyboard up (v5.95, POLISH-LOG.md:370). Because offsetTop only slides the visible window DOWN inside an unchanged layout viewport, the page still fills it and no canvas strip is uncovered.\n\n4. THE REPORT WAS AGAINST A STALE BUILD, AND THE ENTRY SAYS SO HONESTLY. git show 16ed6f0 (\"note his 'still happens when I edit text' was on v7.00 (pre-fix)\") shows the note was added as an explicit flag, not a shrug. His version chip read v7.00 in the shot immediately before; v7.02 was the fix. \"Flagged rather than assumed either way\" is the correct handling of a report against a build that predates the fix, not a request quietly dropped.\n\nAlso checked and empty: no black-bar item appears in the open queue (tools/next.sh lists 33 open items, none of them this), INBOX.md is empty, and no report of a bar during text editing exists anywhere after 14 Aug in REQUESTS.md, POLISH-LOG.md, NEXT-SESSION.md or BUG-HUNT.md.\n\nThe only residue is thin: the words \"which strip do you mean\" were never put to him in that exact form, and the canvas-ground test (tests/tests.js:19351) does not toggle body.text-editing. Neither is a missing feature, and re-asking a question he already answered \"gone\" would be noise.\n\n\u26a0\ufe0f SEPARATE AND GENUINE FINDING, FOUND WHILE CHECKING THIS. Commit b03ae56 is titled \"REQUESTS: log #153 (AM trim readout + frame notches) and #154 (black bar\u2026)\" but only #154 got a header line. **Entry #153 has NO header and does not exist as a numbered item.** Its body is orphaned inside #154's ticked `- [x]` block at /Users/ezrasmith/Claude/FreeMotion/REQUESTS.md:4960-4978 \u2014 it begins mid-air with \"(14 Aug, with an Alight Motion screenshot.) His words: 'When dragging a clip from the edges to extend\u2026'\" and asks for two things: a six-value live trim readout (Start/End/Duration, In/Out/Change) and a frame-notch strip that fills in the exact notch the edge will land on, with his reasoning \"the notches are frames and the whole thing has to actually line up with the notches\". `grep -n '\\*\\*15[0-9]' REQUESTS.md` returns 150, 151, 152, 154, 155\u2026 \u2014 no 153. tools/next.sh cannot see it, and nothing matching a trim readout appears in POLISH-LOG.md. That is a real request, invisible to the queue and carried as done under another item's tick \u2014 exactly the failure the oldest-first tooling exists to prevent. It should be restored to Open as #153."
  },
  {
   "tag": "321",
   "why": "The \"previewing\" half is not an unbuilt feature \u2014 it is empty by construction, and the auditor's own evidence shows a crash-guard rather than a suppressed picture.\n\n1. The behaviour he reported IS fixed and tested. js/fx-browser.js:461 routes the tile through togglePick -> restartPreview (js/fx-browser.js:317) and it only lands via commitPicks (js/fx-browser.js:385). tests/tests.js:34572 asserts the tap badges, does not add, does not close, and that Add then applies it; the 198-tile sweep at tests/tests.js:35470 includes the pseudo tiles by name and fails if any tile applies instead of picking.\n\n2. There is nothing to preview. The preview channel is effectiveFx (js/compositor.js:1410), which concatenates EFFECT INSTANCES onto layer.effects; a mask is layer state, so makeInstance('_mask') has nothing to return and the filter at js/fx-browser.js:315 exists to avoid that, not to hide a result. Even if _mask were passed through, the frame would be identical: masks.make() (js/masks.js:35) creates path: [], and buildMaskAlpha's documented contract is that it \"Returns null when there is no enabled mask, OR when no enabled mask contributes any fillable geometry ... so an enabled-but-still-empty mask never blanks the layer.\" A previewed fresh mask is pixel-for-pixel the unpreviewed frame.\n\n3. The only way to make the tile show something is to preview an invented default shape (ellipse/rect) while Add commits an empty path \u2014 a preview that lies about the result. That is a taste decision, not an omitted implementation.\n\n4. It was not quietly dropped either. #360 (18 Aug, filed AFTER v9.67) is his own follow-up on the mask tile; its named symptoms (swipe to delete, minimise) shipped v10.07 and the remaining piece \u2014 making a mask a real registry effect \u2014 is recorded open and explicitly left as his call. On 21 Aug he closed the related sweep with \"dont test all the effects urself ill just tell you if the others arent behaving as one when i see it\". He has been back through this area since and re-reported other things about the mask, not this.\n\nSEPARATE, VERIFIED FINDING (not the audited clause, do not treat it as one): the entry and the v9.67 log both justify the limit with \"that is what the editor opening on Add is for\", and that no longer happens. FM.fxSheet(root) is called with sheet=true on every open (js/fx-browser.js:1367), on PC too since queue 303, so sheetMode() is always true while the browser is open. Add therefore always runs PSEUDO_ACTION['_mask'](true) -> addMaskFromBrowser(quiet=true), which skips the editor at js/fx-browser.js:430; the non-quiet branch on line 461 is unreachable. Today: pick Mask, press Add, an empty mask lands, the canvas is unchanged and no editor opens \u2014 the only route to drawing it is expanding the collapsed mask row and pressing \"Edit path\" (js/inspector.js:2218)."
  },
  {
   "tag": "405",
   "why": "The auditor's literal observation is correct \u2014 the width sweep at tests/tests.js:36428-36480 never names #view-bar \u2014 but the conclusion does not follow, and I measured it rather than reasoning about it.\n\nI drove the real app headless at 320/360/390/430 with the view rail forced visible. Its geometry is byte-identical at all four widths: box 46.0 x 316.0, scrollHeight/clientHeight 608/314, zero buttons horizontally outside the rail, zero buttons narrower than 27.5px, last control reachable after scrolling to the end, no overlap with the transport row, and app.scrollWidth == clientWidth at every width. Only the rail's x offset moves. That is by construction: styles.css:651 pins it position:absolute, top:6/right:6/bottom:6, width:46px inside #stage (overflow:hidden), so narrowing the frame cannot clip it or push it off-screen. A width loop over the rail would assert a constant.\n\nThe rail's actual clipping failure is VERTICAL \u2014 v5.29, Ezra: \"the view row is kinda ruined, needs to not be crammed in and be slide-able up and down\" \u2014 and it IS gated, by tests/tests.js:8512 ('view-rail'), which asserts overflow-y scrolls, that no .vb-btn is squashed below 34px, and that scrolling to the end brings the last control into view. atPhoneWidth (tests/tests.js:45) sets frameElement.style.width ONLY; it cannot vary height, so wrapping that test in the sweep could not exercise the rail's failure mode at all. Two further reasons the omission is deliberate rather than a miss: #view-bar carries class=\"hidden\" (display:none) until the view button is tapped, so a sweep querying it without un-hiding measures 0x0 and asserts nothing; and six rail controls (vb-zoomin/zoomout/slower/faster/tlin/tlout) are 34x26 steppers, so applying clause 4's tap-target floor to the rail would fail a layout he approved \u2014 the exact trap the entry already documents for the transport row at <=340.\n\nThe auditor's broader claim (\"the sweep proves nothing about the rest of the editor at 320/360\") is contradicted by the entry's own wording: clause 1 reads \"no horizontal overflow anywhere (scrollWidth > clientWidth ON THE SHELL)\", and the sweep does precisely that on #app at all four widths \u2014 a whole-editor check, not a transport-only one. Clauses 2 and 4 are scoped to the transport row because the entry itself scopes the risk there (\"the transport row (queue 373) now carries EIGHT controls, which is exactly the shape that breaks first\"), and POLISH-LOG.md:830 records the same scope honestly rather than overclaiming.\n\nEzra's actual words were \"make sure other peoples phones will actually fit everything nicely on screen and format correctly\" \u2014 he never mentioned the view rail; \"or the view rail\" is the entry author's own elaboration. The shipped gate answers his sentence at four widths, and the rail half of that elaboration is true on measurement. Nothing is quietly missing."
  }
 ]
}