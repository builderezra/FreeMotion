[
 {
  "tag": "?",
  "firstStep": "Write tests/_songtail.html on the pattern of tests/_durationfix.html: import tests/_fixtures/liar.mp3 through FM.loadVideoFile, put it on a 30s timeline, and record el.currentTime / el.duration / el.ended / el.paused every 500ms across 11.21s, on two paths \u2014 (a) straight play from 0, (b) scrub into the tail first (which is what js/app.js:878 does), then play. Path (a) should show duration climbing 11.210 -> 26.36 and the song finishing; path (b) is where I measured it stopping dead at 13.453. Then replace the stub at tests/tests.js:20807 with a real-element regression test asserting the song still advances past 20s, so the gate can never be \"proven\" again by an element that cannot end. Do not apply the auditor's clamp.",
  "risk": "The buildable half is verification, not a fix \u2014 on Chrome the shipped gate already behaves correctly, so the probe may only confirm what is there. The residual worry the auditor raises (Safari/iOS rewinding an ended element) genuinely cannot be measured on this machine and is the one part that does need Ezra's phone. The bigger risk is following the auditor's prescription: landing their clamp would silently reintroduce the truncated-song bug #96 was closed for, and every existing test would stay green because both tests use stub elements that cannot end."
 },
 {
  "tag": "47",
  "firstStep": "Build item 2 first \u2014 smallest, zero taste calls, precedent already in the file. In js/export-resume.js expose why the recorder stopped (e.g. a `get status()` returning 'ok' | 'capped' | 'failed' alongside the existing `get recording()` at line 343, set at the three sites that kill it: lines 292, 310 and 325). Then in js/exporter.js, after `await recorder.settle()` (around line 843) and once mid-render, if the recorder is no longer recording, emit the honest message in the idiom already used at exporter.js:675/:718 \u2014 e.g. `if (FM.toast) FM.toast('Storage is full \u2014 this render is no longer crash-protected', 6000)` for the failed case and a distinct line for the 512MB cap, so the two are told apart the way the three audio-loss paths are. Add a unit test in tests/tests.js next to the existing ceiling test at line 31831 that forces writeMedia to return false mid-run and asserts both that the export still completes and that the user was told; then confirm with tools/mutate.sh that deleting the notification turns it red.",
  "risk": "Three things could make this the wrong build. (1) Scope creep back into the deferred half \u2014 the entry's STATUS line still says \"BIG, wants a session of its own\", and anyone reading top-down may start the OffscreenCanvas move Ezra deprioritised; the STATUS stamp should be corrected as part of the work. (2) The wake-lock item is mis-framed in the audit: it will not survive backgrounding or an iOS freeze, so if it is built and then claimed to fix \"an export interrupted by a call\", that is a false claim of the exact kind this entry's own history warns about \u2014 the honest version is wake lock for screen sleep, plus a pagehide/freeze flush so the resume record is settled before the page is discarded, plus telling him on return what happened. (3) Only he can confirm the real phone cases (a call mid-export, iOS backgrounding). Everything here is buildable and locally testable, but \"verified on his device\" is not, so the shipping note must not overstate it. Minor: the low-storage warning's threshold and wording are a small taste call \u2014 keep it warn-only rather than blocking an export, since a false positive that refuses to render would be worse than the gap it closes."
 },
 {
  "tag": "202",
  "firstStep": "In js/perf-probe.js only (do not touch js/timeline.js): inside run(), wrap FM.timeline.rebuild and FM.timeline.updatePlayhead for the duration of the sample and restore both in finish() \u2014 which already runs on the wall-clock-deadline path too, so the restore cannot be leaked by a hidden tab. Count calls and accumulate total + worst ms for each, guarding with a re-entrancy depth flag because rebuild() calls this.updatePlayhead() internally at js/timeline.js:3383 and a naive wrapper double-counts. Then add a TIMELINE line beside CANVAS/PROJECT at js/perf-probe.js:205-210 (\"TIMELINE  14 rebuilds \u00b7 worst 62.0ms \u00b7 310ms of the 10.0s sample\", or \"no timeline rebuilds happened during this sample\" when the count is zero), and add a branch to verdictLines() BEFORE the appMs branch at :79-81: when worstRebuild on its own exceeds the late-frame threshold (budget * 2.5), name the rebuild as the cause instead of falling through to \"GPU work, video decode or garbage collection\". Extend the _verdict input shape with those fields and add cases to the existing pure-verdict tests at tests/tests.js:22098 \u2014 one where a 60ms rebuild must be named, one where zero rebuilds must leave the current wording untouched \u2014 then mutation-check the new branch with tools/mutate.sh and ship via tools/ship.sh (remember the ?v= cache-buster for js/perf-probe.js in index.html, which ship.sh now enforces).",
  "risk": "The instrumentation could still come back empty and prove nothing: the entry's own 14 Aug measurement found ZERO timeline rebuilds during playback, so if he presses Measure and just watches it play, the TIMELINE line reads all zeros. The line must therefore say \"no timeline rebuilds happened during this sample\" in plain words rather than printing 0s, and the Settings hint should tell him to do the thing that feels slow (tap layers, scrub, add a clip). Bigger risk: the timeline may simply not be his problem at all \u2014 #202 found his actual project is 3024x4032 (12.2 megapixels) and costs 163ms/frame on a fast Mac, and calls that \"very likely the cause of the lag reported across #95, #125 and #202\". If so this work confirms a negative rather than fixing anything. That is still worth having (it lets the entry finally close or redirect instead of guessing), but it is not a fix, and #95 stays \ud83d\udfe0 NEEDS YOU until he sends one reading afterwards. Wrong-build risk on the second half: if someone reads this as licence to also rewrite buildTracks, they will spend a day refactoring the timeline's DOM hub against a desktop number the entry itself already declared irrelevant to his projects."
 },
 {
  "tag": "?",
  "firstStep": "In js/compositor.js: add two params to the textspacing row at :680 \u2014 `word` (-40..200, step 1, def 0, unit 'px') and `line` (-50..200, step 1, def 0, unit '%'); widen the TEXT_FX state at :1723 to `{ text, letterSpacing, wordSpacing: 0, lineScale: 1 }`; and in the handler at :1675 write `st.wordSpacing += word` and `st.lineScale *= (1 + line / 100)` \u2014 ALWAYS additive, deliberately NOT routed through the existing `mode` switch (document why in the comment: mode's \"Replaces\" branch on a 0-default param would zero the line height of every saved project that already carries a textspacing instance). Before touching any draw site, extend the existing test at tests/tests.js:27194 (which already uses the `textRun` fixture with 7px of layer tracking) to assert that default params leave wordSpacing 0 and lineScale 1, and that non-zero values land \u2014 that gives a red-to-green anchor. Then plumb the two fields outward one site at a time: ctx.wordSpacing at :9020 and :10996, the lineScale factor into the `lh` terms at :9022 and :11000, the memo key at :12063, and finally the per-glyph paths drawArcLine (:1863) and drawAnimatedText (:1817), where word spacing must be added manually at space characters because each measureText is a single unit. Run the plan's identity-vs-HEAD diff (EFFECTS-PLAN.md:22-56) on a text layer with a legacy `{spacing:30}` instance and require 0 differing bytes before shipping.",
  "risk": "The `line` param's unit is unstated in the plan (percent-of-line-height vs px added); the two readings happen to give near-identical ranges at the default 96px font but diverge badly at small font sizes, so the calibration is a guess he may want retuned. Bigger risk: this reaches into the shared text layout that kinetic text presets, curved text, caption pills, wrapping and the selection box all run through, so a wrong move silently changes how existing text renders rather than failing loudly \u2014 the plan's identity-vs-HEAD gate (EFFECTS-PLAN.md:22-56) is mandatory here, not optional. Also, layerSize (:12127-12134) deliberately measures with the RAW layer.letterSpacing and never runs text effects, so the on-canvas selection box will disagree with the picture once line height is animated; that disagreement already exists for the shipped letterSpacing effect, so matching it is defensible, but it is a visible wart he did not ask for. Lastly, ctx.wordSpacing degrades to a no-op on any engine that lacks it \u2014 same feature-detect pattern the code already uses for letterSpacing, so it fails quiet rather than broken."
 },
 {
  "tag": "294",
  "firstStep": "Edit /Users/ezrasmith/Claude/FreeMotion/BEFORE-PUBLISHING.md only (docs-only commit, no version bump \u2014 precedent is commit 07a5d5a, and all ship.sh gates already align: index.html v11.50 == newest POLISH-LOG v11.50, REQUESTS summary stamp v11.50, tree clean). Three edits: (1) Add to the \"What is currently modelled on Alight Motion\" list the screens whose own POLISH-LOG entry states an AM-screenshot origin, quoting the log line as evidence the way the existing entries do \u2014 start with v9.97's \"Insert your Media\" template screen (\"Built to his reference rather than to a fresh idea\"), then v5.43 (Z position as a move-pad sub-mode, \"a SUB-MODE of the move pad... exactly as AM has it\"), v5.47 (the three easing graph families, from six AM screenshots), and the four modules whose file headers declare AM origin but which the list never mentions: js/graph-editor.js, js/color-wheel.js, js/fx-browser.js, js/text-edit.js. Do NOT list v8.09, v9.54 or v10.00 \u2014 those mention AM only as a capability comparison, not a design source. (2) Update the stamp at line 50 to \"Swept v4.26 to v11.50\", keeping the existing caveat that a sweep can only find what was written down at the time, and note that v8.84 (queue 294, the + button removed as an identity move away from AM) belongs in Progress, per the request at REQUESTS.md:7000. (3) At line 99, change \"as of v3.73 there are 24 mentions\" to \"as of v11.50 there are 47 mentions across 20 files\", and replace \"so if this goes open-source\" with a statement that the repo IS already public (HTTP 200 unauthenticated) \u2014 leaving the rewording itself as an open, unstarted item.",
  "risk": "Two ways this goes wrong. First, over-listing: an eager sweep that adds every AM-mentioning POLISH-LOG entry (v8.09's speed cap, v9.54's service worker, v10.00's Voronoi) would inflate the list with things that are capability comparisons, not copied design, making the pre-launch blocker look far bigger than it is and possibly discouraging Ezra from ever starting it. The rule must stay mechanical: list it only when the entry's own text says the screen was built from a screenshot. Second, and more serious: making item 7 unconditional must NOT be read as licence to go reword the 47 AM-naming comments across 20 files. Those comments carry real provenance \u2014 several quote Ezra verbatim and explain why a behaviour is shaped as it is \u2014 and stripping them is a genuine judgement call about his own repo, plus a 20-file code change needing a version bump, cache busters and a suite run. The buildable work stops at correcting the note; the rewording stays an open decision for him. Residual limitation, inherited from the original sweep and stated in the file itself: a doc sweep can only find panels whose AM origin was written down, so the result is a more honest list, not a complete one."
 },
 {
  "tag": "?",
  "firstStep": "In js/app.js wrapTo() (line ~1316), give the wrap the same treatment as the v7.66 resume path: alongside the `m.el.currentTime = local` write, add `try { m.el.volume = 0; } catch(e){} m._resumedAt = now;` so declickGain ramps the element back up over DECLICK_S (45ms) instead of splicing at full volume. Then add a suite test next to 'loop wrap re-origins the clock instead of pinning the playhead' (tests/tests.js:15307) using transportRig with loop:true and a loop region set INSIDE the clip (P.loopIn/loopOut mid-clip), asserting that on the tick after a wrap the element's applied volume is below 1 (i.e. FM._declickGain returns < 1 because _resumedAt was set) \u2014 it is 1.0 today, so the test goes red before the edit. Mutation-check it with `tools/mutate.sh js/app.js \"m._resumedAt = now;\" \"\" \"loop wrap declick\"` to prove the test actually catches the removal, and run the suite in the FOREGROUND with `timeout: 500000`.",
  "risk": "It may not be HIS pop. It only bites when loop mode is on AND an in/out region is marked inside a clip \u2014 less common than plain whole-timeline loop, which is already covered. So this can close a real defect without closing his report, and the entry would still be waiting on his one line (loop vs clip edge vs mid-clip). That is acceptable on the entry's own precedent \u2014 v7.66 shipped the edge fade on exactly that reasoning (\"an abrupt cut of a playing waveform is a defect whether or not it is *your* pop\") \u2014 but it must be written up that way, not as a claimed fix for #148. Second risk: the fade-OUT half at loopOut needs declickGain to learn about the loop region (a `loopOut - t` term gated on FM.loop && FM.hasLoopRegion()), and that touches a function three other tests depend on (tests/tests.js:3850, :31053) \u2014 keep it in a separate step from the fade-in so a regression is attributable. Do NOT bundle the preservesPitch change in; it is the one part that needs him."
 },
 {
  "tag": "408",
  "firstStep": "Stamp the missing back-pointer, exactly the way templates already do it. In js/home.js `edit()` (line 1531) pass `fromElement: e.id` into `FM.projects.create({...})`, have js/storage.js `create()` persist it on the index record next to the `elementDraft` flag it already handles at line 1331, and set `FM.scene.project.fromElement = e.id` so it saves and reloads with the doc \u2014 mirroring `useAsNew` at js/storage.js:1555/1566. That is a few lines, touches no UI, and is a prerequisite for every other step; verify by tapping an element and checking `FM.projects.list().find(p => p.elementDraft).fromElement`. Then add `async updateFrom(eid, projectId)` to `FM.elements` modelled on js/storage.js:1578 \u2014 repack as `saveFromProject` does (js/storage.js:1639-1660) but write to the EXISTING `elem:<eid>` key and update that index entry's count/thumb in place, so there is no duplicate entry to unpick. Only then touch `elementDraftCard`: give it the \u22ef it is already advertised as having, carrying 'Update element \"X\"' (when `p.fromElement` resolves to a live element, same confirm wording as js/home.js:1115), 'Save as a new element\u2026', and 'Delete draft' \u2014 that last one alone fixes the undeletable-orphan bug and can ship on its own. Remember the ship.sh gate: a changed js/*.js needs its `?v=` bumped in index.html (currently v11.50) plus a POLISH-LOG entry and the REQUESTS.md summary version stamp.",
  "risk": "One real fork inside the fix: Ezra said \"when you edit it and are done you leave\", which can be read as save-on-leave, whereas queue 408 shipped an explicit one-tap \"Update template X\" with a confirm. Copying 408 is the conservative, non-destructive choice and it is the precedent he did not object to \u2014 but if he meant automatic, he will report this again as \"I edited it and it didn't save.\" The elementDraft is a weaker case for the explicit tap than a template was: a draft is not its own project, it is hidden from Projects and exists solely as that element's workspace, so silently writing back is more defensible here than it was there. Mitigate by making \"Update element X\" the FIRST item on the draft card's \u22ef and prompting on leave if the draft is dirty, rather than requiring him to find a menu.\n\nSecondary risk: this sits in an entry stamped \"\u23f3 WAITING ON EZRA\" whose text says \"Do not guess at clause 3 and do not rebuild clauses 1-2. They work.\" That instruction is what has kept the hole alive \u2014 clause 1 does not work, it half-works \u2014 so shipping this means editing that line to say what was actually missing, and the entry stays OPEN on clause 3 regardless. Do not let this fix look like it closed 342."
 },
 {
  "tag": "408",
  "firstStep": "Start with the two pieces that carry no design risk, in this order. (a) Tick clause 3 in REQUESTS.md (line ~7183) citing js/home.js:1122 and js/storage.js:1459, noting duplicate (#374) and updateFrom (#408) as the rest of 'create'. (b) Fix the silent export drop in js/storage.js:392-399: have serializeScene collect skipped layers into a `dropped` array (layer name + size), return it alongside the payload, and have exportFile (js/storage.js:970-979) toast the count and names instead of an unconditional 'Project file saved' \u2014 with a test that asserts an over-limit layer is reported, since nothing today would notice if the warning regressed. Only then build the template file: add FM.templates.exportFile(tid) that reads getPack(tid) and emits {app:'freemotion', v:1, kind:'template', project, layers, media, fonts}, walking pack.media[layerId].file through fileToDataURL directly (NOT serializeScene, which reads the live FM.media) and deriving fonts from pack.layers' fontFamily against FM.fonts; add a 'Save template file\u2026' row to the template card's \u22ef menu beside duplicate/remove in js/home.js; and branch importFile on kind==='template' to pack into Templates via the save() path instead of creating a project \u2014 keeping the data:-only dataURLToFile guard (js/storage.js:383) and clampProjectDims, since an imported template is untrusted input on the useAsNew route that #470 just hardened. Finish with a round-trip test (save \u2192 exportFile \u2192 importFile \u2192 useAsNew) and run the suite in the foreground with timeout: 500000.",
  "risk": "Two things could make this the wrong build. First, he said 'project FILES \u2026 like what's already in' \u2014 a literal reading is that the existing .fmotion.json already covers sharing, and a template can already make the round trip today in four taps (useAsNew \u2192 Save project file\u2026 \u2192 import \u2192 Save as template), so a bespoke template file is a convenience rather than a missing capability, and could be more than he asked for. Second, and more likely to bite: the 6MB embed cap means a real template's video is dropped on export, so a shared template file arrives structure-only with empty slots \u2014 technically correct, and on his phone it would read as broken. That policy call (raise the cap, keep it and warn, or strip media deliberately) is a technical tradeoff a developer can settle, but it is the part where 'correct' and 'what he pictured' can diverge. The entry is also still marked '\ud83d\udca1 LATER' and sits behind 13 older items, so building it now jumps the oldest-first rule unless the loop reaches it."
 },
 {
  "tag": "215",
  "firstStep": "Build the measurement rig BEFORE the guard, so the failure is witnessed rather than assumed. In tests/tests.js, reuse the window.AudioEncoder/window.AudioData stub from line 33508, wrap window.Mp4Muxer.Muxer to capture its constructor config, stub HTMLAnchorElement.prototype.click (as tests/_xresume.html:81 does) so nothing downloads, and drive FM.exporter.run() over a 160x120 two-second scene with one audio-bearing layer. First assert the encode-failed case really does build the muxer with `audio === undefined` \u2014 the v9.43 claim the entry says was only argued. Then flip the stub to resolve with ZERO chunks and no error and watch it declare `audio: {codec:'aac',...}`. Only then add the guard after js/exporter.js:721: `if (audioChunks && !audioChunks.length) { FM._audioTrackDropped = 'no-chunks'; if (FM.toast) FM.toast('The soundtrack produced no audio \u2014 exporting WITHOUT SOUND', 6000); mix = null; audioChunks = null; }` \u2014 before line 726 where the sink and muxer are built \u2014 and mutation-check both directions with tools/mutate.sh (removing the guard goes red; firing it when chunks exist goes red on the noise control).",
  "risk": "The zero-chunk case is unmeasured and, by run()'s own arithmetic, unreachable from data alone: buildAudioMix returns null unless a layer contributed, and its buffer is always at least ceil(0.01*48000)=480 samples, so encodeAudio always calls encode() at least once. Reaching zero chunks needs a browser that passes isConfigSupported, configures, accepts a frame, resolves flush() and emits nothing without firing the error callback. That is plausible (215's whole record is browsers lying about AAC) but it is a hypothesis, and this entry's own standing instruction is \"Measure, do not read\". So the guard may harden a path that never fires, and it does NOT explain the export Ezra reported \u2014 it must not be written up as a fix for #215 or used to tick the entry. The durable value is the other half: the run()-level test the entry twice records as owed. If the rig turns out to need heavy stubbing of VideoEncoder and the frame loop as well, the honest fallback is to ship the test for the encode-failed ordering (which is real and already claimed) and leave the no-chunks guard out rather than add an untested branch. A smaller risk: nulling `mix` after the crash-resume signature is computed (line ~688 uses `audio: !!mix`) makes the signature disagree with the file \u2014 pre-existing on the encode-failed path, but this adds a second route to it."
 },
 {
  "tag": "?",
  "firstStep": "In js/perf-probe.js, add a `late` records array alongside `gaps`: in tick() (line 144-157), when `gap > (1000/60) * 2.5`, push `{ at: now - t0, gap, renderMs, tier, effective, canvasPx, seeks, drops, videoReady }` captured from FM._perfState()/FM.playbackQualityInfo() and a snapshot delta of FM.playbackStats.seeks/.drops since the previous tick, plus the max readyState/seeking flag across FM.media entries for video layers. Then add a PURE `FM.perfProbe._traceLines(records)` next to `_verdict` (line 107) that prints the worst 5 with those fields and the gap between successive late frames (the clustering signal), and call it from finish() after verdictLines. Write the test first in tests/tests.js beside the existing _verdict tests at line 22098, feeding fabricated records \u2014 one set clustered on seeks, one set with a render spike, one set with neither \u2014 and assert the three read differently. That gives the whole change a red-turns-green proof without needing rAF or a real device. Only after that touch js/settings.js:402. Do NOT build the scene-cost warning; put that to Ezra as a one-line choice instead.",
  "risk": "The trace is instrumentation, not a fix \u2014 it closes nothing on its own and only pays off when Ezra sends another readout, so if he never does, the work sits unused. Worse, it may not answer the question even then: a rAF tick observes state AFTER the gap, so for the second report's 252ms freezes (render 0.02ms \u2014 the cost was outside our loop) the trace can say \"a seek coincided\" or \"our render spiked\" but cannot see inside the gap on iOS Safari, where longtask attribution does not exist. If the cause is GPU compositing or GC, the trace will report \"neither\" and the entry gains a bucket rather than an answer. Second risk: silently making Measure start playback changes what the tool measures \u2014 if what feels laggy to him is scrubbing or editing rather than playback, an auto-play sample answers a question he did not ask. Mitigate by starting playback visibly and saying so in the hint rather than doing it behind the readout. Third: the entry is long and has already produced \"three plausible-but-wrong causes\" by its own count, so any temptation to tune something off the new trace before he confirms it should be resisted."
 },
 {
  "tag": "361",
  "firstStep": "Do not fix anything yet \u2014 measure geometry instead of ink. Extend tests/_sketchlive.html at 380x820 so that, mid-second-stroke with the finger still down, it logs #draw-overlay's getBoundingClientRect() alongside #preview's getBoundingClientRect(), plus overlay.width/height and the live dispScale(), and captures the same four numbers immediately before stroke 1's pointerdown. If the two rects or the two scales disagree across stroke 1's commit, the bug is a stale overlay transform and the fix is to re-derive it \u2014 call FM.drawTool.sync() at the end of commitStroke (js/draw-tool.js, after the FM.selectLayer(null) on line 216, so it runs on the first stroke's commit too, not only the refit branch), which is where the asymmetry lives: stroke 1 goes through FM.addPathLayer -> refreshAll -> inspector.refresh with a selection set, and later strokes do not. Pace the stroke one pointermove per real animation frame (await rAF) while you are in there, and add the control the old probe lacked \u2014 a frame where ink MUST be non-zero because the finger has moved \u2014 so a blind probe fails instead of reporting clean. Separately and independently of the outcome, fix the mis-bucketing: tools/_classify.py's HELD regex needs the same scoping the STANDING_HEAD rule already got, so a hold phrase inside a note ABOUT another entry cannot hold this one, with a self-test case using REQUESTS.md:10388's exact wording \u2014 this is verbatim the \"five real items hidden by a phrase in a note about them\" shape the file says it already cured once, and it is currently telling Ezra he asked to leave a bug he reported the day before.",
  "risk": "The bug has failed to reproduce once already in a careful measured probe, and if it fails again today there is nothing correct to ship \u2014 the honest outcome is a second \"not reproduced\", which is a spent loop tick. The real danger is the auditor's suggested fix being applied anyway: adding syncOverlay()+redraw() after FM.timeline.rebuild() is harmless and idempotent, so the suite will stay green and the clause will look tickable, while the mechanism it targets (a stage-height change on rebuild) is ruled out by the CSS grid. That is a false tick on an entry whose whole discipline is that clauses cannot be ticked without verification. Secondary risk: his 21 Aug repro says \"an extra line\" but never says freehand vs vector, and the entry's own outstanding question asked exactly that \u2014 freehand is the strongly implied reading (multi-stroke append only exists there) but it is an inference, not his word."
 },
 {
  "tag": "387",
  "firstStep": "Rewrite the tail of entry 387 in REQUESTS.md to unblock it, before touching any code: drop the \"STATUS: NEEDS YOU\" line and the closing \"WAITING ON EZRA \u2014 was he PLAYING or SCRUBBING?\", and replace them with the verified answer \u2014 he was PLAYING (accent pill at t=2.0/4.0/6.0s, grey at 8.6s; styles.css:2230 plus js/app.js:1621/1656 make that unambiguous; timecode 00:00:40 -> 00:03:23 -> 00:05:23 -> parked 00:07:47) \u2014 and with a third retraction saying plainly that the \"1.10s freeze starting 8.43s\" is the post-playback tail, not a stall, because the whole-frame region is 32/32 still with the pill grey and the canvas black. Record the corrected reading in its place: during 2.0-7.0s the preview canvas region is still on 57-59 of every 60 frames while the timeline region is still on 4-9, i.e. the preview repaints ~2.6 times a second while the UI runs at ~55fps, measured at v11.19 and to be re-confirmed against current. Then land the instrument that makes that reading trustworthy: move the region probe into tests/ (fold it into tests/_framediff.swift), raise the resolution from 96 to 480, and make a control region containing known-moving content MANDATORY so the tool refuses to report \"identical\" when the control also reads identical \u2014 the exact gate whose absence produced both wrong readings here.",
  "risk": "Three things could make this the wrong thing to build. (1) The CAUSE is still open even though the SYMPTOM is now measured \u2014 if it turns out to be iOS Safari's HEVC decode rather than app code, no local fix is verifiable and the entry bounces back to needing another recording from him. Chrome already failed to reproduce at v10.62, and this harness cannot reach a 1080p H.264/HEVC clip decoded by iOS Safari on a real phone. So step 3 may end in a diagnosis rather than a fix; steps 1, 2 and 4 are safe regardless. (2) The recording is v11.19 and the app is v11.50 \u2014 I checked that no commit in between touched the play cadence, so the risk is low, but it is not zero and the reading should be restated as \"measured at v11.19\" rather than as current fact. (3) ORDERING: ./tools/next.sh shows 387 is NOT the oldest open item \u2014 three unnumbered items plus 47, 95, 96, 98, 125, 129, 148, 179 and 202 sit ahead of it. Several of those (95, 125, 202) are the same phone-lag class and the region probe would serve them too, but per CLAUDE.md's oldest-first rule this entry should not jump the queue just because it has newly become unblocked."
 },
 {
  "tag": "418",
  "firstStep": "In /Users/ezrasmith/Claude/FreeMotion/index.html:436, replace the whole `<g transform=\"translate(0 2.5)\">\u2026</g>` with a ring-plus-head drawn about (12,12): an `<path d=\"\u2026\">` arc of radius ~7 sweeping ~300\u00b0 (start ~60\u00b0 past the gap, `A 7 7 0 1 0 \u2026` for the anticlockwise/undo direction) keeping the svg's existing fill=\"none\" stroke-width=\"1.8\", plus a sibling `<path fill=\"currentColor\" stroke=\"none\">` triangle (~4.5 units across the base) sitting at the sweep's end, tangent to the ring. Mirror it on :437 for redo (clockwise). Then confirm the union bbox is centred: `document.getElementById('btn-undo').querySelector('svg').getBBox()` should give y + height/2 within 0.6 of 12 \u2014 nudge the gap's angular position rather than re-adding a translate. Run the suite in one foreground call, `python3 tests/_cdp.py --port 8777` with timeout: 500000, and check `transport-vcentre` is green. Then screenshot the transport row at 380px to confirm undo and redo read as opposite directions at real size, bump the version label plus a POLISH-LOG entry, and ship with tools/ship.sh (only index.html changes, so no `?v=` bump is required \u2014 that gate covers js/*.js and styles.css).",
  "risk": "The spec is a prior session's prose description of an image this session cannot see, so fine proportions \u2014 ring radius, where the ~60\u00b0 gap sits, head size \u2014 are inferred rather than matched. Two ways that could land wrong: at 24px with a 1.8 stroke a 300\u00b0 ring plus a solid head can read mushy or as a \"refresh/loop\" glyph rather than undo/redo, and the anticlockwise-vs-clockwise distinction has to stay obvious on a phone. Also possible he meant \"skinnier\" relative to the CURRENT buttons rather than the reference, in which case 1.8 is one notch too heavy \u2014 but going thinner would break the row-match that his own 19 Aug clause asked for, so 1.8 is the defensible reading. All of that is iterate-on-feedback risk, not blocked-on-him risk: any correct circular-arrow pair is strictly closer to what he asked for twice than the chevron-and-hook shipping today."
 },
 {
  "tag": "417",
  "firstStep": "In styles.css, make the add row's head column behave like a .track-head: stretch it to the row's full height (align-self: stretch on .tl-addrow-inner, or align-items: stretch on .tl-addrow \u2014 its 21px height is why the divider is a stub), give it background: var(--panel) plus a 1px negative inset so it covers .tl-addrow's own dashed border segments, and put position: sticky; left: 0; z-index: 8 on .tl-addrow-inner (NOT on the head \u2014 the sticky inner makes a stacking context that traps a child's z-index below the ruler's 7). Keep .tl-addrow--empty and .tl-addrow--line exempt (both already dissolve the head with display: contents at styles.css:6940 and 6946), and leave the \u2295 where #417 put it. Verify with the probe I already ran: three clips, FM.scene.project.markers = [{t:1.0},{t:1.6}], #timeline.scrollLeft = 220 at 380px, screenshot via tests/_cdp.py, then assert every pixel in the add row left of --head-w equals the layer head's rgb(10,20,26) \u2014 today it reads rgb(17,45,50) for the wash and rgb(101,104,61) on the bookmark column. Land that assertion as a real test in tests/tests.js (there is none for the #417 divider), then bump the ?v= for styles.css (and js/timeline.js if the DOM changes) and ship with ship.sh. Leave clause 2 open and unticked.",
  "risk": "The screenshot he sent is not in the repo (his images do not survive the text inbox \u2014 see #418), so the only record of what he circled is a previous session's prose, and that prose says the \u2295 must be clipped as well. If that reading is right, shipping only the paint fix under-delivers and he will say \"the plus too\". Doing the opposite \u2014 moving or removing the + \u2014 would silently undo #417, which is the failure this entry itself warns about, so under-delivering is the correct way to be wrong here. Second risk: how the dashed outline terminates at the divider is a small look call (dashed edge at the line, or open there). Making the head column paint like every other row's head \u2014 opaque var(--panel), + on top \u2014 is the choice that invents nothing, since that is already the app's rule for that column and #417's whole point was \"cut off at the line all the others cut off at\"."
 },
 {
  "tag": "428",
  "firstStep": "Reproduce in tests/ with a stub (`FM.mediaLib.list = () => [ ...6 fake entries ]`, `isAudio` by kind), open the sheet at 380px, click Media, and assert `.addmenu--sheet .addmenu-tab` height equals the height it has on Shape (64.1px vs today's 24.0px) \u2014 the assertion fails on today's code. Then fix in styles.css: make the tabs row unshrinkable inside the sheet AND let the body's `clamp(170px, 32vh, 260px)` floor yield when a pinned strip is present, and re-measure that the body's bottom still lands on the sheet's bottom (it overflows by 37px if only the first half is done). Add the tab-height test to the suite so a future strip cannot crush the row silently \u2014 the existing `addsheet-fill` test never sees this state because it runs with an empty library.",
  "risk": "The fix is a flex-shrink negotiation in the exact chain styles.css warns has collapsed the panel three times. Verified in the live page: setting `.addmenu-tabs { flex: 0 0 auto }` alone restores the buttons to 64.1px but pushes the body 37px past the sheet's bottom (body bottom 849 vs sheet 812), so the body's `min-height` floor has to give way in the same change or one complaint is traded for another. Second risk: the same screenshot shows a separate ~124px dead band under the two library rows on Media/Audio (rows are locked to 64px by `.addmenu--sheet .addmenu-body--lib .addmenu-grid`, queue 358, at his explicit instruction) \u2014 that band is what #428 was about and it is back on the lib tabs. Reclaiming it is a taste call and should not be bundled in; and if \"top buttons\" turns out to mean the action row rather than the tab row, the action cards also shrink 173x136.5 -> 112.7x64.1 on the same trigger, so both readings share this one cause but the sizing target for the actions alone would need his eye."
 },
 {
  "tag": "432",
  "firstStep": "Create an empty `.nojekyll` at the repo root, add a viewport meta to tests/_tmplicon.html (`<meta name=\"viewport\" content=\"width=device-width,initial-scale=1\">`, matching index.html) and let its flex row wrap at phone width so the 24px sample keeps a real 44px box, then ship with tools/ship.sh. No version bump is needed \u2014 the change touches no js/*.js or styles.css so the cache-buster gate will not fire, and REQUESTS.md's stamp already matches index.html at v11.50. Then re-curl tests/_tmplicon.html expecting 200 (spot-check tests/_soloroom.html too), load the live URL at 380px and screenshot to confirm the five swatches read at real size, and only then replace the bare file path in entry 432 and in the summary block's row for 432 with the direct link https://builderezra.github.io/FreeMotion/tests/_tmplicon.html. Leave clause 2 open and untouched \u2014 the letter is his. Do not attempt a ship.sh gate for clause 1.",
  "risk": "The repo cannot tell me whether the 21 Aug session also sent him a screenshot of the four icons in chat. If it did, he has seen the options, 432 was genuinely waiting on his taste call all along, and fixing the 404 is valuable housekeeping rather than the thing unblocking this entry \u2014 the buildable work would still be correct and wanted, but the auditor's framing (\"parked on a question he was never able to see\") would be overstated. Secondary risk: adding .nojekyll makes all 233 internal harness pages publicly served. Exposure is near-zero since the repo is already public and tests/run.html and tests/tests.js already serve 200, but it is a real change in what the live site publishes and worth stating rather than sliding past. Sequencing also matters \u2014 putting the URL in the summary block before .nojekyll lands would hand him a 404, which is worse than the file path he has now."
 },
 {
  "tag": "406",
  "firstStep": "In /Users/ezrasmith/Claude/FreeMotion/js/app.js delete lines 5205-5223 whole (the \"---- Canvas presets (queue 183) ----\" comment block through the closing brace of cvCurrentCfg, leaving `const canvasBtn = \u2026` intact), then change line 1867's toast text from \"is bigger than any preset. Change it in Canvas settings.\" to \"is bigger than the largest canvas size. Change it in Canvas settings.\" Then bump index.html's version label and its js/app.js `?v=` cache-buster (ship.sh refuses without it), add the POLISH-LOG.md entry, stamp the new version in the REQUESTS.md summary block, append the two fixes under 454 while leaving it unticked and \ud83d\udfe0 NEEDS YOU on the layerPresets question, and ship with `tools/ship.sh \"\u2026\"`. Suite is non-visual here, so no 380px screenshot needed \u2014 but run the full suite in the foreground with timeout: 500000.",
  "risk": "The toast reword is the one soft edge: he said \"remove any other function\", not \"rename\", and the entry warns against renaming instead of removing. Here there is no preset function left to remove \u2014 only a stale string naming a picker's size list \u2014 and removing the warning itself would be worse than keeping it, so rewording is the only sane compliance. If he actually meant the size picker's fixed 720/1080/1440/2160 options should also go, this is a half-fix (still an improvement, not a wrong turn). Second risk: shipping two cosmetic changes bumps a version and burns a release slot without moving the entry's real blocker, so the ship note and REQUESTS.md must say plainly that the layerPresets question is still waiting on him rather than reading as progress on 454."
 },
 {
  "tag": "456",
  "firstStep": "Make the options page reachable and honest, touching no app CSS. 1) Rename `tests/_createbtn.html` to `tests/createbtn.html` (proven servable \u2014 `tests/run.html` returns 200; prefer this over adding `.nojekyll`, which changes how the whole site is built for one file). 2) Fix its two false lines before pushing: the intro \"the two are currently identical, which is what you want broken\" and the HOME row's \"The rainbow ramp, rotating once every 34s.\" Render the HOME row as the app actually behaves \u2014 static conic ramp, plus the white `orb-sheen 6.4s` band from theme-glass.css:371 \u2014 so he is comparing against the real button. 3) Add a fifth question to the page, since it is now visible that it is open: his \"(both moving)\" is not true of the home button today, so ask whether the sheen counts as \"moving\" or whether home should get colour motion too, and which. 4) Push, then `curl -s -o /dev/null -w \"%{http_code}\" https://builderezra.github.io/FreeMotion/tests/createbtn.html` for a 200, and give him that URL. 5) Correct REQUESTS.md entry 456: strike \"the identical animation, fm-plus-turn 34s linear infinite\" and \"Clause 2 is satisfied by all four\", replace with what the grep shows, and cross-reference 398's recorded TIMELINE-only scope. 6) Fix the stale comment at styles.css:7354 (comment only \u2014 no rule changes) to say the ::after disc was never written and could not have won against the glass theme anyway.",
  "risk": "The real risk is the parent agent reading \"PARTIALLY_BUILDABLE\" and building the auditor's item 1. Restyling `#hm-new` now means silently answering the taste question he was explicitly asked (\"I just want options\"), with the one animation he called lazy, on the button the options page told him would not change \u2014 and 398's entry already recorded not doing it as a considered decision. Secondary: I cannot prove he has never seen the options (he may have been shown screenshots in-session), so the 404 fix may unblock less than it appears \u2014 though it is correct and harmless either way. Also unresolved and genuinely his: he located the in-project button \"top-right of the editor toolbar\" while the code's spinning rainbow is the empty-timeline `+`; the identification is probably right because he said it spins and only that one does, but on 21 Aug the top-right export button WAS wearing the same disc (fixed in 457), so a screenshot would settle it. Do not restyle any button on the strength of that inference."
 },
 {
  "tag": "460",
  "firstStep": "Copy the harness at tests/tests.js:1046-1088 into a scratch probe (or just run that test with the fill swapped) against fill '#cc22cc' at makeInstance defaults, and print the effects whose diff is 0 \u2014 the run doubles as its own control, since a broken harness makes all 43 read silent. Then parameterise the shipped test over [{fill:'#8fd4ff', noop:['darkglow','replacecolor','hslbands','matchgrade']}, {fill:'#cc22cc', noop:<measured list>}], keeping the exact-list assertion and both existing error messages. Run the suite three times (python3 tests/_cdp.py --port 8777, timeout 500000) to prove the new fill is not flaky, mutate-check with tools/mutate.sh by pointing the second fill back at #8fd4ff, and ship via tools/ship.sh. Do NOT touch FLAT_TESTABLE, FX_LABEL or deadWhy. Report to Ezra only the trimmed question the entry already poses \u2014 the hint exists on 7 effects; which of the two options \u2014 plus, if he wants a number, the measured magenta list as a by-product.",
  "risk": "The surviving work is small enough that it may not be worth the tokens: it adds a guard, fixes nothing he can see, and leaves 460 sitting at NEEDS YOU regardless \u2014 so it could read as motion without progress if it is reported as having addressed his complaint. Two concrete failure modes. (1) Flakiness: doubling the fills doubles that test's render load and some Colouring effects carry randomness (noise/voronoi/dither); one that lands near the >6-per-channel diff threshold on magenta would intermittently redden a test that gates every ship. Run it three times before committing and drop any effect that is not stable. (2) Scope creep back into C: the second-fill list will make channelremap's silence on #cc22cc concrete and look like an invitation to hint about it \u2014 that is the option-2 work, and doing it through FLAT_TESTABLE ships a claim the mechanism cannot prove. If both bite, the honest outcome is to skip the test entirely and leave the entry waiting."
 }
]