# Building the UX review's "Start here" fixes

His word, 25 Sep, after the review was published: **"Go"**. Read as: build the 12 Start-here fixes on
this branch, in order, without touching `main`. Each fix gets a test that FAILS against the code before it
and PASSES after (the prove.sh rule), run in headless Chromium at 1280 and 380 px.

**Before merging to main:** this branch does not bump the version label, the `?v=` cache-busters or
POLISH-LOG, so it can merge cleanly under whatever `main` has shipped since. `tools/ship.sh` will ask for
those at merge time, and it is right to: a missed buster serves the old file.

| # | Fix | Status | Test (name contains) | Proof |
|---|-----|--------|----------------------|-------|
| 1 | Tap a layer on the preview to select it | **Not built: your decision.** v2.93 "the canvas never selects… Layers are picked from the timeline." Needs your call (see report). | | |
| 2 | Dragging an animated layer moves the whole animation | **Not built: your decision.** v3.00 "canvas dragging no longer drops keyframes (your clarification)". Needs your call. | | |
| 3 | Keyframe diamonds show a stale state after the playhead moves | Built | `keyframe diamonds follow the playhead` | Fails on 7bb5058 ("at 1.2 s … the rail diamond still claims a keyframe is here"), passes after, 1280 + 380. The 61 other `keyframe` tests pass. |
| 4 | No visible play button | **Your decision + a design question.** Queue 364: *"Make it so that the play button is now the project time pill"*. A glyph inside the pill is a visual change, so per the design rule it needs pictures of options before anything is built. | | |
| 6 (old) | Effects library won't scroll | **Retracted, not a bug.** The review's test browser kept a 1280x720 real window under the 390x844 phone screen, so swipes starting below y=720 went nowhere. With the window fixed, the library and the blend-mode list both scroll from anywhere (measured: 205 px and 249 px). Replaced in the top 12 by "photos imported together stack at 0:00". | | |
| 5 | Moving a clip needs a hold nobody tells you about | Built (tip only, gestures untouched) | `tips how to move it` | Fails on 3e036fe ("FM._holdMoveTip is missing"), passes after at 380. The clip slice is 164/165 with the fix and 163/164 without it: the one failure, `914.9 Extract Audio is one undo step`, fails identically on the old code in that slice order and passes alone either way, so it is pre-existing and order-dependent. **Correction:** the desktop half of this finding was false. A quick mouse drag on a clip body MOVES it (measured: start 0 to 0.76 s, length unchanged); the "it trims" report came from the review browser's window bug. |
| 6 | Photos picked together stack at 0:00 | Built | `several photos from one pick` | Fails on 2199ad4 ("started at 0, 0, 0 s; they should run end to end at 0, 5, 10"), passes after at 380 + 1280. Import 26/26, media 33/33. Audio 44/47 both with and without the change: the three failures are this Linux browser (no H.264 encoder → `NO_VIDEO_CODEC`, and one wall-clock timing test), identical on the old code. The old stacked result stays one tap away in the toast ("Tap to stack them at the start instead"). |
| 7 | A finished export gives no "done" | Built | `a finished GIF or saved PNG frame` | Fails on 3d83b3f ("a finished GIF left no message behind"), passes after at 380. **Narrower than the review said:** MP4 already ends on its own card (queue 141 part 4); only GIF / PNG frames flashed "Done — saved to your Downloads." for 900 ms, and a single PNG frame said nothing. Export slice 82/96 with and without the change, same failure set (all `NO_VIDEO_CODEC` or layout checks this Linux browser cannot reproduce). `385: the release stands down during an export` failed once in one slice run with the change and passed on the rerun and twice alone, so it is intermittent; worth a look on the Mac. |
| 8 | The playhead can't go past the end, so the trim tools can't lengthen a project | Built (your length rule kept) | `typing a time past the end` | Fails on a8bb2ce ("snapped back in silence"), passes after at 380 + 1280. **Your rule stays:** the project is exactly as long as its clips (FM.autoFitDuration). Typing a time past the end now says so, and with a clip selected the toast offers "Tap to make “name” end at 6 s" through the same FM.extendClipTo the Extend button uses (a video still stops where its source runs out). **Also fixed on the way:** every Enter in the type-a-time box threw `NotFoundError` (removing the focused box fires its own blur, which ran the close twice); measured on the old code, now guarded, and the test asserts no uncaught errors. Readout 2/2; playhead 39/40 with and without the change (658 fails identically on the old code in this browser). |
